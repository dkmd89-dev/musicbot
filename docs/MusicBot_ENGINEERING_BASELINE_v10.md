# MusicBot Engineering Baseline v10

> **Status: DRAFT / IN PROGRESS — NICHT eingefroren.**
>
> Dieses Dokument ist der **laufende Zwischenstand** seit dem v9-Freeze
> (2026-09-07). Es wird pro ARCH-Phase / PR mit *ARCH Status*, *Recent Major
> Changes* und *Testzahlen* aktualisiert (CLAUDE.md §30) und erst zu einem
> ausdrücklichen Freeze-Zeitpunkt (nach 🟢-APPROVED-Freeze-Gate-Audit) zum
> eingefrorenen Referenzpunkt gemacht — dann werden die Platzhalter-
> Abschnitte 4–6 befüllt.
>
> **Bis zum v10-Freeze gilt:**
> - Eingefrorener technischer Referenzpunkt = `docs/MusicBot_ENGINEERING_BASELINE_v9.md`.
> - Aktueller Stand aller offenen/zurückgestellten Findings = `docs/FINDINGS_INDEX.md`.
> - Dieses Dokument dupliziert **keine** Findings — es listet nur die
>   Änderungshistorie seit v9 und die rollende Testzahl.

---

## 1. Metadaten

| Feld | Wert |
|---|---|
| Baseline | v10 (DRAFT) |
| Vorgänger | `docs/MusicBot_ENGINEERING_BASELINE_v9.md` (Freeze 2026-09-07, 2580 passed / 1 skipped / 0 failed) |
| Letzte vom Nutzer gemeldete Full-Suite-Zahl | **2920 passed, 1 skipped, 0 failed, 19 subtests passed** (Stand PR #176, 2026-09-08) |
| Letzte Full-Suite-Zahl (mit expliziter Nutzer-Freigabe vom Implementierungsprozess selbst ausgeführt, §8.A-Ausnahme) | **3186 passed, 1 failed (vorbestehend/unabhängig, `test_artist_overrides_orphan_cleanup.py` — 30 statt erwarteter 29 Keys, Datei bereits vor Sitzungsbeginn uncommitted verändert), 1 skipped, 11 subtests passed** (Stand: Family Hub F1–F3, 2026-09-12). Danach (F4/F5) nur gezielte + thematische Suiten (366 passed) — volle Suite steht erneut beim Nutzer aus. |
| Seither (PR #177/#178) | nur gezielte + thematische Suiten durch den Implementierungsprozess (CLAUDE.md §8.A); PR #178 fügt 3 Regressionstests hinzu (`test_mapping_additions_2026_09_08.py` = 14, `test_library_repair_executor_l2_real_pipeline.py` +2). Volle Suite steht beim Nutzer aus. |
| Letzte vom Nutzer gemeldete Full-Suite-Zahl (aktuell) | **3312 passed, 1 skipped, 11 subtests passed, 0 failed** (202,84 s) — Stand PR #203 (ARCH-021 Menu-Subsystem: Models-/Permissions-/Session-Extraktion, P-2/P-3/P-4), 2026-09-13. Der vorbestehende `test_artist_overrides_orphan_cleanup.py`-Failure aus Zeile oben ist damit ebenfalls nicht mehr vorhanden (zwischenzeitlich per Test-Nachzug behoben, siehe Abschnitt 3). |
| Letzte vom Nutzer gemeldete Full-Suite-Zahl (aktuell, nach PR #204) | **3361 passed, 1 skipped, 11 subtests passed, 0 failed** (215,46 s) — Stand PR #204 (ARCH-023 Menu-Router-/Permission-Architektur-Härtung, P-1–P-7), 2026-09-13. |
| Letzte vom Nutzer gemeldete Full-Suite-Zahl (aktuell, nach ARCH-024) | **3461 passed, 1 skipped, 11 subtests passed, 0 failed** (246,20 s) — Stand ARCH-024 (Menu-Datei-Dekomposition: Actions/Definitions/Rendering, P-1–P-4 COMPLETE, P-5 Onboarding NOT WARRANTED), 2026-09-13. +100 gegenüber PR #204, exakt deckungsgleich mit den neu hinzugekommenen ARCH-024-Testdateien (siehe `docs/MusicBot_ARCH-024_Menu_File_Decomposition.md`, Abschnitt „Test Results"). |
| Seither (ARCH-025) | keine neue Full-Suite-Zahl erhoben (Implementierungsprozess führt volle Suite nicht selbst aus, CLAUDE.md §8.A) — nur gezielte Characterization/Read-only-Verifikation vor jeder Änderung (siehe `docs/MusicBot_ARCH-025_Command_Help_Content_Decomposition.md`). Erwarteter Netto-Effekt auf die Testzahl: **-1** (Debt-Fix 3: `tests/test_menu_actions_stats.py` -3/+2, `tests/test_menu_definitions.py` +1) — keine Verhaltensänderung, volle Suite steht beim Nutzer aus. |
| Letzte vom Nutzer gemeldete Full-Suite-Zahl (aktuell, nach ARCH-025 + Closure) | **3460 passed, 1 skipped, 11 subtests passed, 0 failed** (244,26 s), 2026-09-13. Bestätigt exakt beide zuvor nur prognostizierten Effekte: PR #206s „netto -1" (Debt-Fix 3, bis dahin unbestätigt) + Closure-Phasen-Prognose „netto 0" (reine Text-Verschiebung nach `messages.py`, kein neuer/entfernter Test) — 3461 - 1 + 0 = 3460, exakt getroffen. 0 Regressionen. |
| Seither (ARCH-026) | keine neue Full-Suite-Zahl erhoben — ARCH-026 ist ein reines read-only Audit ohne Code-Änderung (Master-Prompt-Vorgabe: kein `pytest`, kein `compileall`). Testzahl unverändert: 3460 passed, 1 skipped, 11 subtests passed, 0 failed bleibt gültig. |
| Letzte vom Nutzer gemeldete Full-Suite-Zahl (aktuell, nach ARCH-027) | **3460 passed, 1 skipped, 11 subtests passed, 0 failed** (244,26 s), 2026-09-13. Exakt identisch zur Vor-ARCH-027-Zahl — 0 Regressionen, wie für eine additive, verhaltenserhaltende Konstruktor-Parameter-Änderung erwartet. |
| Letzte vom Nutzer gemeldete Full-Suite-Zahl (aktuell, nach ARCH-028) | **3490 passed, 1 skipped, 11 subtests passed, 0 failed** (280,76 s), 2026-09-13. +30 gegenüber der Vor-ARCH-028-Zahl (3460), 0 Regressionen — deckungsgleich mit den neuen F6-/F4-/F5-/Scheduler-Tests. |
| Zuwachs seit v9-Freeze | +340 passed (Stand PR #176) — Zwischenstand, nicht laufend nachgezogen; für den aktuellen Gesamtstand siehe die Zeilen „Letzte vom Nutzer gemeldete Full-Suite-Zahl" oben |
| Freeze-Status | offen — kein Freeze-Gate-Audit durchgeführt |

---

## 2. ARCH Status seit v9-Freeze

| ARCH-Änderung | PR | Ergebnis |
|---|---|---|
| **Artist Identity Resolution & Mapping Separation** (Phase A–F) — dedizierte `ArtistIdentityResolver`-Komponente als alleinige Identitäts-Auflösung; `ArtistNormalizer.normalize()` wird reine String-Normalisierung; `known`-Flag steuert AutoLearn; Library→`artist_overrides.json`-Persistenz entfernt. F-01…F-06 CLOSED, F-07 DEFERRED. Vollständiges Protokoll: `docs/audits/ARTIST_IDENTITY_RESOLUTION_MIGRATION_2026-09-08.md`. | #176 | 2920 passed / 1 skipped / 0 Regressionen |
| **INV-01 / F-08 Closure + mitgelaufene Fixes** — `INV-01` (`duplicate/cache.py` synchrone Event-Loop-Persistenz) als akzeptiertes Restrisiko geschlossen (analog DUP-05); toter Artist-/AutoLearn-Code entfernt (F-08). Im selben PR (als „chore" betitelt): deutsche „(MIT Artist)"-Feature-Credit-Regel in `light_title_cleanup()`; `FILENAME_TITLE_MISMATCH`-Normalisierung (`_normalize_for_compare()`: NFC/casefold/Klammer-Äquivalenz) im Health-Scanner; L2 `process_file(requested_issue=…)` für issue-spezifisches Reprocessing; manuelle Mapping-Ergänzungen. | #177 | thematische Suiten grün (volle Suite beim Nutzer) |
| **Doku-Nachzug + L2-Execute-Fix** — die vier in #177 als „chore" mitgelaufenen Änderungen mit eigener Findings-Zeile + Testbezug nachdokumentiert (`docs/FINDINGS_INDEX.md`). Dabei gefundener Bug: `apply_level2()` reichte `requested_issue` nur im DRY-RUN, nicht im EXECUTE durch (Vorschau ≠ `--apply`) — behoben, Regressionstest ergänzt. Charakterisierungstest für die manuellen Mapping-Ergänzungen. | #178 | gezielte + thematische Suiten grün (`-k "library_repair or library_health"` 431, `-k "title_clean or genre or mapping or artist"` 834; volle Suite beim Nutzer) |
| **Library Closure Phase** — 7 Lücken aus einer Read-only-Analyse in 7 PRs geschlossen: Grob-Disposition (`disposition_for_code()` → `AUTO_REPAIR`/`MANUAL_REVIEW`/`UNREPAIRABLE`) + Coverage-Matrix; `accept_finding`/`unaccept_finding`/`get_accepted_findings`/`get_review_summary`/`ReviewSummary` in `findings.py`; ID-basierte Review-CLI (`--accept`/`--unaccept`/`--accepted`/`--show`/`--summary`, additiv); Report-Tri-State 🔴 Offen / 🟢 Repariert / ⚪ Akzeptiert; Per-Repair-Verification je Datei (`_verify_issue_resolved()` → `UNRESOLVED` statt `SUCCESS`, wenn der Befund bleibt). „⚪ Akzeptiert" == bestehendes `FALSE_POSITIVE` (kein neuer Status). Telegram-UI für Accepted/Unaccept bewusst zurückgestellt (Core-API steht). Protokoll: `docs/audits/LIBRARY_CLOSURE_AUDIT_2026-09-09.md`. | (Branch `feat/library-closure-cp1`) | Vollsuite in 6 Etappen (Speicherdruck-Rechner): **3060 passed / 1 skipped / 2 failed** — die 2 Failures (`test_artist_overrides_orphan_cleanup.py`, hartkodierte Key-Count-Erwartung 21 vs. `mapping/artist_overrides.json` inzwischen 29) reproduzieren identisch auf `main` HEAD `7f76c51` (per `git stash` verifiziert), **unabhängig von dieser Phase** — 0 Regressionen |
| **ARCH-021 — Menu-Subsystem-Architekturmigration, P-1–P-4** — P-1 (read-only Audit): Entscheidung `handlers/menu/` bleibt bestehen, kein Top-Level `menu/` (Kopplung an Telegram durchdringt praktisch jede Methode, kein UI-agnostischer Kern vorhanden/nötig); Zielstruktur + Phasenplan P-2…P-9 festgelegt. P-2 Models Extraction: `handlers/menu/models.py` neu (`MenuState`/`AccessLevel`/`MenuItem`/`MenuSession`, unverändert verschoben). P-3 Permissions Extraction: `handlers/menu/permissions.py` neu (`is_admin_or_owner()`/`get_user_access_level()` vereinheitlichen die bisher unabhängigen, aber äquivalenten `RichMenuHandler._is_admin()`/`RichMenuSystem._is_admin_check()`; TGPERM-001-Schutz unverändert verifiziert). P-4 Session/State Extraction: `handlers/menu/session.py` neu (`SessionManager`, `RichMenuSystem.sessions`-Property mit Dict-Identität zum `SessionManager`-Live-Dict — bestehende direkte Zugriffe/Tests unverändert lauffähig). Alle drei neuen Module ausschließlich von `handlers.menu.models` abhängig, keine Abhängigkeit auf `rich_menu_system.py`/`rich_menu_handler.py`/Telegram. 4 bewusst zurückgestellte Legacy-Findings dokumentiert (`max_sessions` ohne Durchsetzung, `MenuSession.state`/`.data`/`.message_id` ungenutzt). Vollständiges Protokoll: `docs/MusicBot_ARCH-021_Menu_Architecture_Migration.md`. | #203 | Gezielt 65 passed, thematische Menu-Suite 390 passed, Vollsuite (Nutzer): 3312 passed / 1 skipped / 11 subtests passed / 0 failed |
| **ARCH-023 — Menu-Router- und Permission-Architektur-Härtung, P-1–P-7** — Fortsetzung des ursprünglich in ARCH-021/P-1 als „P-5 Router-Härtung" skizzierten Schritts unter neuer Nummer (eigene, bei P-1 neu beginnende Zählung). P-1/P-2 (read-only Audit + Characterization): Kernbefund `MenuItem.access_level` wurde im generischen `menu:`-Fallback nie vor dem Handler-Aufruf geprüft; 34 neue Characterization-Tests. P-3 Menu-Fallback-Gate: zentrale `is_accessible()`-Prüfung vor jedem `menu:`-Handler-Aufruf schließt die TGPERM-001-Fehlerklasse strukturell für alle `menu:`-Items; `doctor:`/`review:`/`repair:` auf `self._is_admin_check()` konsolidiert; `reprocess:` bewusst unverändert (OWNER-only); dokumentierte UX-Änderung (`show_alert=True` für `admin_users`/`admin_logs`/`admin_navidrome`-Ablehnung). P-4: `admin_navidrome`-`is_action`-Modellierungsinkonsistenz behoben, neue verhaltensbasierte TGPERM-001-Testklasse ergänzt (bestehende bleibt), `maintenance_gate.py` konsolidiert. P-5 `erradmin:`: `ErrorHandlerAdminInterface.is_admin()` fehlte der `OWNER_USER_ID`-Sonderfall (Initial-Commit-Code, älter als jede spätere Owner/Admin-Konsolidierung) — jetzt über `permissions.is_admin_or_owner()`. P-6: vollständiger repoweiter Permission-Audit inkl. `utils/` (16 Dateien), kein P0/P1-Fund, `utils/bot_restart_trigger.py`/`utils/navidrome_scan_trigger.py` als reine technische Utilities ohne eigene Autorisierungsverantwortung bestätigt. P-7: die letzten 5 eigenständig implementierten `OWNER_USER_ID or ADMIN_USER_IDS`-Duplikate (`library_doctor_handler.py`, `library_health_review_handler.py`, `repair_musicbot_handler.py`, `admin/bot_restart_handler.py`, `test_menu_handler.py`) auf `permissions.is_admin_or_owner()` konsolidiert, Defense-in-Depth-Struktur vollständig erhalten. Vollständiges Protokoll: `docs/MusicBot_ARCH-023_Menu_Router_Permission_Hardening.md`. Folgeblock `docs/MusicBot_ARCH-024_Menu_File_Decomposition.md` (Actions-/Definitions-/Rendering-Extraktion) — COMPLETE, siehe Zeile unten. | #204 | Kumulativ nach P-7: 348 passed, 0 Regressionen. Vollsuite (Nutzer): 3361 passed / 1 skipped / 11 subtests passed / 0 failed |
| **ARCH-024 — Menu-Datei-Dekomposition (Actions/Definitions/Rendering), P-1–P-4** — Fortsetzung des in `ARCH-021/P-1` als „P-6 Actions … P-9 Onboarding" skizzierten, durch `ARCH-023`s Nummernbelegung verdrängten Teils der Menü-Migration, unter eigener Zählung. P-1 (read-only Audit): Kernbefund — die drei bestehenden UI-Admin-Gruppen (`admin_group_library`/`_operations`/`_diagnostics`) sind die belastbarere fachliche Gliederung als die ursprüngliche `ARCH-021/P-1`-Hypothese; Architekturentscheidung, nur `handle_callback()` + 5 Kernmethoden als ROUTING zu klassifizieren, alle `_handle_<domäne>_callback`-Sub-Dispatcher als ACTION. P-2 Actions Extraction: 9 kohäsive Module unter `handlers/menu/actions/` (family/duplicates/navidrome/stats/admin_diagnostics/usermgmt/library/admin_operations/download) — zustandslose Funktionen mit explizit übergebenen Abhängigkeiten statt Actions-Klassen; 3 Testinfrastruktur-Kopplungen an alte Modulpfade gefunden und korrigiert (Patch-Ziele, Bare-Object-Konstruktion), keine Produktionsverhaltensänderung. P-3 Definitions Extraction: `handlers/menu/definitions.py` neu (`build_menu_tree()`/`populate_registry()`, einzige bewusste Ausnahme von der Rückreferenz-Regel — MenuItem-Handler-Bindungen zeigen weiterhin auf die dünnen `RichMenuSystem`-Delegatoren). P-4 Rendering Extraction: `handlers/menu/rendering.py` neu (`render_menu()`/`get_menu_text()`/`show_menu()`, vollständig rückreferenzfrei). P-5 Onboarding-Extraktion nach den 5 vorgegebenen Kriterien geprüft (fachliche Grenze/Eigenständigkeit/Komplexität/Testbarkeit/künstliche Abstraktion) und als **NOT WARRANTED** eingestuft — bleibt in `RichMenuHandler`. `rich_menu_system.py` 3117 → 1095 Zeilen (-65 %), `rich_menu_handler.py` 1608 → 1411 Zeilen (-12 %). Dependency-Graph zyklenfrei (AST-verifiziert), Router/Permissions/Session unverändert. Vollständiges Protokoll: `docs/MusicBot_ARCH-024_Menu_File_Decomposition.md`. | #205 | Thematisch: 535 passed (Menu-Sweep) / 1428 passed (breiter Nachbarthemen-Sweep), 0 Regressionen. `compileall` fehlerfrei. Vollsuite (Nutzer): **3461 passed / 1 skipped / 11 subtests passed / 0 failed** (246,20 s) |
| **ARCH-025 — Command/Help-Content-Dekomposition + ARCH-024-Debt-Fixes** — Teil A: `RichMenuHandler`s Onboarding-Content (personalisierte `/start`-Begrüßung, `/help`-Themenübersicht, Help-Callback-Routing, 4 statische Hilfetexte, Nutzerkontext-Auflösung/Feature-Katalog) nach neuem Paket `handlers/menu/content/` extrahiert (`greeting.py`/`help.py`/`user_context.py`) — `RichMenuHandler` behält nur dünne Delegatoren (Maintenance-Gate + Activity-Tracking + 1 Aufruf) sowie die 5 Delegator-Methoden `_load_user_data()`/`_get_user_info()`/`_is_new_user()`/`_get_user_role()`/`_get_available_features()` (bewusst nicht entfernt, da `tests/test_rich_menu_handler.py` sie direkt als gebundene Methoden aufruft). `handle_menu_command()` bewusst nicht extrahiert (bereits dünner Adapter). Teil B — die 3 ARCH-024-Remaining-Technical-Debt-Punkte nach erneuter Verifikation behoben: (1) stale Kommentar-Referenz in `rich_menu_system.py` auf `_handle_download_new()`/`_handle_download_history()` korrigiert (zeigt jetzt auf `actions/download.py`); (2) 6 tote Importe (`Any`/`MenuState`/`Path`/`json`/`timedelta`/`CallbackQueryHandler`) aus `rich_menu_system.py` entfernt; (3) 5 tote `_handle_stats_*`-Methoden (`rich_menu_system.py`) + 5 tote `stats_actions.handle_stats_*_system`-Funktionen (`actions/stats.py`) entfernt, `definitions.py` setzt für die 5 betroffenen MenuItems kein `handler=` mehr (Registrierung weiterhin unverändert per `register_handler()` zur Laufzeit). `rich_menu_handler.py` 1411 → 1109 Zeilen (-21 %). Dependency-Graph weiterhin zyklenfrei (AST-verifiziert, 14 Module), keine Rückreferenz von `content/` auf `rich_menu_handler.py`/`rich_menu_system.py`. Vollständiges Protokoll: `docs/MusicBot_ARCH-025_Command_Help_Content_Decomposition.md`. | #206 | Keine lokale Testausführung (Master-Prompt-Vorgabe); vollständige Characterization vor jeder Änderung bestätigte 0 Tests, die interne Struktur inspizieren. 2 Testdateien wegen Debt-Fix 3 angepasst (netto -1 Test). Vollsuite steht beim Nutzer aus. |
| **ARCH-025 Closure — Content Separation** — verbleibende statische UI-Texte aus `greeting.py`/`help.py` in neues `handlers/menu/content/messages.py` zentralisiert (reine Move-Operation, keine Textänderung): 33 String-Konstanten (12 Greeting-Texte, 14 Help-Texte inkl. der 4 vormals in `help.py` liegenden statischen Hilfetexte, 6 mehrfach verwendete Button-Labels, 1 generischer Fehlertext). `greeting.py`/`help.py` bleiben für Orchestrierung (Kontext laden, dynamische Werte einsetzen, Telegram-Versand) zuständig; `get_download_help()`/`get_stats_help()`/`get_navidrome_help()`/`get_admin_help()` sind jetzt dünne Wrapper um `messages.HELP_*`. Einmalig verwendete Labels (`⬅️ Zurück zur Hilfe`, `❓ Hilfe`) bewusst lokal belassen (keine erzwungene Zentralisierung ohne Duplizierung). `user_context.py` unverändert (FEATURES-Katalog bleibt strukturierte Metadaten, kein Text). `messages.py` hat null Projekt-Logik-Abhängigkeiten (verifiziert: keine Imports). Die 3 ARCH-024/025-Technical-Debt-Punkte (tote Importe/stale Kommentar/tote Stats-Methoden) per erneutem `grep`-Audit als bereits behoben bestätigt (keine erneute Änderung nötig). Vollständiges Protokoll: `docs/MusicBot_ARCH-025_Menu_Command_Help_Content_Closure.md`. | #207 (gemergt) | Keine lokale Testausführung, kein `compileall` (Master-Prompt-Vorgabe, härter als die reguläre §8.A-Regel). Statische Verifikation: AST-Parse aller 3 geänderten/neuen Dateien fehlerfrei, alle 33 Konstanten Zeichen-für-Zeichen gegen Originaltexte verglichen (PASS), `git diff --stat` bestätigt Scope-Discipline (nur `greeting.py`/`help.py`/`messages.py` betroffen). Vollsuite (Nutzer, nach Merge): 3460 passed / 1 skipped / 11 subtests passed / 0 failed (244,26 s), bestätigt exakt prognostiziert. |
| **ARCH-026 — Enhanced Error Handler Integration & Coverage Audit** — read-only Architektur-/Integrations-/Coverage-Audit von `handlers/enhanced_error_handler.py`. Kernbefund: zwei unabhängige `EnhancedErrorHandler`-Instanzen (bot.py „A", PTB-registriert; `RichMenuHandler.initialize()` „B", an fast alle Sub-Handler propagiert), nie synchronisiert (`RichMenuHandler.set_error_handler()` hat keinen externen Aufrufer) — bewertet als **C — DUPLICATED / INCONSISTENT**, nicht als bewusste Entscheidung dokumentiert. Konsequenzen: Admin-Monitoring (`/error_stats` u. a.) sieht nur Instanz A, ~90 % der tatsächlichen `handle_callback_error`/`handle_command_error`-Aufrufe (Instanz B) bleiben unsichtbar; `recovery_attempts` von Instanz B wächst unbegrenzt (kein erreichbarer Reset). Zusätzlich: Download-Pipeline (`klassen/download_handler.py`, P0) hat 0 `error_handler`-Referenzen (läuft komplett außerhalb beider Instanzen); `handle_async_exceptions`/`handle_sync_exceptions`-Decorators 0 produktive Verwendungen plus latenter `AttributeError`-Bug (`Config` hat kein `.get()`); 4 Handler (Doctor/Review/Repair/Reprocessing) erhalten `error_handler` injiziert, nutzen ihn aber nie (kein Coverage-Loch dank zentralem `RichMenuSystem.handle_callback()`-Catch-All). 12 neue Findings F1–F12 in `docs/FINDINGS_INDEX.md` aufgenommen. **Keine Code-Änderung in dieser Phase** — Fix-Plan für F1-F4 dokumentiert, für eine eigene künftige Phase zurückgestellt (Risiko/Blast-Radius zu hoch für eine ungetestete Änderung an der Bot-Initialisierung). Vollständiges Protokoll: `docs/MusicBot_ARCH-026_Error_Handler_Integration_Audit.md`. | #208 (gemergt) | Reines Audit, keine Tests ausgeführt (Master-Prompt-Vorgabe). Testzahl unverändert: 3460 passed / 1 skipped / 11 subtests passed / 0 failed. |
| **ARCH-027 — Error Handler Consolidation** — setzt den in ARCH-026 zurückgestellten Fix-Plan für F1-F3 um. Ursache der leeren Telegram-Admin-Berichte bewiesen (nicht nur vermutet): `ErrorHandlerAdminInterface` (einzige Instanz, aus `bot.py`s Instanz A gebaut) beobachtete die falsche von zwei `EnhancedErrorHandler`-Instanzen — die tatsächlichen `handle_callback_error`/`handle_command_error`-Aufrufe liefen über Instanz B. Fix: `RichMenuHandler.__init__()` erhält neuen optionalen Parameter `error_handler: Optional[EnhancedErrorHandler] = None`; `bot.py` injiziert seine Instanz A (`RichMenuHandler(self.config, error_handler=self.error_handler)`); `RichMenuHandler.initialize()` erzeugt nur noch einen kontrollierten Fallback, wenn keine Instanz injiziert wurde (Standalone-/Test-Konstruktion) — kein stilles Überschreiben einer bereits injizierten Dependency. Variante A (Constructor Injection) gewählt statt B (Setter) oder eigenständiger `set_error_handler()`-Nutzung (letztere ohnehin unvollständig gegenüber `initialize()`s direkter 11-Handler-Propagation, siehe Analyse im Protokoll). Nur 2 Dateien geändert (`bot.py`, `handlers/menu/rich_menu_handler.py`), additiv, alle 4 bestehenden `RichMenuHandler(config)`-Testkonstruktionsstellen unverändert kompatibel. `enhanced_error_handler.py` selbst nicht angefasst — Decorators/Dead-Code (F5/F9/F10) und Download-Pipeline-Lücke (F4) bewusst außerhalb des Konsolidierungs-Scopes zurückgestellt. F1/F2/F3 in `docs/FINDINGS_INDEX.md` auf CLOSED gesetzt. Modularisierungs-Assessment für `enhanced_error_handler.py` durchgeführt: NOT WARRANTED (kein akuter Bedarf, analog ARCH-024/P-5). Vollständiges Protokoll: `docs/MusicBot_ARCH-027_Error_Handler_Consolidation.md`. | #209 (gemergt) | Statisch verifiziert vor Implementierungsabschluss: `ast.parse()` beider geänderten Dateien fehlerfrei, Instanz-Zählung/Propagationskette/Callback-/Permission-Flow per Grep nachvollzogen. Vollsuite (Nutzer, 2026-09-13): **3460 passed / 1 skipped / 11 subtests passed / 0 failed** (244,26 s) — exakt identisch zur Vor-ARCH-027-Zahl, 0 Regressionen. |
| **ARCH-028 — Error Handler Closure** — schließt F4/F5/F6 aus dem ARCH-026-Audit ab. F6: die 4 Handler mit zuvor injiziertem, aber ungenutztem `error_handler` (Reprocessing/Doctor/Review/Repair) rufen jetzt in ihren generischen `except Exception`-Hintergrund-Task-Pfaden tatsächlich `handle_exception()` auf (erwartete, bereits lokal behandelte Fehler wie `HealthScanFailedError`/`RepairAlreadyRunningError`/`FindingsRegistryError` bleiben bewusst lokal). F4: `DownloadHandler` erhält einen optionalen `error_handler`-Parameter (Klassenattribut-Fallback für 10 bestehende `object.__new__(DownloadHandler)`-Testdateien); das Hintergrund-Task-Sicherheitsnetz `_log_background_download_task_exception()` meldet zusätzlich zentral. F5: `self.config.get(...)` → `getattr(self.config, ...)` (der dokumentierte Bug) UND ein beim Testen entdeckter zweiter Bug (`asyncio.create_task()` ohne laufenden Event-Loop im Sync-Decorator, behoben mit `asyncio.get_running_loop()`-Check + `asyncio.run()`-Fallback) — beide Decorators bleiben als LEGACY API bestehen, weiterhin 0 produktive Aufrufer. Anschließende Scheduler-Verifikation (statisch, deterministisch statt Live-Bot-Betrieb) bestätigte FALL B: `FamilyChallengeScheduler` hatte 0 Error-Handler-Integration; zusätzlich dabei entdeckt und behoben: Live-Bug `Config.FAMILY_CHALLENGE_TIME` als Klassen- statt Instanzzugriff auf eine `@property` (`'property' object has no attribute 'split'`) — `FamilyChallengeScheduler` erhält jetzt `config`+`error_handler` von `bot.py` injiziert, alle 3 Exception-Pfade (Daily-Loop/pro Familie/pro Broadcast-Empfänger) melden zentral, Fehlerisolation unverändert erhalten. Vollständiges Protokoll: `docs/MusicBot_ARCH-028_Error_Handler_Closure.md`. | — (noch nicht gemergt) | Gezielt nach jedem Punkt grün (F6 125, F4 63, F5 26, Scheduler 16). Thematischer Sweep: 1323 passed, 0 failed. Vollständige Suite (Nutzer): **3490 passed / 1 skipped / 11 subtests passed / 0 failed** (280,76 s), +30 gegenüber Vor-ARCH-028-Stand, 0 Regressionen. |

---

## 3. Recent Major Changes (seit v9-Freeze)

| Datum | Änderung | PR | Testzahl danach |
|---|---|---|---|
| 2026-09-08 | Artist-Identity-Resolution-Migration (Phase A–F): `services/metadata/artist_identity_resolver.py` neu; Priorität `artist_override > known_artist > auto_learned_alias > library_identity > musicbrainz_mbid > parser`; `MetadataResult.artist_known`; `EnhancedMetadataProcessor`/`AutoLearnManager`/`DuplicateDetector`/`utils/artist_map.py` umgestellt; toter Alias-Code entfernt. `mapping/`-Dateien inhaltlich unverändert. | #176 | 2920 passed |
| 2026-09-08 | Testinfra: `handlers/test_menu_handler.py::TestMenuHandler.__test__ = False` (pytest-Collection-Sperre, beseitigt 3 Collection-Warnings). | #176 | 2920 passed |
| 2026-09-08 | INV-01 (akzeptiertes Risiko) + F-08 (toter Code) CLOSED. Mitgelaufen: `light_title_cleanup()` deutsche „(MIT Artist)"-Regel; `_normalize_for_compare()` NFC/casefold/Klammer-Äquivalenz gegen `FILENAME_TITLE_MISMATCH`-False-Positives; `process_file(requested_issue=…)` + `apply_level2()`-Durchreichung (DRY-RUN); manuelle Mapping-Ergänzungen (new wave→Pop, Piano Pop→Pop, lea→LEA, +4 known_artists). | #177 | thematische Suiten grün |
| 2026-09-08 | `FINDINGS_INDEX.md`: 4 Nachzugs-Zeilen für die #177-„chore"-Änderungen. Bugfix: `apply_level2()` EXECUTE-Zweig reicht `requested_issue` jetzt ebenfalls durch (war nur DRY-RUN → Vorschau ≠ `--apply`). Neu: `test_mapping_additions_2026_09_08.py` (14), 2 Regressionstests in `test_library_repair_executor_l2_real_pipeline.py`, 3 lokale Reprocess-Stubs auf reale Signatur nachgezogen. | #178 | gezielte + thematische Suiten grün |
| 2026-09-09 | Finding A (Download-Pipeline-Testlauf, live): Feature-Artists aus einem `feat.`/`ft.` **im Titel** gingen komplett verloren — `EnhancedMetadataProcessor` las `youtube_parsed["featuring"]` nie. Additiv zusammengeführt. E2E-Regressionstest (`test_metadata_processor_happy_path.py` +2), Pre-Fix-Diskriminierung bestätigt. | #180 | `-k "metadata or feat or … or youtube_parser"` 592 grün |
| 2026-09-09 | Finding F: deutscher Video-Marker (`(Offizielles Musikvideo)`) klebte am Feature-Namen — `_extract_features()` fängt jetzt nur bis zur ersten öffnenden Klammer. Durch PR #180 von P3 auf P1 gehoben (Wert landet seither sichtbar im Tag). `test_youtube_parser_feat_video_marker.py` (neu, 8). | #182 | `-k "youtube_parser or duplicate or metadata or feat or title_clean or artist"` 1133 grün |
| 2026-09-09 | Finding C: 8-Zeichen-All-Caps-Kanalname „MARTERIA" wurde als Akronym gelernt + nach `case_preserve.yaml` persistiert → jedes „Marteria" → „MARTERIA". RULE-2-Schwelle `<= 8` → `<= 6`. `test_artist_normalizer.py` +4. (black hat `utils/artist_map.py` beim selben Commit vollständig neu formatiert — Tabs→Spaces, keine Logikänderung, per Suite verifiziert.) | #183 | `-k "artist or normaliz or duplicate or metadata or case_preserve or genre"` 1247 grün |
| 2026-09-09 | Finding B: Fuzzy-Channel-Match (`WRatio`, Schwelle 75) matchte `Akon`→`kontor.tv` (77) und `Digster Pop Music`→`vibe music` (85) → falsches `channel_fuzzy`-Genre `Electronic`. Schwelle Channel-Fuzzy **75 → 90** (Artist-Fuzzy unverändert 85). `test_genre_map_channel_fuzzy_false_positive.py` (neu, 6). | #185 | `-k "genre or metadata or channel or auto_learn"` 638 grün |
| 2026-09-09 | Finding H (durch B-Fix sichtbar): Last.fm-User-Tags (`laut mitsing`/`eingedeutscht`/`radio`) wurden im `prioritize_genres()`-Fallback-Zweig blind zu `©gen: Laut Mitsing`. Fallback verlangt jetzt ein als Genre erkennbares Tag; sonst generisches Genre (`pop`) als Notnagel, sonst `Unknown`. `test_genre_processor.py` +3, 1 ARCH-022-Charakterisierungstest auf Sollverhalten umgestellt. | #187 | `-k "genre or musicbrainz or metadata or auto_learn or lastfm"` 651 grün |
| 2026-09-09 | **Download-Pipeline-Optimierung K1** — Loudness in Schritt 15b lief als voller AAC→AAC-Re-Encode (`AudioEnhancer.normalize_loudness()`, ~22 s = ~49 % der Pipeline). Ersetzt durch `services/metadata/loudness_replaygain.py::apply_replaygain_tags()`: EBU-R128-Scan (rsgain ~1,5 s / FFmpeg-Fallback) + verlustfreier `replaygain_track_gain`/`_peak`-Tag (Navidrome), Audio byte-identisch. Schließt das DEFER-Finding „Schritt 15b LUFS-Ziel verifizieren". `test_loudness_replaygain.py` (neu, 12), 8 EMP-Testdateien auf den neuen Hook umgestellt. | #188 | `-k "download or metadata or reprocess or duplicate or enhanced or loudness"` 1165 grün |
| 2026-09-09 | **Download-Pipeline-Optimierung H2** — `MusicBrainzClient.fetch_metadata()` wird pro Track ~2× aufgerufen (Genre + Album); die Suche war gecacht, der teure `get_recording_by_id()`-Detail-Call lief jedes Mal (~1 s + MB 1-req/s-Limit). Jetzt unter `recording_detail:<mbid>` im selben TTLCache. `test_musicbrainz_client.py` +1. | #189 | `-k "musicbrainz or genre or album or cover or metadata_processor or enhanced_metadata"` 579 grün |
| 2026-09-09 | **Download-Pipeline-Optimierung H1** — yt-dlp-Metadaten wurden pro Download 2× extrahiert (Duplicate-Vorab-Probe + Download-Pfad, je ~3-4 s). Prozessweiter Kurzzeit-Cache in `DownloadExecutor.extract_info_async(use_cache=True)` (TTL 120 s, nur `download=False`, Mix-URLs ausgenommen, Retry ≥ 1 holt frisch). `test_download_executor.py::TestExtractInfoCache` (+6). | #190 | `-k "download or duplicate or metadata_processor or enhanced_metadata or youtube"` 888 grün |
| 2026-09-09 | **Download-Pipeline-Optimierung H3** — Cover-Quellen-Suche lief sequenziell (allein Cover Art Archive ~6 s). Primär-Tier (Prio ≥ 50: CAA/Fanart Album/Apple/Deezer/Fanart Artist) jetzt parallel via `ThreadPoolExecutor`, danach einmal Early-Exit/Ranking; YouTube-Fallbacks (Prio 30) nur bei Bedarf, sequenziell. Gewinner-Auswahl unverändert. `test_cover_processor_validation.py` (Orchestrierung + Timing-Beweis). | #191 | `-k "cover or metadata_processor or enhanced_metadata or download"` 524 grün |
| 2026-09-09 | **Download-Pipeline-Optimierung M1** — Schritt 19d `ArtistIdentityResolver.refresh()` (Library-Disk-Walk + 3 YAML-Reloads) lief pauschal bei jedem `known=False`-Track. Jetzt nur bei tatsächlicher Änderung: neuer Artist-Ordner ODER AutoLearn-Schreibvorgang (`learn_artist()`-Bool / `LEARNED`/`UPDATED`-Feature-Entscheidung ausgewertet). `test_metadata_processor_happy_path.py` +2. | #192 | artist-identity/autolearn/EMP/playlist/reprocess 217 grün |
| 2026-09-09 | Finding I (Test-Bot-Re-Download): Live-/TV-Session-Marker mit „ @ " klebte am Songtitel („Leony - Remedy @ Deluxe Music Session 2022" → „Remedy @ Deluxe Music Session" in Titel-/Album-Tag + Dateiname). `_clean_title_suffixes()` schneidet ein Titelende-`\s+@\s+.+$` jetzt ab (vor der Upload-Jahr-Regel), nur im Erfolgspfad, nicht in `light_title_cleanup()`. `test_youtube_parser_live_session_suffix.py` (neu, 10), Pre-Fix-Diskriminierung via `git stash` bestätigt. | #198 | `test_youtube_parser*` + `test_title_cleanup_pure.py` 97 grün (volle Suite beim Nutzer) |
| 2026-09-09 | Finding E Folge-Fund (Fritz Kalkbrenner „Sky and Sand", Re-Download): Aufnahme existiert in MB nur auf Compilations/DJ-Mixen → `_select_primary_release()` liefert zwangsläufig eine Compilation, deren Datum (2018) wurde trotzdem das Jahr. Jetzt: bei „nur Compilations" kein `release_date` aus Release-/RG-Datum → `AlbumProcessor` fällt auf YouTube-/Upload-Jahr zurück; `release_id`/`album` unverändert vom frühesten Release. `first-release-date` am Recording bleibt maßgeblich. `TestSelectPrimaryReleaseFindingE` (+2, 1 umbenannt). | #197 | `test_musicbrainz_client.py` thematisch grün (volle Suite beim Nutzer) |
| 2026-09-09 | Finding E (Download-Pipeline-Log-Analyse): MusicBrainz-`release-list` einer Aufnahme ist unsortiert — `_build_metadata()` nahm blind `release_list[0]` für Album/Jahr/`release_id`, real oft eine Compilation/DJ-Mix (Akon „Smack That" → 2009 statt 2006). Neu: `_select_primary_release()` stellt Compilations zurück und wählt die früheste datierte Veröffentlichung; `release_date` fällt zusätzlich auf `first_release["date"]` zurück. `first-release-date` von Recording/Release-Group bleibt höher priorisiert. `test_musicbrainz_client.py::TestSelectPrimaryReleaseFindingE` (+4), Pre-Fix-Diskriminierung via `git stash` bestätigt. | #196 | `test_musicbrainz_client.py` + `test_album_processor.py` 52, `test_genre_processor.py` + happy path 54 grün (volle Suite beim Nutzer) |
| 2026-09-09 | Finding G (Download-Pipeline-Log-Analyse): MusicBrainz-`TimeoutError` in `fetch_metadata()` lief in den generischen `except Exception`-Zweig → `💥 Unerwarteter Fehler` + Stacktrace auf ERROR, obwohl der Last.fm-Fallback der Normalfall ist. Eigener `except asyncio.TimeoutError` vor dem generischen, WARNING ohne exc_info. `test_musicbrainz_client.py` +1, Pre-Fix-Diskriminierung via `git stash` bestätigt. | #195 | `test_musicbrainz_client.py` 31 grün (volle Suite beim Nutzer) |
| 2026-09-09 | Finding „Fritz & Paul Kalkbrenner" (Download-Pipeline-Testlauf, live): gemeinsamer Nachname — `_split_multi_artists()` zerlegt „Fritz & Paul Kalkbrenner" in `["Fritz", "Paul Kalkbrenner"]`, der abgeschnittene Primär „Fritz" ist eine andere, reale Band → Last.fm-Tags → Genre „Indie", Ordner „Fritz/". Generischer Split-Fix unsicher (echte Doppel-Primär-Acts) → gezielter Mapping-Override (8 Kollab-Schreibweisen + Self-Eintrag → „Fritz Kalkbrenner", Nutzer-Entscheid: Paul als Zweit-Artist) **plus** neuer `EnhancedMetadataProcessor`-Schritt 6a: der ungesplittete Roh-Artist-String (`raw_artist_string` / `", ".join(all_artists)`) wird gegen die Override-Stufe des `ArtistIdentityResolver` geprüft; greift ein Override, das den abgeschnittenen ersten Künstler erweitert, wird `all_artists[0]` ersetzt. Bare „Fritz" unberührt. `test_collab_artist_shared_surname_kalkbrenner.py` (neu, 10), Pre-Fix-Diskriminierung via `git stash` bestätigt. | #194 | `-k` artist/identity/override/youtube_parser/metadata_processor/genre/special_channel thematisch grün (volle Suite beim Nutzer) |
| 2026-09-09 | Nachprüf-Durchgang `requested_issue` im Telegram-Doctor-/Repair-MusicBot-Pfad: **kein Finding** — `apply_level2()`/`requested_issue` sind CLI-only, die Telegram-Pfade (`doctor_runner.py`, `repair_service.py`) laufen ausschliesslich `--level SAFE_AUTOMATIC` (L2 doppelt ausgeschlossen: Planner-Level-Filter + `l2_requested`-Gate), die „Reprocessing"-Ansicht ruft `process_file()` ohne den Parameter. Sicherheitsgrenze in `test_library_repair_cli_safe_automatic_scope.py` (2) gepinnt; `docs/LIBRARY_REPAIR.md` §6a um einen Reichweiten-Absatz ergänzt. Kein Produktionscode geändert. | #179 | `-k "library_repair or planner or doctor or repair_service or repair_integration or repair_musicbot"` 285 grün |
| 2026-09-09 | **Library Closure Phase** (Branch `feat/library-closure-cp1`, 7 PRs A–G): (A) `disposition_for_level()`/`disposition_for_code()` in `planner.py` + `docs/audits/LIBRARY_CLOSURE_COVERAGE_MATRIX_2026-09-09.md` + `test_library_repair_disposition_matrix.py` (11) — 53 Codes = 29 AUTO_REPAIR / 22 MANUAL_REVIEW / 2 UNREPAIRABLE. (B) `accept_finding`/`unaccept_finding`/`get_accepted_findings`/`get_review_summary`/`ReviewSummary` in `services/library_health/findings.py` (+19 Tests) — „⚪ Akzeptiert" == `FALSE_POSITIVE` (kein neuer Status, Nutzerentscheidung); `unaccept_finding` = neue Reaktivierung → `OPEN`, vom Scan-Merge getrennt. (C) `scripts/library_health_review.py` `--accept`/`--unaccept`/`--accepted`/`--show`/`--summary` additiv (+10 Tests), interaktiver Kategorie-Review unverändert Default. (D) `report.py` Labels 🔴 Offen / 🟢 Repariert / ⚪ Akzeptiert (abwärtskompatibel). (E) `_verify_issue_resolved()` in `executor.py` — nach jedem echten `SUCCESS` bei `apply_level1`/`_rename`/`_cover_repairs`/`_external_metadata` frische Einzeldatei-Health-Analyse → `ExecOutcome.status = "UNRESOLVED"`, wenn der Befund bleibt (Journal ebenso); album-scope + `LOUDNESS_OFF_TARGET` + L2 ausgenommen; `scripts/library_repair.py`: UNRESOLVED in Tally, „geschrieben" = SUCCESS ∪ UNRESOLVED. `repair_service` markiert ein UNRESOLVED-Finding weiterhin nicht als RESOLVED. (+13 Tests: 11 Verify, 2 Idempotenz). (F) §41-End-to-End-Closure + §35-Idempotenz in `test_repair_integration.py` (+2). (G) Doku (`LIBRARY_HEALTH.md` §1a, `LIBRARY_REPAIR.md` §5b, `INDEX.md`, `FINDINGS_INDEX.md`) + `docs/audits/LIBRARY_CLOSURE_AUDIT_2026-09-09.md`. **Zurückgestellt:** Telegram-UI für Accepted-Ansicht/Unaccept (Core-API + Modelle stehen). | #199 (`655cd04`) | Vollsuite in 6 Etappen: **3060 passed / 1 skipped / 2 failed** — die 2 Failures (`test_artist_overrides_orphan_cleanup.py`) sind vorbestehend auf `main` (per `git stash` gegen HEAD verifiziert), **0 durch diese Phase verursachte Regressionen**. Vorbestehender Fehler wird nicht ungefragt behoben (CLAUDE.md §8.A). |
| 2026-09-09 | **Telegram-UI für ⚪ akzeptierte Findings + Unaccept** — direkt im Anschluss an die Closure Phase (nach CLI-Test des Nutzers gegen die echte Registry). `handlers/library_health_review_handler.py`: `handle_accepted_list`/`handle_accepted_category`/`handle_accepted_show`/`handle_unaccept`. `rich_menu_system.py`-Review-Dispatcher: `review:accepted`/`review:acccode:<code>`/`review:accshow:<fid>`/`review:unaccept:<fid>`. Übersicht zeigt „⚪ Akzeptierte Findings (N)" nur wenn vorhanden → Liste nach Code → Detail mit Grund/Scan-Status → „↩️ Reaktivieren" (`unaccept_finding()` → OPEN, Stale-Revalidierung wie bei allen Statusänderungen). Rein lesend bis zum expliziten Tap; stale mit ⚠️. Kein neues Menüsystem, gleiche `FindingsRegistry`-API + Admin-Gating. `test_library_health_review_handler.py` +11, `test_rich_menu_review.py` +8. | #200 (`0ba9138`) | Vollsuite in 6 Etappen: **3079 passed / 1 skipped / 2 failed** (die 2 vorbestehenden `test_artist_overrides_orphan_cleanup.py`-Failures; +19 = die neuen Tests, sonst 0 Regressionen) |
| 2026-09-09 | **Test-Nachzug `test_artist_overrides_orphan_cleanup.py`** — die beiden seit PR #194 vorbestehenden Failures behoben: PR #194 ergänzte 8 legitime Keys (`Fritz & Paul Kalkbrenner`-Kollab → `Fritz Kalkbrenner`, ein realer Artist), zog aber diesen Daten-Integritätstest nicht mit. `WHITELIST_VALUES_LOWER` += `fritz kalkbrenner`, Key-Count-Erwartung `21 → 29`, Nachtrag-Kommentar. Keine Änderung an `mapping/artist_overrides.json`. | (Branch `fix/artist-overrides-orphan-test-catchup`) | Vollsuite jetzt **3081 passed / 1 skipped / 0 failed** |
| 2026-09-12/13 | **Family Hub (Phase F1–F5)** — private Familienstruktur (explizit konfigurierte, bereits registrierte Telegram-Nutzer, keine erfundenen IDs, keine automatische Zuordnung) über 5 Phasen: **F1** `services/family/family_service.py`/`family_repository.py` (Membership/Berechtigung, `data/family_data.json`, atomar). **F2** `family_stats_service.py` — aggregiert die bestehende `PlayHistoryRepository`/`StatisticsCalculator`-Infrastruktur unverändert über mehrere Navidrome-User (keine zweite Statistik-Engine); neuer Menüzweig „👨‍👩‍👧‍👦 Familien-Statistik" unter dem bestehenden `stats`-Menü. **F3** `family_chat_service.py`/`family_message_repository.py` — bot-interner Chat, Broadcast an aktive Mitglieder mit Notifications, Pending-State-Textfluss analog `NavidromeMenuHandler.browse_states` statt des `user_mgmt_handler`-gebundenen `TextWorkflowDispatcher`; neues Top-Level-Menü „💬 Familien-Chat". **F4** `family_challenge_service.py`/`family_challenge_repository.py` — tägliche Musik-Challenge, 3 deterministisch gewählte Typen aus der Play-History (2 der 5 Master-Prompt-Beispiele bewusst zurückgestellt, siehe `FINDINGS_INDEX.md`), idempotente Tageserzeugung, `FamilyChallengeScheduler` (`handlers/family_challenge_scheduler.py`) nach dem bestehenden `PlayHistoryPoller`-asyncio-Muster (kein neuer Scheduler), in `bot.py` gestartet/gestoppt; neues Top-Level-Menü „🎯 Familien-Challenge". **F5** Hardening-Audit (Berechtigungen/Isolation/Tagesgrenzen/Mitglieder ohne Plays) — 2 echte Lücken gefunden und geschlossen (Mitglieder-ohne-Plays-Charakterisierung, Cross-Family-Challenge-ID-Kollisions-Isolationstest), sonst bereits abgedeckt. Vollständige Doku: `docs/MusicBot_TELEGRAM_MENU_SYSTEM.md` Abschnitt 6. Dabei 1 echte Regression selbst gefunden+behoben: `RichMenuHandler.__init__()` deklarierte `family_stats_handler`/`family_chat_handler`/`family_challenge_handler` nicht als `None`-Default (anders als alle übrigen Handler) → `AttributeError` in `_record_initial_handler_statuses()`. | (kein PR/Branch — direkt auf `main`, Commit steht aus) | Thematische Regressionsgruppe (F1–F5 + Statistik + User Management + Menü + Poller-Muster): **366 passed, 0 Regressionen**. Volle Suite (mit Nutzer-Freigabe ausgeführt, Stand F1–F3): **3186 passed / 1 failed (vorbestehend, `test_artist_overrides_orphan_cleanup.py` — 30 statt 29 Keys, Datei war bereits vor Sitzungsbeginn uncommitted durch Live-Auto-Learning verändert, unabhängig von Family Hub) / 1 skipped**. Volle Suite nach F4/F5 steht beim Nutzer aus. |
| 2026-09-12 | **TGPERM-001 (Telegram Permission-Bypass, Logger-/Test-System-Menü) — gefunden + behoben** — Full-Project-Architecture-Audit im Anschluss an den Family-Hub-Merge (`docs/audits/FULL_PROJECT_ARCHITECTURE_AUDIT_2026-09-12.md`) fand: 7 Logger-MenuItems (`logger_main_menu` u. a.) und 3 Test-System-MenuItems (`test_unit`/`test_integration`/`test_performance`) trugen `access_level=AccessLevel.ADMIN`, ihr automatisch generiertes `callback_data` (`"menu:<id>"`) erreichte aber keinen der in `_ADMIN_ONLY_PREFIXES` gegateten Präfixe und wurde über den generischen `menu:`-Fallback in `RichMenuSystem.handle_callback()` ohne jede Berechtigungsprüfung ausgeführt — jeder Bot-Nutzer konnte u. a. das globale Log-Level ändern (hebelt den SEC-001-Schutz aus) oder einen bis zu 900s-Testlauf auslösen. Der ursprüngliche SEC-003-Regressionstest testete dabei den literalen ID-String statt des real gesendeten `callback_data` und schlug deshalb nie an. Fix: die 7 Logger-Items routen jetzt über explizites `callback_data="logger_<id>"` in den bereits vorhandenen, gegateten `logger_`-Präfixpfad (die 7 dadurch obsoleten `_handle_logger_*`-Wrapper entfernt); `TestMenuHandler._execute_test_run()` erhielt einen eigenen `_is_admin()`-Check (Defense-in-Depth-Muster wie `admin_users`/`admin_logs`/`admin_navidrome`). Neuer Registry-weiter Sweep-Test `TestPrivilegedMenuItemsAreGatedTGPERM001` verhindert dieselbe Fehlerklasse für zukünftige Admin-Menüpunkte. Pre-Fix-Diskriminierung via `git stash` bestätigt (3 neue Tests schlagen ungefixt fehl). Vollständiges Protokoll: `docs/audits/FULL_PROJECT_ARCHITECTURE_AUDIT_2026-09-12.md` (Nachtrag + Abschnitt 5/6/27), `docs/FINDINGS_INDEX.md`. | (kein PR/Branch — direkt auf `main`, Commit steht aus) | Thematische Suite (Rich-Menu/Logger/Test-Menu/User-Management/Navidrome/Maintenance/Family): **513 passed, 0 Regressionen**. `python3 -m compileall -q .` fehlerfrei. Volle Suite steht beim Nutzer aus. |
| 2026-09-12 | **Test-Nachzug `test_artist_overrides_orphan_cleanup.py`** — beim Full-Suite-Lauf des Nutzers (nach dem TGPERM-001-Fix) sichtbar geworden: ein echter, regulärer Download während laufender Entwicklungsarbeit hatte das Auto-Learn-System zusätzlich zum bestehenden Key `toobrokeforfiji` einen weiteren Key `fiji` → `Toobrokeforfiji` schreiben lassen (derselbe, bereits seit 2026-09-03 whitelistete Wert, keine neue Person) — Key-Count-Erwartung `29 → 30` nachgezogen (analog zum bereits einmal aufgetretenen Muster bei PR #194/`fix/artist-overrides-orphan-test-catchup`). Keine Änderung an `mapping/artist_overrides.json` selbst. | (kein PR/Branch — direkt auf `main`, Commit steht aus) | `test_artist_overrides_orphan_cleanup.py` 3/3 grün; thematisch (`auto_learn or case_preserve or artist_alias or featured_artist or genre_map or artist_overrides`) 149/149 grün. Volle Suite steht erneut beim Nutzer aus. |
| 2026-09-13 | **ARCH-021 — Menu-Subsystem-Architekturmigration (P-1–P-4)** — P-1 read-only Audit (Entscheidung: `handlers/menu/` bleibt, kein Top-Level `menu/`, Zielstruktur + Phasenplan P-2…P-9). P-2 `handlers/menu/models.py` neu (`MenuState`/`AccessLevel`/`MenuItem`/`MenuSession`, Move ohne Verhaltensänderung). P-3 `handlers/menu/permissions.py` neu (`is_admin_or_owner()`/`get_user_access_level()` vereinheitlichen `RichMenuHandler._is_admin()`/`RichMenuSystem._is_admin_check()`; einzige dokumentierte Verhaltensänderung: `AttributeError`-Randfall bei Config ohne `OWNER_USER_ID` fällt jetzt wie beim bisherigen `_is_admin_check()` defensiv auf `False` zurück, in Produktion unerreichbar; TGPERM-001-Schutz `_ADMIN_ONLY_PREFIXES`/`TestPrivilegedMenuItemsAreGatedTGPERM001` unverändert verifiziert). P-4 `handlers/menu/session.py` neu (`SessionManager`; `RichMenuSystem.sessions`-Property mit verifizierter Dict-Identität zum `SessionManager`-Live-Dict, bestehende direkte Zugriffe/`_handle_close()`/Tests unverändert lauffähig). Alle drei neuen Module ausschließlich von `handlers.menu.models` abhängig. 4 Legacy-Findings charakterisiert, bewusst nicht behoben (`max_sessions` ohne Durchsetzung, `MenuSession.state`/`.data`/`.message_id` ungenutzt/write-only) — neu in `FINDINGS_INDEX.md`. Vollständiges Protokoll: `docs/MusicBot_ARCH-021_Menu_Architecture_Migration.md`. | #203 (gemergt) | Gezielt 65 passed, thematische Menu-Suite 390 passed, `compileall` fehlerfrei. Vollsuite (Nutzer): **3312 passed / 1 skipped / 11 subtests passed / 0 failed** (202,84 s) |
| 2026-09-13 | **ARCH-023 — Menu-Router- und Permission-Architektur-Härtung (P-1–P-7)** — Fortsetzung des ursprünglich in ARCH-021/P-1 als „P-5 Router-Härtung" skizzierten Schritts unter neuer Nummer (eigene Zählung ab P-1, `ARCH-021`/`ARCH-022` bereits anderweitig vergeben). P-1/P-2 read-only Audit + Characterization (34 neue Tests, `tests/test_menu_router_characterization.py`): Kernbefund `MenuItem.access_level` wurde im generischen `menu:`-Fallback nie vor dem Handler-Aufruf geprüft. P-3 Menu-Fallback-Gate in `RichMenuSystem.handle_callback()` (`menu_item.is_accessible(user_level)` vor jedem `menu:`-Handler-Aufruf) schließt die TGPERM-001-Fehlerklasse strukturell für alle `menu:`-Items; `doctor:`/`review:`/`repair:` auf `self._is_admin_check()` konsolidiert; `reprocess:` bewusst unverändert (OWNER-only); dokumentierte UX-Änderung (`show_alert=True` bei Ablehnung für `admin_users`/`admin_logs`/`admin_navidrome`). P-4: `admin_navidrome`-`is_action`-Modellierungsinkonsistenz behoben (war `False` trotz gesetztem `handler=`), neue verhaltensbasierte TGPERM-001-Testklasse `TestPrivilegedMenuItemsActuallyDenyNonAdminTGPERM001` ergänzt (bestehende `TestPrivilegedMenuItemsAreGatedTGPERM001` bleibt unverändert), `handlers/menu/maintenance_gate.py` auf `permissions.is_admin_or_owner()` umgestellt. P-5 `erradmin:`: `ErrorHandlerAdminInterface.is_admin()` prüfte nur `ADMIN_USER_IDS`, kein `OWNER_USER_ID`-Sonderfall (Initial-Commit-Code) — jetzt konsistent über `permissions.is_admin_or_owner()`, `__init__()` erhält Pflichtparameter `config`. P-6: vollständiger repoweiter Permission-Audit inkl. `utils/` (16 Dateien) — kein P0/P1-Fund, `utils/bot_restart_trigger.py`/`utils/navidrome_scan_trigger.py` als reine technische Utilities ohne eigene Autorisierungsverantwortung bestätigt. P-7: die letzten 5 eigenständig implementierten `OWNER_USER_ID or ADMIN_USER_IDS`-Duplikate (`library_doctor_handler.py`, `library_health_review_handler.py`, `repair_musicbot_handler.py`, `admin/bot_restart_handler.py`, `test_menu_handler.py`) auf `permissions.is_admin_or_owner()` konsolidiert, Defense-in-Depth-Struktur vollständig erhalten (je Datei vorab Characterization-Test, dann Umstellung, dann Regression). Vollständiges Protokoll: `docs/MusicBot_ARCH-023_Menu_Router_Permission_Hardening.md`. Folgeblock `docs/MusicBot_ARCH-024_Menu_File_Decomposition.md` (Actions-/Definitions-/Rendering-Extraktion) — COMPLETE, siehe Zeile unten. | #204 (gemergt) | Kumulativ nach P-7: 348 passed, 0 Regressionen. `compileall` fehlerfrei. Vollsuite (Nutzer): **3361 passed / 1 skipped / 11 subtests passed / 0 failed** (215,46 s) |
| 2026-09-13 | **ARCH-024 — Menu-Datei-Dekomposition (Actions/Definitions/Rendering), P-1–P-4 COMPLETE, P-5 NOT WARRANTED** — der in `ARCH-021/P-1` als „P-6 Actions … P-9 Onboarding" skizzierte, durch `ARCH-023`s Nummernbelegung verdrängte Teil der Menü-Migration, unter eigener Zählung fortgeführt. P-1 read-only Audit: Kernbefund — die drei bestehenden UI-Admin-Gruppen (`admin_group_library`/`_operations`/`_diagnostics`) sind die belastbarere fachliche Gliederung als die ursprüngliche `ARCH-021/P-1`-Hypothese; nur `handle_callback()` + 5 Kernmethoden als ROUTING klassifiziert, alle `_handle_<domäne>_callback`-Sub-Dispatcher als ACTION. P-2 Actions Extraction: 9 kohäsive Module unter `handlers/menu/actions/` (family/duplicates/navidrome/stats/admin_diagnostics/usermgmt/library/admin_operations/download) als zustandslose Funktionen mit explizit übergebenen Abhängigkeiten (kein Actions-Klassen-State); 3 Testinfrastruktur-Kopplungen an alte Modulpfade (Patch-Ziele, Bare-Object-Konstruktionsreihenfolge) gefunden und korrigiert, keine Produktionsverhaltensänderung. P-3 Definitions Extraction: `handlers/menu/definitions.py` neu (`build_menu_tree()`/`populate_registry()` aus `initialize_menu_structure()`/`_build_registry()`) — einzige bewusste, dokumentierte Ausnahme von der Rückreferenz-Regel, da MenuItem-Handler-Bindungen weiterhin auf die dünnen `RichMenuSystem`-Delegatoren zeigen. P-4 Rendering Extraction: `handlers/menu/rendering.py` neu (`render_menu()`/`get_menu_text()`/`show_menu()`), vollständig rückreferenzfrei. P-5 Onboarding-Extraktion nach 5 Kriterien (fachliche Grenze/Eigenständigkeit/Komplexität/Testbarkeit/künstliche Abstraktion) geprüft — 3 von 5 negativ/schwach → **NOT WARRANTED**, bleibt bewusst in `RichMenuHandler`. `rich_menu_system.py` 3117 → 1095 Zeilen (-65 %), `rich_menu_handler.py` 1608 → 1411 Zeilen (-12 %). Dependency-Graph zyklenfrei (AST-verifiziert), Router-/Permission-/Session-Kern (`handle_callback()`, `_ADMIN_ONLY_PREFIXES`, Menu-Fallback-Gate, `permissions.py`, `session.py`) byte-identisch unverändert. Finaler Closure-Audit (read-only, kein Refactoring) bestätigte alle Kategorien PASS bzw. PASS WITH NOTES (2 rein kosmetische Funde: stale Kommentar-Referenz in `rich_menu_system.py`, 6 bereits vor ARCH-024 tote Importe — beide nicht behoben, außerhalb Scope). Vollständiges Protokoll: `docs/MusicBot_ARCH-024_Menu_File_Decomposition.md`. | #205 | Thematisch: 535 passed (Menu-Sweep) + 1428 passed (breiter Nachbarthemen-Sweep), 0 Regressionen. `compileall` fehlerfrei. Vollsuite (Nutzer): **3461 passed / 1 skipped / 11 subtests passed / 0 failed** (246,20 s) — +100 gegenüber PR #204, exakt deckungsgleich mit den 100 neuen ARCH-024-Tests |
| 2026-09-13 | **ARCH-025 — Command/Help-Content-Dekomposition + ARCH-024-Debt-Fixes** — Teil A: Research→Characterization (vollständige Dependency Map aller Command-/Help-Methoden inkl. Test-/Patch-Ziel-Analyse) ergab, dass kein Bestandstest die interne Struktur von `handle_start_command()`/`handle_help()`/`handle_help_callback()` inspiziert (nur Gate/Tracking/finaler Nachrichtentext) - Extraktion nach `handlers/menu/content/{user_context,greeting,help}.py` ohne Testanpassung möglich. `RichMenuHandler` behält dünne Delegatoren + die 5 Rollen-/Feature-Methoden (Bestandstests rufen sie direkt auf) + `handle_menu_command()` (bewusst nicht extrahiert, bereits dünner Adapter). 4-Datei-Struktur aus dem Master-Prompt (`commands/`+`content/` getrennt) geprüft und zugunsten von 3 Dateien ohne `commands/`-Paket verworfen (Begründung im ARCH-025-Dokument, Abschnitt A.2). Teil B: die 3 ARCH-024-Remaining-Technical-Debt-Punkte nach erneuter Verifikation (Runtime-Bindungs-Nachvollzug, Testtreffer-Suche, Patch-Ziel-Suche) tatsächlich behoben - stale Kommentar-Referenz korrigiert, 6 tote Importe entfernt, 5 tote Stats-Methoden (`rich_menu_system.py`) + 5 tote `_system`-Funktionen (`actions/stats.py`) entfernt, `definitions.py` setzt für die 5 betroffenen MenuItems kein `handler=` mehr. `rich_menu_handler.py` 1411 → 1109 Zeilen (-21 %). Vollständiges Protokoll: `docs/MusicBot_ARCH-025_Command_Help_Content_Decomposition.md`. | #206 | Keine lokale Testausführung (Master-Prompt-Vorgabe, harte Regel). Vollständige Characterization vor jeder Änderung statt Testlauf. 2 Testdateien wegen Debt-Fix 3 angepasst (netto -1 Test: `test_menu_actions_stats.py` -3/+2, `test_menu_definitions.py` +1). Vollsuite steht beim Nutzer aus. |

---

## 4. Technical Debt — Snapshot

> Platzhalter — wird beim v10-Freeze als Schnappschuss befüllt (Vergleichswert
> zum v9-Stand, CLAUDE.md §30). **Bis dahin maßgeblich:**
> [`docs/FINDINGS_INDEX.md`](FINDINGS_INDEX.md).
>
> Seit v9 neu zurückgestellt: **F-07** (MusicBrainz-Artist-MBID nicht als
> Identitätssignal, P3) — in `FINDINGS_INDEX.md`. **F-08** (deprecated
> Artist-Code) und **INV-01** (`duplicate/cache.py`) sind seit 2026-09-08
> CLOSED (F-08 Cleanup PR #177, INV-01 akzeptiertes Risiko). Kein offener
> P0/P1.

---

## 5. Security-Baseline

> Platzhalter — wird beim v10-Freeze vollständig befüllt.
>
> **Seit v9 eine sicherheitsrelevante Änderung:** TGPERM-001 (2026-09-12,
> siehe Abschnitt 3 oben) — Telegram-Permission-Bypass bei 10 Admin-Level-
> Menüpunkten (Logger-Verwaltung + Test-System), CLOSED noch am Tag der
> Entdeckung. Details: `docs/audits/FULL_PROJECT_ARCHITECTURE_AUDIT_2026-09-12.md`,
> `docs/FINDINGS_INDEX.md`.

---

## 6. Architecture Freeze

> Platzhalter — Freeze-Entscheidung (GO/NO-GO mit Evidenz) ist ein
> ausdrücklicher, eigenständiger Prüfschritt (CLAUDE.md §30) und noch nicht
> erfolgt.

---

## Freeze-Checkliste (beim v10-Freeze abzuarbeiten)

- [ ] Freeze-Gate-Audit → 🟢 APPROVED (alle Kriterien PASS, kein offener P0/P1)
- [ ] Abschnitte 4–6 als Schnappschuss befüllen
- [ ] „Baseline Frozen (JJJJ-MM-TT)"-Footer setzen, DRAFT-Kopf entfernen
- [ ] Referenzen umstellen: `README.md`, `docs/INDEX.md`, `CLAUDE.md` §30
- [ ] `docs/MusicBot_ENGINEERING_BASELINE_v9.md` → `docs/archive/`
