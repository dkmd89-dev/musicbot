# Findings-Index

Lebendes, fortlaufend gepflegtes Register aller aktuell **offenen/
zurückgestellten** Findings — die einzige Stelle, die man befragen muss,
um zu wissen „ist Finding X offen oder geschlossen, und wo steht die
Begründung", ohne durch mehrere Baseline-Dokumente blättern zu müssen.

**Geltungsbereich:** nur offene/zurückgestellte Punkte (bewusste
Design-Entscheidung, siehe Diskussion 2026-09-02 — kein rückwirkendes
Backfill der kompletten Projekthistorie). Ein Fund kommt hierher, sobald
er entdeckt und zurückgestellt wird; er wird hier auf `CLOSED`
umgestellt (nicht gelöscht) und mit Schließungs-Referenz versehen, sobald
er behoben wird — für die volle historische Begründung bleibt die
verlinkte Quelle (Baseline-Abschnitt, Audit-Dokument, PR) maßgeblich,
dieser Index selbst bleibt bewusst kurz.

**Pflegeregel (Definition of Done, CLAUDE.md Abschnitt 22):** jede
Änderung, die einen hier gelisteten Punkt schließt, einen neuen offenen
Punkt erzeugt, oder eine bestehende Priorität/Einschätzung ändert,
aktualisiert die entsprechende Zeile in diesem Dokument im selben PR.
Die Tech-Debt-Tabelle in jeder `MusicBot_ENGINEERING_BASELINE_vN.md`
bleibt davon unberührt ein eingefrorener Schnappschuss zum jeweiligen
Freeze-Zeitpunkt (wird nach dem Freeze nicht mehr editiert) — dieser
Index ist ab sofort die einzige Stelle für den *aktuellen* Stand.

**Baseline-Freeze:** 2026-09-02 (Baseline v8 — eingefrorener Schnappschuss,
Tech-Debt-Tabelle dort unverändert, siehe z. B. `docs/archive/MusicBot_ENGINEERING_BASELINE_v8.md`). Aktuell eingefrorener Referenzpunkt: `docs/MusicBot_ENGINEERING_BASELINE_v9.md` (Freeze 2026-09-07); laufender Zwischenstand seit dem v9-Freeze: `docs/MusicBot_ENGINEERING_BASELINE_v10.md` (DRAFT — nur ARCH-Historie/Testzahlen, keine Findings; die stehen hier).
**Aktueller Finding-Stand (dieses Dokument):** 2026-09-13 (zuletzt aktualisiert:
ARCH-027 — Error Handler Consolidation — F1/F2/F3 aus dem ARCH-026-Audit
CLOSED: `RichMenuHandler` erhält die von `bot.py` erzeugte, PTB-registrierte
`EnhancedErrorHandler`-Instanz jetzt per Constructor Injection statt eine
eigene zweite zu erzeugen; Admin-Statistiken/-Reports/-Reset (`/error_stats`
u. a., `erradmin:*`) zeigen dadurch den tatsächlichen Fehlerzustand. F4–F12
aus ARCH-026 unverändert offen/außerhalb Scope. Siehe
`docs/MusicBot_ARCH-027_Error_Handler_Consolidation.md`. Davor:
ARCH-026 — Enhanced Error Handler Integration & Coverage Audit — 10 neue
OPEN-Findings F1–F8/F11/F12 aufgenommen, Kernbefund: zwei unabhängige
`EnhancedErrorHandler`-Instanzen (bot.py vs. `RichMenuHandler`, nie
synchronisiert, F1) mit Monitoring-Blindspot (F2) und unbegrenzt
wachsendem `recovery_attempts` (F3) bei der praktisch relevanteren
Instanz; die P0-Domäne Download hat gar keine Integration (F4). Reines
Audit, keine Code-Änderung in dieser Phase. Siehe
`docs/MusicBot_ARCH-026_Error_Handler_Integration_Audit.md`. Davor:
ARCH-021-Menu-Subsystem (Models/Permissions/Session-Extraktion, P-2/P-3/P-4,
PR #203) — 4 bewusst zurückgestellte Legacy-Findings aus der Charakterisierung
neu als OPEN (DEFER) aufgenommen (`max_sessions` ohne Durchsetzung,
`MenuSession.state`/`.data`/`.message_id` ungenutzt/write-only), siehe
`docs/MusicBot_ARCH-021_Menu_Architecture_Migration.md`. Davor:
TGPERM-001 CLOSED — Full-Project-Architecture-Audit vom 2026-09-12
(`docs/audits/FULL_PROJECT_ARCHITECTURE_AUDIT_2026-09-12.md`) fand einen
Telegram-Permission-Bypass: 7 Logger-Menüpunkte und die 3 Test-System-
Menüpunkte trugen `access_level=AccessLevel.ADMIN`, liefen aber über den
ungegateten generischen `menu:`-Fallback in `RichMenuSystem.handle_callback()`,
weil ihr automatisch generiertes `callback_data` keinen der gegateten
Präfixe traf — jeder Bot-Nutzer konnte dadurch u. a. das globale Log-Level
ändern oder einen bis zu 900s-Testlauf auslösen. Noch am selben Tag
behoben: die 7 Logger-Items routen jetzt über explizites `callback_data`
in den bereits vorhandenen, gegateten `logger_`-Präfixpfad; die drei
Test-System-Items erhielten einen eigenen `_is_admin()`-Check in
`TestMenuHandler._execute_test_run()`. Neuer Registry-weiter Sweep-Test
schützt gegen dieselbe Fehlerklasse bei zukünftigen Admin-Menüpunkten.
Details siehe Tabelle unten. Davor:
Family Hub (Phase F1–F5) umgesetzt — private Familienstruktur (explizit
konfiguriert, keine erfundenen IDs), Familien-Statistik (wiederverwendet die
bestehende Play-History-Infrastruktur unverändert), bot-interner Familien-Chat,
tägliche Familien-Musik-Challenge mit Scheduler (nach dem bestehenden
`PlayHistoryPoller`-Muster). Von den 5 im Master-Prompt skizzierten
Challenge-Typen wurden nur 3 zuverlässig aus Play-History-Daten umsetzbare
implementiert — „Rate den Song" (bräuchte Audio-/Lyrics-Snippets) und
„Playlist für Stimmung erstellen" (keine automatisch prüfbare Antwort) sind
neue OPEN (DEFER)-Zeilen unten. Vollständige Doku:
`docs/MusicBot_TELEGRAM_MENU_SYSTEM.md` Abschnitt 6. Davor:
Telegram-UI für ⚪ akzeptierte Findings + Unaccept CLOSED — direkt im Anschluss
an die Library Closure Phase (nach CLI-Test des Nutzers gegen die echte
Registry): `handlers/library_health_review_handler.py` +
`rich_menu_system.py`-Dispatcher um `review:accepted`/`acccode`/`accshow`/
`unaccept` erweitert; „⚪ Akzeptierte Findings"-Einstieg in der Übersicht →
Liste nach Code → Detail → „↩️ Reaktivieren". +19 Tests. Davor:
Library Closure Phase CLOSED — 7 Lücken aus der Read-only-Analyse in 7 PRs
geschlossen: ID-basierte Review-CLI (`--accept`/`--unaccept`/`--accepted`/
`--show`/`--summary`, additiv), `unaccept_finding`/`get_accepted_findings`/
`get_review_summary`/`ReviewSummary`-Core-API in `findings.py`, Report-Tri-State
🔴 Offen / 🟢 Repariert / ⚪ Akzeptiert, Per-Repair-Verification je Datei
(`_verify_issue_resolved()` → `UNRESOLVED` statt `SUCCESS`, wenn der Befund nach
der Reparatur bleibt), Coverage-Matrix (`disposition_for_code()` +
`test_library_repair_disposition_matrix.py`). „Akzeptiert" == bestehendes
`FALSE_POSITIVE` (Nutzerentscheidung, kein neuer Status). 674 thematische Tests
grün. Siehe `docs/audits/LIBRARY_CLOSURE_AUDIT_2026-09-09.md` +
`docs/audits/LIBRARY_CLOSURE_COVERAGE_MATRIX_2026-09-09.md`. Davor:
Finding I aus dem Test-Bot-Re-Download 2026-09-09 CLOSED — Live-/TV-Session-Marker
mit „ @ " klebte am Songtitel („Remedy @ Deluxe Music Session") → Titel-/Album-Tag
+ Dateiname; `_clean_title_suffixes()` schneidet ein Titelende-`" @ <Rest>"` jetzt
ab (PR #198). Davor:
Finding E Folge-Fund CLOSED — Aufnahme nur auf Compilations/DJ-Mixen: deren Datum
war trotzdem das Ergebnis-Jahr (Fritz Kalkbrenner „Sky and Sand" → 2018 statt
2009/2010); jetzt kein `release_date` → Fallback auf YouTube-/Upload-Jahr (PR #197).
Davor:
Finding E aus der Download-Pipeline-Log-Analyse 2026-09-09 CLOSED — MusicBrainz-
`release-list` unsortiert, `release_list[0]` war oft eine Compilation/DJ-Mix →
falsches Album + Jahr (Akon „Smack That" → 2009 statt 2006). Neuer
`_select_primary_release()`: Compilations zurückstellen, dann früheste
Veröffentlichung (PR #196). Davor:
Finding G aus der Download-Pipeline-Log-Analyse 2026-09-09 CLOSED — MusicBrainz-
`TimeoutError` lief in den generischen `except Exception`-Zweig („💥 Unerwarteter
Fehler" + Stacktrace auf ERROR); jetzt eigener `except asyncio.TimeoutError` auf
WARNING ohne exc_info (Last.fm-Fallback ist der Normalfall) (PR #195). Davor:
Fritz & Paul Kalkbrenner aus dem Download-Pipeline-Testlauf 2026-09-09 CLOSED —
gemeinsamer Nachname: der Parser-Split „Fritz & Paul Kalkbrenner" → primär „Fritz"
(andere, reale Band) → falsches Genre/falscher Ordner. Gezielter Kollab-Override
+ EMP-Schritt-6a-Interception auf dem ungesplitteten Roh-String (PR #194). Davor:
Download-Pipeline-Optimierung K1/H1/H2/H3/M1 (PR #188–#192) — Schritt 15b vom
Loudness-Re-Encode auf verlustfreien ReplayGain-Tag (schließt das DEFER-Finding
„Schritt 15b LUFS-Ziel verifizieren"), yt-dlp-/MB-Detail-Doppelabrufe gecacht,
Cover-Quellen parallel, Resolver-Refresh bedingt. Details siehe
`docs/MusicBot_ENGINEERING_BASELINE_v10.md` §3. Davor:
Finding H aus dem Download-Pipeline-Testlauf 2026-09-09 CLOSED — Last.fm-User-Tags
(„Laut Mitsing"/„Radio") wurden zu Genres, `prioritize_genres()` verlangt jetzt
ein erkennbares Genre (PR #187). Davor:
Finding B aus dem Download-Pipeline-Testlauf 2026-09-09 CLOSED — Fuzzy-Channel-
Match-Schwelle 75→90, verhindert falsche `channel_fuzzy`-Genres (PR #185). Davor:
Findings F + C aus dem Download-Pipeline-Testlauf 2026-09-09 CLOSED — deutscher
Video-Marker klebte am Feature-Namen (PR #182), 8-Zeichen-All-Caps-Kanalname
„MARTERIA" wurde als Akronym gelernt (PR #183). Davor:
Finding A aus dem Download-Pipeline-Testlauf 2026-09-09 CLOSED — Feature-Artists
aus einem `feat.`/`ft.` im Titel gingen komplett verloren, Fix PR #180. Davor:
Doku-Nachzug PR #178 — vier in PR #177 als „chore" mitgelaufene Änderungen
nachdokumentiert (deutsche „(MIT Artist)"-Feature-Credits, `FILENAME_TITLE_MISMATCH`-
Normalisierung, L2 `requested_issue`-Durchreichung inkl. eines dort gefundenen
Execute-Pfad-Bugs, manuelle Mapping-Ergänzungen). Davor:
F-08 (toter Artist-/AutoLearn-Code) CLOSED — Cleanup durchgeführt, siehe
`docs/audits/ARTIST_IDENTITY_RESOLUTION_MIGRATION_2026-09-08.md`. Davor:
INV-01 als akzeptiertes Risiko CLOSED, analog zum DUP-05-Präzedenzfall,
siehe `docs/audits/SERVICES_ARCHITECTURE_AUDIT_2026-09-01.md` §22
(Entscheidung 2026-09-08). Davor: Artist-Identity-Resolution-Migration
Phase A–F — F-01…F-06 CLOSED, F-07 DEFERRED, siehe
`docs/audits/ARTIST_IDENTITY_RESOLUTION_MIGRATION_2026-09-08.md`. Davor:
Library Repair Production Audit P1–P3 CLOSED, siehe
`docs/audits/LIBRARY_REPAIR_P1_P2_P3_CLOSURE_AUDIT_2026-09-08.md`).

---

| ID | Status | Prio | Kurzfassung | Quelle |
|---|---|---|---|---|
| TGPERM-001 (Telegram Permission-Bypass, Logger-/Test-System-Menü) | CLOSED (2026-09-12) | war P1 | Full-Project-Architecture-Audit vom 2026-09-12 fand: 7 Logger-MenuItems (`logger_main_menu` u. a., `handlers/menu/rich_menu_system.py`) sowie `test_unit`/`test_integration`/`test_performance` (`handlers/test_menu_handler.py`) tragen `access_level=AccessLevel.ADMIN`, aber ihr automatisch generiertes `callback_data` (`"menu:<id>"`) erreichte NICHT `_ADMIN_ONLY_PREFIXES` und wurde über den generischen `menu:`-Fallback in `RichMenuSystem.handle_callback()` ohne jede Berechtigungsprüfung an den jeweiligen Handler durchgereicht — jeder Bot-Nutzer konnte globales Log-Level ändern (hebelt den SEC-001-Schutz aus), Log-Dateien einsehen oder einen bis zu 900s-Testlauf auslösen. Der ursprüngliche SEC-003-Regressionstest (`tests/test_rich_menu_access_control.py`) testete dabei den literalen ID-String statt des real gesendeten `callback_data` und schlug deshalb nie an. Fix: die 7 Logger-Items erhalten jetzt explizites `callback_data="logger_<id>"` und routen damit über den bereits vorhandenen, gegateten `logger_`-Präfixpfad (`_handle_logger_callback()`s `routing_map` kannte dieselben IDs bereits); die dadurch obsoleten `_handle_logger_*`-Wrapper-Methoden wurden entfernt. Für die drei Test-System-Items (kein existierender gegateter Präfix) neuer `TestMenuHandler._is_admin()`-Check in `_execute_test_run()` (Defense-in-Depth-Muster wie `admin_users`/`admin_logs`/`admin_navidrome`). Neuer Registry-weiter Sweep-Test `TestPrivilegedMenuItemsAreGatedTGPERM001` verhindert dieselbe Fehlerklasse für zukünftige Admin-/Owner-Menüpunkte, statt nur die konkret gefundenen Items abzudecken. Pre-Fix-Diskriminierung via `git stash` bestätigt (3 neue Tests schlagen gegen den ungefixten Code fehl); thematische Suite (Rich-Menu/Logger/Test-Menu/User-Management/Navidrome/Family) 513/0 grün. | `docs/audits/FULL_PROJECT_ARCHITECTURE_AUDIT_2026-09-12.md` Abschnitt 5/6 |
| — (Family Hub, Challenge-Typ „Rate den Song") | OPEN (DEFER) | P3 | Master-Prompt nennt „Rate den Song!" als Beispiel-Challenge; nicht umgesetzt — bräuchte eine Audio-/Lyrics-Snippet-Auslieferung über Telegram, kein bestehender Baustein dafür vorhanden. `FamilyChallengeService` unterstützt aktuell nur 3 Play-History-basierte Typen. | `docs/MusicBot_TELEGRAM_MENU_SYSTEM.md` Abschnitt 6.12 |
| — (Family Hub, Challenge-Typ „Playlist für Stimmung erstellen") | OPEN (DEFER) | P3 | Master-Prompt-Beispiel „Erstelle eine Playlist mit 5 Songs für [Stimmung]" nicht umgesetzt — keine automatisch prüfbare korrekte Antwort, würde manuelle Bewertung erfordern. | `docs/MusicBot_TELEGRAM_MENU_SYSTEM.md` Abschnitt 6.12 |
| F-01 (Artist Identity Resolution) | CLOSED (2026-09-08, Migration Phase B/D) | war P1 | Keine dedizierte Identity Resolution nach der Kandidatenwahl — `normalize()` war String-Normalizer + opportunistischer Override-Lookup ohne Signal. Behoben: neue Komponente `services/metadata/artist_identity_resolver.py` (`ArtistIdentity{canonical, source, known}`), in `EnhancedMetadataProcessor` Schritt 6b nach `determine_best_artist()` verdrahtet; `normalize()` seit Phase D reine String-Normalisierung. Priorität `artist_override > known_artist > auto_learned_alias > library_identity > musicbrainz_mbid > parser`. | `docs/audits/ARTIST_IDENTITY_RESOLUTION_MIGRATION_2026-09-08.md` |
| F-02 (`artist_source`-Überschreibung) | CLOSED (2026-09-08, Migration Phase B) | war P1 | `EnhancedMetadataProcessor` überschrieb `artist_source` bedingungslos mit `"first_artist_from_title"` → Mapping-Wirkung nicht nachweisbar. Überschreibung ersatzlos entfernt; `artist_source`/`artist_known` stammen jetzt aus dem Resolver, `MetadataResult.artist_known` neu (+ Cache-Roundtrip). | `docs/audits/ARTIST_IDENTITY_RESOLUTION_MIGRATION_2026-09-08.md` |
| F-03 (`known_artists.yaml` nicht im Entscheidungspfad) | CLOSED (2026-09-08, Migration Phase B) | war P2 | Datei wurde nur von `AutoLearnManager._is_artist_known()` als Post-Decision-Dedup gelesen, nie in der Namensbestimmung. Jetzt Resolver-Tier 2 (`source="known_artist"`, `known=True`). | `docs/audits/ARTIST_IDENTITY_RESOLUTION_MIGRATION_2026-09-08.md` |
| F-04 (AutoLearn kompensiert fehlende Resolution) | CLOSED (2026-09-08, Migration Phase B) | war P2 | Uncommitteter EMP-Fallback registrierte den bestimmten Artist selbst in `known_artists.yaml` (Identity Registration im Lern-Subsystem, nach der Entscheidung). Fallback entfernt; Artist-AutoLearn-Gate ist jetzt `not artist_known`. | `docs/audits/ARTIST_IDENTITY_RESOLUTION_MIGRATION_2026-09-08.md` |
| F-05 (Library → `artist_overrides.json`-Persistenz) | CLOSED (2026-09-08, Migration Phase C/E) | war P2 | `ArtistNormalizer` spiegelte Library-Ordner beim Start persistent in die manuelle Fachdatei (Git-Rauschen). Phase C: nur in-memory + refreshbarer `library_index`; Phase E: auch der in-memory-Spiegel und jeder Schreibpfad (`_save_overrides`) entfernt. Bestehende auto-generierte Einträge bewusst unangetastet (Nutzerentscheidung). | `docs/audits/ARTIST_IDENTITY_RESOLUTION_MIGRATION_2026-09-08.md` |
| F-06 (toter Alias-Code in `artist_map.py`) | CLOSED (2026-09-08, Migration Phase E) | war P3 | `_check_overrides` / `_mirror_library_artists_in_overrides` / `_save_overrides` / `learn_from_feedback` entfernt (0 Aufrufer); `ALLOWED_ARTIST_SOURCES` auf `{"youtube_parsed"}` reduziert. Leere `auto_learned_artist_aliases.json` ist jetzt korrekt eine reine Resolver-Lesequelle. Verbleibender deprecateter Rest → F-08. | `docs/audits/ARTIST_IDENTITY_RESOLUTION_MIGRATION_2026-09-08.md` |
| F-07 (MusicBrainz Artist-MBID nicht als Identitätssignal) | OPEN (DEFER, 2026-09-08, Migration Phase F — Entscheidung B) | P3 | MBID entsteht in der Genre-Pipeline (EMP Schritt 9), **nach** `ArtistIdentityResolver.resolve()` (Schritt 6b) — nicht rechtzeitig verfügbar. Vorziehen nur mit unzulässigem Umbau (MB-Suche nutzt `final_artist` als Query, läuft in `genre_processor`). Zusätzlich Zuverlässigkeitsvorbehalt: die wertschöpfende Nutzung (MB-Artist-Korrektur bei abweichendem Credit) ist repoweit bereits als „noch nicht kalibriert" zurückgestellt (`MusicBrainzClient` `WEICHT AB`-Characterization, MB-01). Resolver-Tier-Slot #5 reserviert (`_resolve_mbid()` → `None`). Spätere Integration = separater Architekturentscheid. | `docs/audits/ARTIST_IDENTITY_RESOLUTION_MIGRATION_2026-09-08.md` |
| F-08 (deprecated Artist-Code, Cleanup) | CLOSED (2026-09-08) | war P3 | `utils/artist_map.py::add_auto_learned_alias()` / `::_save_auto_learned_entry()` und `services/metadata/auto_learn.py::_is_non_artist_channel()` hatten nach der Migration keine Produktions-Aufrufer mehr (repoweit erneut verifiziert), besaßen aber weiterhin Tests. Entfernt: beide Funktionen in `artist_map.py` (inkl. `__main__`-Demo-Aufruf) sowie `_is_non_artist_channel()` in `auto_learn.py` (inkl. dadurch ungenutztem `import re`). Ausschließlich dadurch obsolet gewordene Tests entfernt: `tests/test_artist_normalizer.py::TestSaveAutoLearnedEntryAtomicWrite` (2 Tests), `tests/test_auto_learn.py::test_is_non_artist_channel`. Übrige AutoLearn-/Identity-Tests unverändert. Keine Änderung an `artist_overrides.json` / `known_artists.yaml` / `auto_learned_artist_aliases.json` / der Identity-Priorität. Gezielte Tests (49) + thematische Suite `-k "artist or auto_learn"` (532) grün, 0 Regressionen. | `docs/audits/ARTIST_IDENTITY_RESOLUTION_MIGRATION_2026-09-08.md` |
| INV-01 (`duplicate/cache.py`) | CLOSED (2026-09-08, akzeptiertes Risiko) | war P2 | Synchrone Filesystem-Persistenz im Event-Loop-Thread; 3 Lösungsoptionen bewertet (A verboten laut Architecture-Evolution-Gate, B/C bräuchten je ein eigenes Sicherheitsnetz und führen neue Risikoklassen ein). Analog zu DUP-05 (seinerzeit über mehrere Baselines als „akzeptiert" geführt) formal als Restrisiko akzeptiert und geschlossen — keine Code-Änderung, kein bekannter aktiver Datenverlust/Fehlverhalten, nur strukturelles Risiko bei künftigem Wachstum. Wiedereröffnung nur bei geänderter Problemgröße als neuer Befund. | `docs/audits/SERVICES_ARCHITECTURE_AUDIT_2026-09-01.md` §22 (Entscheidung 2026-09-08) |
| — (`download_executor.py::download_single_track()`/`download_utils.py::_process_single_download()`, verwaiste Teildatei bei echter Task-Cancellation) | CLOSED (2026-09-02) | P2 | War in Baseline v8 als „kein akutes Risiko, wird vom 24h-Start-Sweep erfasst" zurückgestellt — diese Begründung ist inzwischen widerlegt (siehe Zeile oben zum Hard-Cancel-`.part`-Fund: der Sweep schließt `.part`/`.ytdl`-Dateien bewusst aus). Gezielte Untersuchung (Nutzer-Auftrag, direkter Folgeschritt zum Hard-Cancel-Fund): `asyncio.CancelledError` (echte Task-Cancellation, z. B. Bot-Shutdown während eines laufenden Downloads — `asyncio.run()` bricht beim Beenden alle noch offenen Tasks ab, real reproduzierbar über exakt dieselben Bot-Neustarts, die in dieser Session mehrfach durchgeführt wurden) erbt seit Python 3.8 von `BaseException`, nicht von `Exception` — wurde von keinem der bestehenden `except`-Blöcke in `download_single_track()`/`_process_single_download()` gefangen (weder `except DownloadCancelledError`/`except DownloadError` noch `except Exception`), lief unbehandelt durch, kein Cleanup. Klar abzugrenzen vom bereits gefixten Nutzer-Hard-Cancel (`DownloadCancelledError`, eigener Mechanismus, siehe Zeile oben) und vom bereits bestehenden `except asyncio.CancelledError`-Zweig in `_process_playlist_download()` (DL-08) — der sichert nur die Teilergebnis-Liste für die Duplicate-Registrierung, hat aber keinen Zugriff auf `raw_downloaded_path` des gerade laufenden Tracks. Fix: neuer `except asyncio.CancelledError:`-Zweig in beiden Funktionen (Playlist-Track und Single-Download), identische Bereinigung wie beim Nutzer-Hard-Cancel (`cleanup_single_download_artifact()`, profitiert vom `tmpfilename`-Fix der Zeile oben), danach unverändertes `raise` — Cancellation wird nicht unterdrückt. Pre-Fix-Diskriminierung via `git stash` bestätigt (2 neue Tests schlagen ungefixt fehl, inkl. Bestätigung, dass `asyncio.CancelledError` als Typ unverändert propagiert). Vollständige Suite: 1914 passed, 1 skipped, 0 Regressionen. **Closure-Audit (2026-09-03, Nutzer-Auftrag):** gezielt geprüft, ob `_process_single_download()` eine vom bereits gefixten `download_single_track()` unabhängige Variante der Lücke besitzt — struktureller Unterschied gefunden: der try-Block dort umschließt zusätzlich zum reinen Download auch die nachfolgende Metadaten-Verarbeitung (`call_process_single_track()`), ein weiterer `await`-Punkt für eine mögliche `CancelledError`. Per `git stash`-Probe verifiziert, dass ein neuer, gezielter Test diese Lücke *erkennen würde* (except-Zweig testweise deaktiviert → Test schlägt korrekt fehl); gegen den echten Code bereits grün — der bestehende `except asyncio.CancelledError`-Zweig deckt beide await-Punkte gleichermaßen ab, `cleanup_single_download_artifact()`s Existenz-/Pfad-Checks verhindern zusätzlich jede Fehlwirkung, falls die Datei zwischenzeitlich bereits in die Library verschoben wurde. `download_artifact_cleanup.py` (beide Strategien) und deren Aufrufer (`bot.py`, exakt vor `start_polling()`, einziger Aufrufer) ebenfalls geprüft — Sicherheitsgrenzen (`_is_within_directory()`, Endungs-Whitelist, `.part`/`.ytdl`-Ausschluss) intakt. Ergebnis: **CLOSED bestätigt, kein weiterer Fix notwendig** — neuer Regressionstest verankert die Absicherung dauerhaft. Thematische Download-Suite: 100 passed, 0 Regressionen (kein Produktionscode geändert). | `tests/test_download_executor_cancel.py::TestAsyncioCancelledErrorDuringDownload`, `tests/test_download_utils_single_download_cleanup.py::TestAsyncioCancelledErrorDuringDownload` (inkl. `test_cancelled_error_after_successful_download_during_metadata_processing_still_cleans_up`) |
| — (`mugge_statistik_handler.py`) | CLOSED (won't-fix, 2026-09-02) | — | Kein `error_handler` integriert. Nutzer-Entscheidung final bestätigt: jeder `except`-Block editiert die separat gesendete Zwischennachricht, nicht die `callback_query`-Nachricht — mechanisches Verdrahten würde die falsche Nachricht treffen. Bestehende lokale `except`-Blöcke sind bereits funktional äquivalent. Dokumentiert direkt im Code (Klassen-Docstring). | `handlers/mugge_statistik_handler.py` (Docstring); Historie: `docs/archive/MusicBot_ENGINEERING_BASELINE_v6.md` §8 |
| — (`YoutubeDownloader.download_audio`) | CLOSED (2026-09-02) | war P3 | `AttributeError` bei `download_result=None` behoben — sauberer Guard liefert jetzt `{"success": False, "error": ...}` statt zu crashen. Pre-Fix-Diskriminierung via `git stash` bestätigt. | `tests/test_youtube_downloader_telegram_decoupling.py::test_empty_result_returns_clean_error_dict` |
| — (Downloader-Fehlertaxonomie) | CLOSED (2026-09-03, kein Fix nötig) | — | Charakterisierung bestätigt: `FormatNotAvailableError`/`PermissionError` bleiben bewusst ungenutzte, aber korrekt verdrahtete Infrastruktur — `_classify_ytdlp_error()` klassifiziert laut eigenem Docstring nur eindeutig belegte yt-dlp-Fälle („kein Raten bei Unsicherheit"), kein Log-Beleg für einen real aufgetretenen, fälschlich als retry-würdig behandelten Format-/Permission-Fehler. Ein echter OS-`PermissionError` beim Library-Move fällt bereits über `enhanced_metadata_processor.py`s generisches `except Exception` zu `MetadataResult(success=False)` → `MetadataError` (bereits non-retryable) — Schutzzweck ist erfüllt, nur die granulare Diagnose fehlt. Neuer Charakterisierungstest: `tests/test_download_error_taxonomy_characterization.py`. | `docs/archive/MusicBot_ENGINEERING_BASELINE_v8.md` §6/8 |
| — (`FileProcessingError`) | CLOSED (2026-09-03, kein Fix nötig) | — | Charakterisierung bestätigt die bereits im Code dokumentierte Begründung (`download_utils.py`-Kommentar zu `_NON_RETRYABLE_ERROR_TYPES`): wird nirgends geworfen, keine Klassifikation ohne belegten Erzeugungspfad — bewusst außerhalb der Non-Retryable-Menge. Kein Fix. Siehe Zeile oben (gemeinsamer Charakterisierungstest). | `docs/archive/MusicBot_ENGINEERING_BASELINE_v8.md` §6/8 |
| — (14 Delegate-Methoden, `EnhancedMetadataProcessor`) | CLOSED (2026-09-02) | war P3 | Alle 14 entfernt — 0 externe Aufrufer UND 0 interne Selbstaufrufe repoweit erneut verifiziert (Beweispflicht vor Löschung, CLAUDE.md Abschnitt 20), 0 Testreferenzen. `_fetch_album_info_from_musicbrainz()` (ebenfalls 0 Aufrufer, aber außerhalb des benannten Blocks) bewusst nicht mitentfernt — separater, nicht angefragter Fund. | `services/metadata/enhanced_metadata_processor.py` |
| MIG-04 | CLOSED (nicht umgesetzt, 2026-09-02) | war P3 | Vertiefte Prüfung statt Verschiebung: `CoverProcessor` orchestriert 5 externe Quellen + eigenes Scoring/Caching — kein „reiner Client" laut CLAUDE.md-Definition, an seinem jetzigen Ort (`services/metadata/`) korrekt einsortiert; Verschiebung nach `services/clients/` wäre Fehlklassifizierung. `DownloadExecutor` würde strukturell passen (ein externes Tool), aber Verschiebung hätte 0 Funktionsnutzen bei ~6 Importpfad-Änderungen — nicht gerechtfertigt. | `docs/FINDINGS_INDEX.md` (diese Zeile); ursprünglich `docs/audits/SERVICES_ARCHITECTURE_AUDIT_2026-09-01.md` |
| MIG-06 | CLOSED (2026-09-02) | war P3 | Automatisierter Layer-Boundary-Test ergänzt (AST-basiert, `services/` darf nie `handlers`/`klassen`/`telegram` importieren) — Grenze war bereits sauber, ist jetzt dauerhaft gegen Regression abgesichert. | `tests/test_services_layer_boundary.py` |
| DUP-05 | CLOSED (2026-09-02) | war P1 (akzeptiert) | Check-then-Register-Race ohne Lock — behoben durch In-Memory-„in Bearbeitung"-Markierung (URL-/Content-Hash, TTL-basiert selbstheilend). Kein reiner UX-Feinschliff: schließt eine reale Race-Bedingung. | `docs/audits/DUP05_IN_FLIGHT_RACE_FIX_2026-09-02.md` |
| — (`DuplicateCache.duplicate_count`) | CLOSED (2026-09-03) | P3 | Vollständig charakterisiert (Nutzer-Auftrag: Stellen ermitteln, Bedeutung feststellen, URL-/Content-Verhalten vergleichen, Tests prüfen, Soll-Semantik festlegen): Feld wird repoweit von keinem Konsumenten gelesen (reine Persistenz-Telemetrie), live an echten Testdaten belegte Asymmetrie bestätigt (Artist „Zartmann": `url_duplicates.json` zwei Einträge mit `duplicate_count` 2/12, `content_duplicates.json` nur 2). Nutzer-Entscheidung: „Konsistent zählen" — beide Pfade zählen jetzt gleich (jeder Treffer, auch reine Checks) und werden zuverlässig persistiert (`check_url_duplicate()`/`check_content_duplicate()` rufen beide `_save_caches()` auf). Vollständige Suite: 2034 passed, 1 skipped, 0 Regressionen. | `tests/test_duplicate_cache_duplicate_count_consistency.py` |
| — (`DuplicateCache._normalize_url_for_cache`) | CLOSED (2026-09-03) | P3 | `/embed/<id>`, `/live/<id>` normalisierten bisher nicht auf dieselbe Video-ID wie `/watch?v=<id>` (analog zum P0-F-Shorts-Fix) — fielen in den generischen netloc+path-Fallback-Zweig, dadurch als andere URL behandelt. Fix analog zum bereits vorhandenen Shorts-Zweig: beide Pfad-Formen teilen sich einen neuen `elif`-Zweig (Video-ID an derselben Position — letzter Pfad-Abschnitt), Query-String (z. B. `?start=30`/`?feature=share`) stört die Erkennung nicht. Pre-Fix-Diskriminierung bestätigt (6 neue Tests schlagen ungefixt fehl). Vollständige Suite: 2040 passed, 1 skipped, 0 Regressionen. | `tests/test_duplicate_handler.py::TestEmbedUrlNormalization`, `::TestLiveUrlNormalization` |
| — (`ArtistConfig`/`ArtistNormalizer`-Verdrahtung, `DuplicateDetector`) | CLOSED (P1, PR #102) | — | War offen seit P0-E — durch P1 vollständig behoben, `DuplicateDetector` nutzt jetzt denselben `ArtistProcessor`-Pfad wie die Metadaten-Pipeline. Zeile bewusst als Beispiel für den CLOSED-Zustand stehen gelassen (siehe Geltungsbereich oben). | `docs/audits/P1_DUPLICATE_DETECTOR_ARTIST_NORMALIZER_WIRING_2026-09-02.md` |
| — (`run_test_bot.py` BOT_TOKEN-Leak) | CLOSED (2026-09-02) | P0 | Security-Sweep (erster systematischer Secrets-in-Logs-Durchgang): `BOT_TOKEN` (URL-Pfad-Auth der Telegram-Bot-API) landete bei Verbindungsfehlern unmaskiert in der Exception-Message. Gefixt (Maskierung + `__main__`-Guard, der die Datei erst sicher testbar machte). Alle anderen geprüften Secrets (`NAVIDROME_PASS` bereits gefixt, `LASTFM_API_KEY`/`_SECRET`/`GENIUS_ACCESS_TOKEN` strukturell nicht betroffen — Header/POST-Body statt URL) unauffällig. | `docs/audits/SECURITY_SECRETS_IN_LOGS_SWEEP_2026-09-02.md` |
| YTPARSE-01 | CLOSED (2026-09-02) | P0 | Live-Fund beim End-to-End-Testdownload über den echten Test-Bot: `_parse_artist_and_title()` in `utils/youtube_parser.py` splittete den generischen Bindestrich-Trenner (`\s*[-–—]\s*`, kein Whitespace zwingend) am erstbesten BAREN Bindestrich — bei „Miksu/Macloud, makko, t-low - Ich will" traf das den Bindestrich in „t-low" selbst statt des echten Artist/Titel-Trenners: Artist-Liste enthielt fälschlich isoliertes „t", Titel begann mit Leak „low - Ich will". Analog zum bereits bestehenden, aber hier fehlenden Schutz in `artist_processor.py::clean_artist_before_normalization()`. Gefixt: Regex verlangt jetzt `\s+` (zwingend Leerzeichen) auf beiden Seiten. Pre-Fix-Diskriminierung via `git stash` bestätigt (Test schlägt ungefixt fehl). | `tests/test_youtube_parser.py::TestParseYoutubeTitleDocumentedExamples::test_t_low_comma_separated_no_x_keyword_is_not_split_on_bare_hyphen` |
| — (`mapping/artist_overrides.json` + `enhanced_metadata_processor.py` Schritt 6a, Kollab „Fritz & Paul Kalkbrenner") | CLOSED (2026-09-09, PR #194) | P2 | Live-Fund End-to-End-Testdownload: „Fritz & Paul Kalkbrenner - Sky and Sand" landete im Ordner „Fritz/" mit Genre „Indie". `_split_multi_artists()` zerlegt „&"/„x"/„/"/„und" überall als Kollab-Trenner (an vielen Titeln korrekt) → `all_artists = ["Fritz", "Paul Kalkbrenner"]`; der abgeschnittene Primär „Fritz" ist eine andere, reale Band (australisch), der Resolver findet keinen Override → `source="parser"`, Last.fm-Tags der Band Fritz („shoegaze"/„dream pop"/„indie rock") → Genre „Indie". Gleiche Fehlerklasse wie Finding B/D. Ein generischer „gemeinsamer-Nachname"-Split-Fix ist unsicher (Macklemore & Ryan Lewis, Jay-Z & Kanye West sind echte Doppel-Primär-Acts) → gezielter Mapping-Override analog Miksu-Präzedenzfall. Fix: 8 Override-Einträge (Kollab-Schreibweisen von „Fritz … Paul Kalkbrenner" + Self-Eintrag „fritz kalkbrenner" → „Fritz Kalkbrenner"; Nutzer-Entscheid: Primär „Fritz Kalkbrenner", Paul als Zweit-Artist) **plus** neuer EMP-Schritt 6a: der UNGESPLITTETE Roh-Artist-String (`raw_artist_string` bzw. die stabile Komma-Form `", ".join(all_artists)`) wird gegen die Override-Stufe des `ArtistIdentityResolver` geprüft; greift ein Override, das eine Erweiterung des abgeschnittenen ersten Künstlers ist, wird `all_artists[0]` ersetzt (Paul bleibt über den all_artists-Rest-Zweig Zweit-Artist). Bare „Fritz" bleibt unberührt. Pre-Fix-Diskriminierung via `git stash` bestätigt (E2E-Test schlägt ungefixt fehl: `result.artist == "Fritz"`). | `tests/test_collab_artist_shared_surname_kalkbrenner.py` |
| — (`mapping/artist_overrides.json`, Duo „Miksu & Macloud") | CLOSED (2026-09-02) | P1 | Selber Live-Testdownload: der Schrägstrich in „Miksu/Macloud" wird überall bewusst als Kollaborations-Trenner behandelt (korrektes Verhalten für andere Titel), zerlegte hier aber den echten Duo-Namen — YT-Parser lieferte `parsed_artist="Miksu"` (nur `all_artists[0]`), das gewann laut Prioritätskette gegen den korrekten, unzerlegten `raw_artist`/`channel_name` „Miksu / Macloud" → finaler Artist „MIKSU" (Macloud komplett verloren). Bereits vorhandene Overrides deckten nur die „&"-Schreibweisen ab. Fix: 4 neue Override-Einträge (bare „miksu", „miksu / macloud", „miksu/macloud", „miksu x macloud" → „Miksu & Macloud"), Nutzer-Entscheidung für Mapping-Override statt Code-Änderung an der generischen Slash-Behandlung. Verifiziert mit echten Produktionsklassen (`ArtistProcessor.determine_best_artist()`), Pre-Fix-Diskriminierung via `git stash` bestätigt. | `tests/test_artist_overrides_miksu_macloud_duo.py` |
| — (`filenamefixer.py::extract_main_artist()`, Library-Ordner „Miksu & Macloud") | CLOSED (2026-09-02) | P1 | Nutzer-Fund nach obigem Mapping-Fix: der Tag zeigte korrekt „Miksu & Macloud", der Library-ORDNER blieb aber „Miksu" — `extract_main_artist()` splittet jeden Artist-String an „,"/„&"/„feat."/„ft."/„ x " fürs Ordner-Grouping (sinnvoll für echte Ad-hoc-Kollaborationen), zerlegte aber den bereits als atomaren Duo-Namen normalisierten Override-Wert zurück in „Miksu". Nebenwirkung: `check_library_duplicate()` (`services/duplicate/detector.py`) fand den vorhandenen Ordner dadurch auch nicht mehr (dessen `ArtistNormalizer` liefert seit dem Override-Fix ebenfalls „Miksu & Macloud"). Fix: Artist-Strings, die exakt einem Zielwert aus `artist_overrides.json` entsprechen, gelten als bereits kanonisch/atomar und werden nicht mehr gesplittet — Ad-hoc-Kollaborationen ohne eigenen Override-Eintrag unverändert. Pre-Fix-Diskriminierung via `git stash` bestätigt. Live end-to-end verifiziert (sauberer Testdownload nach vollständiger Miksu-Bereinigung: Ordner korrekt „Miksu & Macloud", erneuter Download desselben Tracks nach URL-Cache-Leerung korrekt als Duplikat über die Content-Cache-Ebene erkannt, keine erneute Datei-Verschwendung, korrekte Telegram-Meldung). | `tests/test_filenamefixer.py::TestBuildFinalPathKnownGroupNames` |
| TAG-02 (`tag_writer.py::write_tags()`, doppelter Artist im ARTISTS-Tag) | CLOSED (2026-09-02) | P1 | Nutzer-Fund am tatsächlich getaggten Testfile im Anschluss an obige zwei Funde: `ARTISTS`-Tag zeigte „Miksu & Macloud; Miksu & Macloud; MACLOUD; makko" — Duo-Name doppelt. Root Cause: `feat_artists` (roh: `["Miksu", "Macloud", "makko"]`, aus `enhanced_metadata_processor.py`s Fallback-Restartisten-Logik, die rohe geparste Artists gegen den bereits normalisierten `final_artist`-String vergleicht — funktionierte zufällig, solange kein Override einen rohen Fragment-Namen auf einen mehrteiligen kanonischen String abbildete) wird in `write_tags()` PRO EINTRAG normalisiert (Zeile 43-46) — „Miksu" trifft über den neuen Override auf denselben String wie der bereits finale Haupt-Artist „Miksu & Macloud", ohne Dedup danach. Fix: nach der Normalisierung case-insensitiv gegen `artist` und untereinander dedupen, `artist` behält immer den ersten Slot. Pre-Fix-Diskriminierung via `git stash` bestätigt. Verwandter, NICHT gefixter Punkt (separater Scope): die rohe-vs-normalisierte Vergleichslogik in `enhanced_metadata_processor.py` (Zeilen ~488-497) bleibt strukturell fragil für zukünftige ähnliche Override-Kollisionen — der Dedup in `write_tags()` fängt das Symptom robust ab, behebt aber nicht die eigentliche Ursache dort. | `tests/test_tag_writer_multi_value_artists_tag.py::TestFeatArtistDuplicateAfterNormalizationIsDeduped` |
| — (Playlist-Downloads ohne Pro-Track-Duplikatprüfung) | CLOSED (2026-09-02) | P1 | Nutzer-Fund beim Playlist-Testdownload: „Zartmann - schönhauser" existierte bereits als Single (`Zartmann/Singles/...`), wurde aber beim Download der Playlist „schönhauser EP" für denselben Track erneut heruntergeladen und als zweite Kopie unter `Zartmann/2025 - schönhauser EP/03 - schönhauser.m4a` abgelegt — der alte Duplicate-Cache-Eintrag wurde dabei durch den neuen (identischer Content-Hash) stillschweigend überschrieben. Root Cause: `_probe_artist_title_for_duplicate_check()` (`klassen/download_handler.py`) liefert für Playlist-URLs bewusst `(None, None)` zurück (dokumentiert, kein Zufallsbug) — `check_for_duplicates()` prüft für Playlists dadurch nur die URL-Ebene der Playlist selbst, nie Artist/Titel der einzelnen enthaltenen Tracks. Fix: `duplicate_detector` wird jetzt optional durch `YoutubeDownloader` → `enhanced_download_with_retry()` → `_process_playlist_download()` durchgereicht; die Playlist-Schleife ruft vor jedem Track-Download `check_for_duplicates()` mit dem eigenen Artist/Titel dieses Tracks auf und überspringt ihn bei Treffer (`DownloadResult(success=False, is_duplicate=True, ...)`, wird von `_register_playlist_track_duplicates()` korrekt nicht erneut registriert). `duplicate_detector=None` (Default) erhält das bisherige Verhalten für Aufrufer ohne eigenen Detector. Pre-Fix-Diskriminierung via `git stash` bestätigt. | `tests/test_playlist_per_track_duplicate_check.py` |
| — (Metadata-Cache-Hit + Duplicate-Cache leer, verwaiste Rohdatei) | CLOSED (2026-09-02) | P2 | Beim gezielten Testen der Library-Fallback-Ebene entdeckt (nicht der ursprünglich gemeldete Fund): wird `url_duplicates.json`/`content_duplicates.json` für einen Track geleert, dessen Library-Datei aber noch existiert UND dessen `metadata_cache`-Eintrag noch vorhanden ist, lädt die Pipeline die Audiodatei komplett neu von YouTube, der Metadata-Cache-Hit in `process_single_track()` (`enhanced_metadata_processor.py:283-287`) gibt aber sofort das alte `MetadataResult` zurück — `move_to_library()` (Schritt 16) wird dadurch nie aufgerufen. Die frisch heruntergeladene Datei blieb dadurch unverschoben/verwaist im `DOWNLOAD_DIR` liegen. Untersucht (vorher offene Frage): der bestehende 24h-Start-Sweep (`download_artifact_cleanup.py::cleanup_download_artifacts()`) erfasst solche Dateien zwar irgendwann (bekannte Endung, `mtime` > 24h), aber erst beim nächsten Bot-Neustart — bis dahin liegt die Datei ungenutzt in `DOWNLOAD_DIR`. Fix: `process_single_track()` ruft im Cache-Hit-Zweig jetzt sofort `cleanup_single_download_artifact()` (bereits bestehende Strategie-C-Utility, hier wiederverwendet statt dupliziert) für `track_metadata["filepath"]` auf — No-op, falls kein `filepath` vorhanden ist oder es (Normalfall, kein Cache-Hit) bereits außerhalb von `DOWNLOAD_DIR` liegt. Pre-Fix-Diskriminierung via direktem Testlauf bestätigt (Regressionstest schlägt ungefixt fehl: verwaiste zweite Rohdatei bleibt liegen). Bewusst NICHT Teil dieses Fixes: die redundante Netzwerk-Download selbst — das Leeren der Duplicate-Caches ist eine bewusste Nutzeraktion, die genau diese Schutzebene entfernt (kein Bug, sondern Absicht); die „Download erfolgreich"-Meldung mit dem (weiterhin korrekten) alten Library-Pfad bleibt technisch korrekt, nur uninformativ darüber, dass kein neuer Download tatsächlich übernommen wurde — als reine UX-Nuance nicht weiterverfolgt. | `tests/test_metadata_processor_happy_path.py::test_cache_hit_after_redundant_redownload_cleans_up_orphaned_raw_file` |
| — (Playlist-Album unvollständig durch Singles-Vorrang) | CLOSED (2026-09-02) | P1 | Folge-Fund zur Zeile oben (Playlist-Downloads ohne Pro-Track-Duplikatprüfung): der dortige Fix übersprang den doppelten Track nur, das Album blieb dadurch unvollständig — zusätzlich griff bereits VOR diesem Check der Metadata-Cache-Kurzschluss in `CacheManager.lookup_playlist_track()`/`_build_result()`, der einen Treffer als „Erfolg" meldet, ohne die Datei in den Album-Ordner zu verschieben. Live beobachtet: „wie du manchmal fehlst" fehlte im Album „Zartmann - dafür bin ich frei EP" (Track 6/8), obwohl bereits unter `Zartmann/Singles/` vorhanden. Nutzer-Entscheidung: Playlist-/Album-Vollständigkeit hat bewusst Vorrang vor der älteren Einzeldatei. Fix: neue Methode `DuplicateDetector.resolve_playlist_single_conflict()` — findet einen Treffer ausschließlich in einem Ordner, der exakt „Singles" heißt (andere, bereits existierende Alben werden nicht angetastet), löscht die Datei physisch und invalidiert den zugehörigen Duplicate-Cache-Eintrag. Läuft in `_process_playlist_download()` jetzt VOR dem Cache-Check, sodass dieser die Datei danach korrekt als fehlend erkennt und der Track normal (neu) heruntergeladen wird. Pre-Fix-Diskriminierung via `git stash` bestätigt. Live end-to-end verifiziert (Album „Zartmann - 11 bis 2", Track „Ritalin 4K" existierte als Singles-Datei aus einem vorherigen Testdownload — Datei korrekt gelöscht, Track neu heruntergeladen, Album 6/6 vollständig). | `tests/test_duplicate_detector_playlist_singles_priority.py`, `tests/test_playlist_singles_priority_pre_check.py` |
| — (`youtube_parser.py::_clean_bracket_content()`, freistehende Auflösungsangabe im Titel) | CLOSED (2026-09-02) | P1 | Direkter Folge-Fund desselben Live-Testdownloads (Album „Zartmann - 11 bis 2"): 3 von 6 Tracks wurden mit angehängter Auflösungsangabe getaggt (Titel „Mama 4K"/„Ritalin 4K"/„Easy 4k" statt „Mama"/„Ritalin"/„Easy"). Root Cause: im Original-YouTube-Titel (z.B. „Zartmann - Mama (prod. by Drumla) [Official Video] 4K") stand „4K" NICHT in eigenen Klammern wie im bereits behandelten Fall „[4K]" — nach Entfernen von „[Official Video]" blieb „4K" als freistehendes, durch Whitespace abgetrenntes Suffix zurück, das keines der bestehenden Klammer-Patterns erfasste. Fix: zusätzliches Pattern in `_clean_bracket_content()` entfernt einen freistehenden Auflösungs-/Qualitäts-Marker (4k/8k/2k/hd/uhd/1080p/720p/480p/360p etc.) am Ende des (Titel-)Strings, unabhängig davon ob geklammert. Pre-Fix-Diskriminierung via `git stash` bestätigt (4 Tests schlagen ungefixt fehl). | `tests/test_youtube_parser.py::TestCleanBracketContent::test_removes_bare_trailing_resolution_marker`, `tests/test_youtube_parser.py::TestParseYoutubeTitleBareTrailingResolutionMarker` |
| — (`progress_tracker.py::ProgressTracker`, Playlist-Progress-State-Erweiterung) | CLOSED (2026-09-02) | P3 | Nutzer-Vorschlag (kein Bug-Report): `compute_progress_message()` erhöhte `processed_items` bisher bei JEDEM Aufruf blind um 1, unabhängig davon ob ein Element tatsächlich fertig war — zu grob für eine Playlist mit mehreren Status-Updates pro Track. Aktuell in Produktion nirgends live verdrahtet (`init_tracker()`/`self.progress_tracker` in `download_utils.py`/`download_handler.py` werden konstruiert, aber `compute_progress_message()`/`set_current_item()` haben 0 Aufrufer im Repo — bereits in `tests/test_progress_tracker.py` dokumentiert), daher kein akuter Produktionsfehler, aber ein Semantik-Fix vor künftiger Verdrahtung. Erweiterung (Architektur bewusst NICHT neu gebaut): neue `completed_items`-Liste + `mark_completed(item_name)` — `processed_items` wird jetzt ausschließlich daraus abgeleitet (Anzahl echter Abschlüsse), `compute_progress_message()` erhöht nichts mehr selbst. Dabei entdeckter Nebeneffekt gefixt: der 0-Elemente-Zweig der Nachrichtenerzeugung zeigte `current_item` bisher nicht an (durch den alten blinden Zähler faktisch nie mit `processed_items == 0` erreichbar, jetzt ein echter, häufiger Zustand — z.B. erster Track lädt noch). Telegram-spezifische Darstellung (Fortschrittsbalken, Emoji-Sektionen) bleibt bewusst Aufgabe der aufrufenden Schicht, nicht Teil dieser Erweiterung. Pre-Fix-Diskriminierung via `git stash` bestätigt (8 Tests schlagen ungefixt fehl). | `tests/test_progress_tracker.py::TestMarkCompleted`, `tests/test_progress_tracker.py::TestComputeProgressMessage` |
| — (Playlist-Fortschrittsanzeige, `ProgressTracker`-Verdrahtung) | CLOSED (2026-09-02) | P3 | Direkter Folgeschritt zur Zeile oben (Nutzer-Freigabe): der bis dahin ungenutzte `ProgressTracker` wird jetzt tatsächlich live verdrahtet, damit Schritt 3/6 ("Audio-Download") während eines Playlist-Downloads laufend aktualisiert statt eine statische Meldung für die gesamte Download-Dauer zu zeigen. `_process_playlist_download()` (`services/downloader/download_utils.py`) erzeugt pro Playlist einen `ProgressTracker`, ruft `set_current_item()`/`mark_completed()` pro Track (`mark_completed()` in einem neuen `finally:`-Block, damit auch fehlgeschlagene Tracks den Zähler korrekt weiterschalten) und ruft `status_callback` (throttled über `tracker.compute_progress_message()`) mit dem `ProgressTracker`-Objekt selbst auf — reiner Zustand, kein vorformatierter Text, `services/` bleibt dadurch weiterhin frei von Telegram-Importen (durch `tests/test_services_layer_boundary.py` automatisiert abgesichert, bleibt grün). Ersetzt die vorherige `status_callback`-Signatur `(chat_id, step, total, message, module)`, die 0 echte Aufrufer hatte. `status_callback` wird jetzt durch `YoutubeDownloader` (`services/downloader/downloader.py`) durchgereicht und in `klassen/download_handler.py` als neue Methode `_on_playlist_progress()` gebunden — baut dort die eigentliche Telegram-Darstellung (Fortschrittsbalken + "⬇️ Aktuell"/"✅ Abgeschlossen"-Sektionen, letztere auf die letzten 8 Einträge begrenzt) und sendet über das bestehende `status_msg`. Pre-Fix-Diskriminierung via `git stash` bestätigt (13 Tests schlagen ungefixt fehl). | `tests/test_playlist_progress_callback.py`, `tests/test_download_handler_playlist_progress_callback.py`, `tests/test_youtube_downloader_telegram_decoupling.py` |
| — (Dead-Code-/Legacy-Cleanup-Audit, 8 Kandidaten) | CLOSED (2026-09-02) | P3 | Nutzer-Auftrag: vollständiges Read-Only-Audit des `main`-Baums auf verwaiste Dateien, danach explizite Freigabe „lösche 1-8". Entfernt: `handlers/error_handler.py` (415 Zeilen, unveraendert seit Initial-Commit, 0 Referenzen, vollstaendig durch `handlers/enhanced_error_handler.py` ersetzt), `handlers/status_handler.py` (1-Zeilen-Stub, 0 Referenzen, ersetzt durch `handlers/enhanced_status_handler.py`), `handlers/adapters/__init__.py` (leeres Scaffold-Verzeichnis — bereits von zwei frueheren Audits als tot markiert, aber nie entfernt, siehe `docs/archive/POST-SERVICES_PROJECT-WIDE_ARCHITECTURE_AUDIT.md` G.4), `helfer/telegram_escaper.py` (0 Referenzen, 0 Tests, kein `__main__`), `Download_Pipeline.txt` (versehentlich eingechecktes rohes Log-Mitschnitt-Artefakt), `migration_report.json` (einmaliger Migrationsreport vom 2025-09-30 mit Pfadverweis auf eine andere Maschine), `mapping/test_genre_map.py` (falsch platzierte Legacy-Testdatei ausserhalb `tests/`, dadurch vom etablierten `pytest tests/`-Workflow nie erfasst — beim Baseline-Lauf mit bloßem `pytest` **7 von 8 Tests bereits fehlschlagend**, bestätigt echten Bitrot statt nur Fehlplatzierung), `__init__.py` (Root, 0 Bytes, 0 Referenzen). Vor/nach der Löschung mit bloßem `pytest` (sammelt auch außerhalb `tests/` ein) verglichen: exakt die 8 entfernten Testfälle fehlen danach, alle übrigen 1829 unveraendert gruen, 0 Regressionen. Alle anderen Audit-Funde (`scripts/*`, `mapping/*.yaml`, `docs/archive/*`) als aktiv/historisch bestaetigt, nicht angetastet. | `git diff --stat` dieses Commits (reine Loeschungen, keine Code-Aenderung) |
| — (Telegram Download-Control-Center: Cancel + Live-Status) | CLOSED (2026-09-02) | P1 | Nutzer-Vorgabe (neue Phase): „📥 Downloads" wird von einer statischen 2-Optionen-Liste zu einem echten Steuerzentrum mit Live-Status und Abbruch-Funktion. Neu: `services/downloader/active_downloads.py::ActiveDownloadRegistry` (prozessweite, threadsichere Registry, auf `RichMenuHandler` verankert — im Gegensatz zu `DownloadHandler`, das pro Update neu instanziiert wird) + `DownloadCancelledError` (`services/downloader/errors.py`, non-retryable). Hard-Cancel: ein Cancel-Check-Hook wird einmalig in `ydl_opts["progress_hooks"]` injiziert (`enhanced_download_with_retry()`) und dadurch automatisch von jedem nachfolgenden yt-dlp-Aufruf (Single **und** jeder Playlist-Track) mitgezogen — bricht den *gerade laufenden* Track sofort ab, nicht erst den nächsten. Soft-Cancel: `_process_playlist_download()` prüft vor jedem Track zusätzlich `is_cancel_requested()` und startet dann keine weiteren Tracks. `DownloadHandler.handle_youtube_links()` registriert/deregistriert (finally-Block) den aktiven Download und unterscheidet „echter Fehlschlag" von „abgebrochen" (eigene, kurze Meldung bei 0 fertigen Tracks; bei Playlists mit bereits fertigen Tracks läuft die normale Metadaten-/Bibliotheks-/Zusammenfassungs-Pipeline weiter — die Zusammenfassung selbst zeigt „🛑 Download abgebrochen" statt des Erfolgs-Headers). Telegram-Seite (`handlers/menu/rich_menu_system.py`, neuer `dl:`-Callback-Präfix, bewusst nicht admin-gated): „🔄 Aktive Downloads" zeigt Titel/Fortschrittsbalken/aktuellen Track/zuletzt abgeschlossene Tracks/verbleibende Anzahl aus demselben geteilten `ProgressTracker`, den auch die automatischen Zwischen-Updates nutzen; „❌ Abbrechen" erscheint nur, wenn tatsächlich ein Download aktiv ist (keine toten Buttons); „ℹ️ Details" zeigt URL/Typ/Laufzeit/Fortschritt. Bewusst **nicht** Teil dieser Ausbaustufe (Nutzer-Priorisierung): „📋 Download-Verlauf"/„🔁 Erneut versuchen" (brauchen persistenten JSON-Verlaufsspeicher, eigener Folgeschritt — aktuell nur Platzhalter „kommt in einem späteren Schritt"), „🔄 Reprocessing" (eigener Bereich laut Nutzer-Entscheidung), Pause/Resume (Nutzer-Entscheidung: technisch deutlich anspruchsvoller als Cancel bei yt-dlp/Playlist-Orchestrierung, unnötige Komplexität). Pre-Fix-Diskriminierung via `git stash` für jeden Teilschritt bestätigt. Vollständige Suite: 1895 passed, 1 skipped, 0 Regressionen. | `tests/test_active_download_registry.py`, `tests/test_download_executor_cancel.py`, `tests/test_playlist_download_cancellation.py`, `tests/test_download_handler_active_download_lifecycle.py`, `tests/test_rich_menu_download_control_center.py` |
| — (Download-Verlauf/Erneut-versuchen, persistenter Speicher) | CLOSED (2026-09-03) | war P2 | Umgesetzt: neuer `services/downloader/download_history.py::DownloadHistoryStore` (struktureller Zwilling zu `duplicate/cache.py` — atomares Schreiben, `Config.DOWNLOAD_HISTORY_DIR`, gedeckelt auf 20 Einträge/Chat), als geteilte, prozessweite Instanz analog zu `ActiveDownloadRegistry` auf `RichMenuHandler` verankert. Vier Schreib-Hooks in `klassen/download_handler.py` (Single-Erfolg, Playlist-Erfolg pro Track, Fehlschlag, Abbruch) über `_record_history_entry()`, No-op-sicher falls kein Store injiziert (bestehende Tests bypassen `__init__()`). „📋 Download-Verlauf" zeigt jetzt die letzten Einträge des Chats (neueste zuerst, Status-Icons ✅/❌/🛑) mit je einem „🔁 Erneut versuchen"-Button. Retry baut bewusst KEINEN Eingriff in `handle_youtube_links()` (als „große Klasse" geschützt, CLAUDE.md Abschnitt 19), sondern ein schreibgeschütztes Duck-Typing-Adapterobjekt anstelle des eingefrorenen PTB-`Update`/`Message` und reicht es an den bereits produktiv genutzten `_process_url()`-Pfad weiter — keine Parallel-Implementierung der Pipeline. Volle Suite: 2097 passed, 1 skipped, 0 Regressionen. | `tests/test_download_history_store.py`, `tests/test_download_handler_history_recording.py`, `tests/test_rich_menu_download_history.py` |
| — (`rich_menu_handler.py::_process_url()`, Bot blockiert während Downloads) | CLOSED (2026-09-02) | P0 | Live-Fund direkt beim Testen der Zeile oben (Nutzer-Report: „sobald Download läuft öffnet sich das Menü nicht"): die Telegram-`Application` läuft ohne `concurrent_updates=True` (`bot.py`) — PTB holt das nächste Update aus der Warteschlange erst, NACHDEM der Handler für das aktuelle Update komplett zurückgekehrt ist. `_process_url()` awaitete `handler.handle_url()` bisher direkt — das blockierte dadurch die Verarbeitung JEDES weiteren Updates (inkl. Menü-Klicks) für die gesamte Downloaddauer, live bestätigt über einen „Query is too old"-`BadRequest` für einen während eines laufenden Downloads geklickten Button. Damit war „🔄 Aktive Downloads"/„❌ Abbrechen" aus der Zeile oben *praktisch unerreichbar*, solange ein Download lief — trotz korrekter Implementierung dort. Root Cause vorbestehend (nicht durch das Control-Center verursacht), aber erst durch dessen Anwendungsfall (Interaktion WÄHREND eines Downloads) überhaupt bemerkbar. Fix: der Download läuft jetzt als eigenständiger `asyncio.create_task()` in `_process_url()` statt inline awaited zu werden — PTB verarbeitet das nächste Update ohne Wartezeit. Bewusst NICHT die globale `concurrent_updates`-Einstellung geändert (deutlich größerer Blast-Radius). Pre-Fix-Diskriminierung ungewöhnlich eindeutig: der Kern-Regressionstest hängt am ungefixten Code tatsächlich (echter Deadlock, kein Assertion-Fehlschlag) — entspricht exakt dem gemeldeten Symptom. Vollständige Suite: 1899 passed, 1 skipped, 0 Regressionen. | `tests/test_rich_menu_handler.py::TestProcessUrlRunsAsBackgroundTask` |
| — (`rich_menu_handler.py::get_telegram_handlers()`, fehlender `^dl:`-CallbackQueryHandler) | CLOSED (2026-09-02) | P1 | Live-Fund direkt beim erneuten Testen nach der Zeile oben (Nutzer-Report: „Downloads lässt sich öffnen aber die restlichen Buttons sind tot außer Hauptmenü"): `CallbackQueryHandler` werden PTB-seitig über feste `pattern=`-Allowlists geroutet (`^menu:`, `^dup:`, `^backup_`, `^status_`, …) — das interne `dl:`-Präfix-Routing in `RichMenuSystem.handle_callback()` (bereits korrekt implementiert, siehe Zeile „Telegram Download-Control-Center" oben) wurde dadurch nie erreicht, weil in `get_telegram_handlers()` schlicht **kein** Handler für `^dl:` registriert war. PTB fand für `dl:*`-Callback-Daten dadurch keinen passenden Handler und tat gar nichts — weder Log-Eintrag noch Exception, was die Fehlersuche erschwerte (kein Fehlerbild, nur Stille). `menu:download` selbst funktionierte bereits (passt auf das bestehende `^menu:`-Pattern), nur die `dl:*`-Folge-Callbacks liefen ins Leere. Fix: `CallbackQueryHandler(self.menu_system.handle_callback, pattern="^dl:")` ergänzt. Pre-Fix-Diskriminierung via `git stash` bestätigt (3 Tests schlagen ungefixt fehl). Vollständige Suite: 1902 passed, 1 skipped, 0 Regressionen. | `tests/test_rich_menu_handler.py::TestGetTelegramHandlersRegistersDlPrefix` |
| — (`download_result_reporter.py::build_final_summary_message()`, „N/A" nach Mid-Track-Abbruch) | CLOSED (2026-09-02) | P2 | Live-Fund beim ersten erfolgreichen End-to-End-Test des Hard-Cancel (Nutzer bricht Playlist-Download während Track 2 ab, Track 1 bereits fertig): die Abschluss-Zusammenfassung zeigte „Beispiel-Track: N/A" / „Speicherort: N/A" trotz erfolgreich abgeschlossenem Track 1. Root Cause: der Pfad wurde blind aus `tracks[-1]` gelesen — bei einem Abbruch ist der letzte Listeneintrag aber der neue, durch `DownloadCancelledError` erzeugte fehlgeschlagene Track-2-Eintrag ohne `library_path`. Fix: für Playlists wird jetzt `reversed(tracks)` nach dem ersten Eintrag mit einem tatsächlich vorhandenen `library_path` durchsucht statt blind der letzte genommen; Single-Downloads unverändert über `result.get("library_path")`. Zusätzlich Header/Tracks-Zeile der Zusammenfassung angepasst: „🛑 Download abgebrochen" / „N/M abgebrochen bei" statt „🎉 … erfolgreich" bei `cancelled=True`. Live end-to-end verifiziert (korrekte „1/2 abgebrochen bei"-Meldung nach dem Fix). | `tests/test_download_result_reporter.py::TestBuildFinalSummaryMessageCancelled`, `tests/test_download_result_reporter.py::test_cancelled_mid_track_shows_last_successful_track_not_na` |
| — (`rich_menu_system.py`, dl:-Menüs mit `parse_mode="Markdown"` an dynamischen Inhalten) | CLOSED (2026-09-02) | P1 | Live-Fund direkt im Anschluss an den N/A-Fix oben (Nutzer testete erneut live): Klick auf „ℹ️ Details" während eines laufenden Downloads warf zweimal reproduzierbar `BadRequest: Can't parse entities: can't find end of the entity starting at byte offset 120`. Ursache: `active.url` (eine echte YouTube-Playlist-URL mit „_", z. B. „…list=OLAK5uy_nRANgdz-…") wurde roh in eine mit `parse_mode="Markdown"` gesendete Nachricht eingebettet — Telegrams Legacy-Markdown-Parser interpretiert ein einzelnes „_" als unvollständig geschlossene Kursiv-Formatierung. Dieselbe Gefahr bestand systemisch in allen dl:-Bildschirmen mit dynamischen/externen Inhalten (Tracktitel, Artist-/Albumnamen aus `tracker.current_item`/`completed_items`, `active.title`): `_render_download_menu()`, `_handle_download_active()`, `_handle_download_details()`. Fix: `parse_mode="Markdown"` aus der gesamten dl:-Sektion entfernt (alle 5 Methoden, auch die beiden bislang statischen/sicheren, für Konsistenz und um die ganze Risikoklasse zu eliminieren statt einzelne Felder selektiv zu escapen — genau dieses selektive Escapen hatte die URL im vorherigen Stand bereits übersehen). `*bold*`-Syntax aus den Kopfzeilen entfernt, da ohne `parse_mode` sonst wörtliche Sternchen erschienen wären. Pre-Fix-Diskriminierung via `git stash` bestätigt (3/5 neue Tests schlagen ungefixt fehl). Vollständige Suite: 1908 passed, 1 skipped, 0 Regressionen. | `tests/test_rich_menu_download_control_center.py::TestDlMessagesAvoidMarkdownParseErrors` |
| — (`download_executor.py::download_single_track()`/`download_utils.py::_process_single_download()`, `.part`-Datei nach Hard-Cancel bleibt dauerhaft liegen) | CLOSED (2026-09-02) | P2 | Gezielte Untersuchung des verbleibenden Cancellation-P2-Punkts (Nutzer-Auftrag): der `_capture_raw_downloaded_path`-Hook beider Download-Pfade wertete bisher nur `status=="finished"` aus. Ein Hard-Cancel (oder jeder andere Fehler) WÄHREND des eigentlichen Downloads — `status=="downloading"`, der weitaus größte Teil der gesamten Downloaddauer und damit der realistische Fall für einen Nutzer-Abbruch — ließ `raw_downloaded_path` dadurch auf `None` stehen, obwohl yt-dlp zu diesem Zeitpunkt bereits real eine `.part`-Datei auf der Platte hat (`status["tmpfilename"]`, per yt-dlp-Quellcode verifiziert: `FileDownloader.temp_name()` = `filename + ".part"`, `status["filename"]` bleibt bis `status=="finished"` der noch nicht existierende Zielname). `cleanup_single_download_artifact()` griff dadurch im Cancel-/Fehlerfall faktisch nie, solange der Fehler während statt nach dem Rohdownload auftrat. Verifiziert (Kernfrage des Auftrags): der 24h-Start-Sweep (`download_artifact_cleanup.py::cleanup_download_artifacts()`) erfasst `.part`/`.ytdl`-Dateien bewusst NIE (dokumentierte Design-Entscheidung, siehe dortiger Docstring) — für exakt diese Artefaktklasse existierte damit **kein einziger** Cleanup-Pfad, weder sofort noch verzögert. Fix: beide `_capture_raw_downloaded_path`-Hooks werten jetzt jeden Hook-Aufruf aus (`status.get("tmpfilename") or status.get("filename")`) statt nur `"finished"` — nutzt yt-dlps eigenes, autoritatives Feld für die tatsächlich physisch vorhandene Datei, keine eigene Pfad-Rekonstruktion. Kein Verhaltensunterschied im Erfolgsfall (`"finished"` liefert kein `tmpfilename`, Fallback auf `"filename"` identisch zum bisherigen Wert). Pre-Fix-Diskriminierung via `git stash` bestätigt (3 neue Tests schlagen ungefixt fehl). Vollständige Suite: 1912 passed, 1 skipped, 0 Regressionen. Verbleibendes, bewusst nicht behobenes Restrisiko (sehr schmales Zeitfenster: Abbruch exakt vor dem allerersten Hook-Aufruf, sowie ein Bot-Crash/Kill statt eines regulären Cancel — dort läuft kein except-Block) als `OPEN (akzeptiert)` in der Zeile darunter dokumentiert. | `tests/test_download_executor_cancel.py::TestDownloadCancelledErrorIsNotRetried::test_cancel_during_downloading_status_cleans_up_part_file`, `tests/test_download_utils_single_download_cleanup.py::TestDownloadingStatusPartFileCleanup` |
| — (`utils/youtube_parser.py::_clean_title_suffixes()`, "OFFICIAL ... VERSION"-Klammer-Suffix) | CLOSED (2026-09-03) | P2 | Live-Fund (echter Testdownload über den Test-Bot): "SIDO - AUGEN AUF (OFFICIAL HD VERSION AGGROTV)" / "SIDO - SCHLECHTES VORBILD (OFFICIAL HD VERSION AGGRO BERLIN)" behielten den kompletten Klammer-Suffix in Datei-/Tag-Namen. Ursache: keine bestehende Regel in `_clean_title_suffixes()` (der YouTube-Titel-PARSING-Stufe, vor `title_cleaner.py::light_title_cleanup()`) erfasste eine Klammer, deren erstes Wort ein Marketing-Schlüsselwort ist, gefolgt von einem reinen Kanalnamen — „OFFICIAL HD VERSION AGGROTV" enthält weder „video" noch „audio" noch ein anderes bereits erkanntes Schlüsselwort. Fix: neue Regel in `_clean_title_suffixes()` entfernt die komplette Klammer, sobald das erste Wort ein bekanntes Marketing-Schlüsselwort ist (official/music/lyric/video/audio/live/version/remaster/hd/4k/vevo/explicit), unabhängig von allen Folgewörtern — nur am Titelende verankert, analog zu den übrigen Regeln dieser Funktion. Erst versuchsweise in `title_cleaner.py::light_title_cleanup()` gelöst, auf Nutzer-Entscheidung stattdessen in die vorgelagerte `youtube_parser.py`-Parsing-Stufe verlegt (konsistent mit dem bereits etablierten Bare-„prod."-Fix dort). Pre-Fix-Diskriminierung bestätigt (5 von 7 neuen Tests schlagen ungefixt fehl). Volle Suite: 2102 passed, 1 skipped, 2 vorbestehende/unabhängige Fehlschläge (siehe Zeile unten), 0 durch diesen Fix verursachte Regressionen. **Nachtrag, selbiger Tag** (zweiter echter Testdownload: "Christina Stürmer - Ein Teil von mir (offical Video) - Lyric Video" → blieb bei "Ein Teil von mir (offical Video) - Lyric"): zwei weitere Lücken am selben Muster — (1) „offical" (Tippfehler, fehlendes zweites „i", verbreiteter YouTube-Tippfehler) matchte „official" nicht, `offici?al` deckt beide Schreibweisen ab; (2) die Klammer-Regel war zwingend an das Titelende verankert — eine Marketing-Klammer, der noch ein weiterer Suffix folgt, wurde dadurch nie erfasst, selbst bei korrekter Schreibweise. Anker entfernt (bewusster Trade-off: erkennt jetzt auch eine Marketing-Klammer mitten im Titel — identische, bereits etablierte Risikoklasse wie die längst bestehenden unverankerten „(live)"/„(live in …)"-Muster in `_clean_bracket_content()`) + neue, weiterhin verankerte Regel für einen zusätzlichen, nicht geklammerten Bindestrich-Suffix am Titelende („- Lyric Video"). Pre-Fix-Diskriminierung bestätigt (5 von 7 neuen Tests schlagen ungefixt fehl). Volle Suite: 2108 passed, 1 skipped, dieselben 2 vorbestehenden/unabhängigen Fehlschläge, 0 neue Regressionen. | `tests/test_youtube_parser.py::TestCleanTitleSuffixesOfficialVersionSuffix`, `::TestCleanTitleSuffixesOfficialVersionSuffixNachtrag` |
| — (`mapping/artist_overrides.json`, verwaister „Toobrokeforfiji"-Eintrag) | CLOSED (2026-09-03) | war P3 | Beim Vollsuite-Lauf des TitleCleaner-Fixes entdeckt: `tests/test_artist_overrides_orphan_cleanup.py` schlug fehl, weil „Toobrokeforfiji" (aus den viel früheren Live-Tests der Genre-Lock-in-Phase dieser Session) in der echten `mapping/artist_overrides.json` steht, aber nicht in der statischen Test-Whitelist (`WHITELIST_VALUES_LOWER`) erfasst war. Nutzer-Entscheidung: „Toobrokeforfiji" ist ein gewollter, dauerhafter Override — Whitelist entsprechend erweitert (20 statt 19 erwartete Keys), Eintrag in `artist_overrides.json` bleibt bestehen. | `tests/test_artist_overrides_orphan_cleanup.py` |
| — (`BotStatusTracker.update_handler_status`/`record_user_activity`, Funktionslücke aus dem Handler-Sweep) | CLOSED (2026-09-03) | war P3 | Der "🤖 Bot-Status"-Screen (`handlers/enhanced_status_handler.py::show_bot_status()`) zeigt "Handler: Gesamt/Aktiv" und "User-Aktivität: Aktive Users/Letzte Aktivitäten" aus `BotStatusTracker.get_handler_overview()`/`get_user_activity()` an — beide Methoden lasen aus Feldern, die nie beschrieben wurden (`update_handler_status()`/`record_user_activity()` hatten 0 Aufrufer), Anzeige dadurch dauerhaft 0/leer. Fix: `record_user_activity()` an denselben 7 Telegram-Einstiegspunkten aufgerufen, die bereits für den Wartungsmodus-Gate-Check instrumentiert sind (gemeinsamer neuer Helfer `handlers/menu/activity_tracking.py::record_activity()`, analog zu `maintenance_gate.py`) — nur tatsächlich durchgelassene (nicht per Wartungsmodus blockierte) Interaktionen zählen. `update_handler_status()` wird in einer neuen, isoliert testbaren Methode `RichMenuHandler._record_initial_handler_statuses()` aufgerufen (aus `initialize()` herausgezogen, das selbst wegen vieler echter Handler-Konstruktionen nicht end-to-end unit-testbar ist) — zeichnet "active"/"error" für alle 10 dort initialisierten Handler auf, je nachdem ob deren Konstruktion erfolgreich war. `update_service_status()` (dritte, ebenfalls ungenutzte Methode derselben Klasse) bewusst NICHT mitbehoben — ihre "gesund"-Semantik für die 4 vordefinierten Services würde neue, nicht belegte Business-Logik über mehrere Dateien hinweg erfordern (kein Rateraten bei Unsicherheit). Pre-Fix-Diskriminierung bestätigt (10 neue Tests schlagen ungefixt fehl). Volle Suite: 2168 passed, 1 skipped, 0 Regressionen. | `tests/test_activity_tracking.py`, `tests/test_rich_menu_activity_tracking.py`, `tests/test_rich_menu_handler_activity_tracking.py` |
| — (Handler-Methoden-Level-Sweep, Cleanup der 22 bestätigten DEAD-Kandidaten) | CLOSED (2026-09-03) | war P3 | Fortsetzung/Abschluss der Cleanup-Phase des `HANDLER_METHOD_LEVEL_SWEEP_2026-09-03.md` (Nutzer-Freigabe „Punkt 4"): alle 22 damals katalogisierten DEAD-Kandidaten vor der Löschung nochmals frisch repoweit gegrept (CLAUDE.md Abschnitt 20 — Wiederholungsnachweis unmittelbar vor Löschung, nicht nur Verlass auf den älteren Audit-Stand), keiner hatte einen echten Aufrufer bekommen. Dabei 2 zusätzliche, transitiv tote Funde entdeckt und mitentfernt: `create_enhanced_logger_handler()` (einziger Aufrufer war das bereits als DEAD gelistete `integrate_enhanced_logger_handler()`) und `EnhancedErrorHandler._menu_fallback_recovery()` (einziger Aufrufer war das gelistete `handle_menu_system_error()`). Entfernt aus 10 Dateien: `handlers/enhanced_error_handler.py` (`integrate_enhanced_error_handler`, `install_global_exception_handler`, `try_catch_decorator`, `handle_menu_system_error`, `_menu_fallback_recovery`), `handlers/enhanced_logger_menu_handler.py` (`handle_log_level_change`, komplette Klassen `LoggerModuleManager`/`LoggerStatsTracker`, `create_enhanced_logger_handler`, `integrate_enhanced_logger_handler`, `ModuleLoggerManager.get_module_log_file`), `handlers/enhanced_status_handler.py` (`format_bytes`, `integrate_status_handler`), `handlers/mugge_statistik_handler.py` (`create_statistics_handler`), `handlers/navidrome_menu_handler.py` (Klasse `MediaItem` inkl. `get_display_text`, `handle_recent`, `create_navidrome_handler`, zusätzlich das dabei entdeckte, selbst nie gelesene/geschriebene `self.search_cache`-Attribut), `handlers/test_menu_handler.py` (`create_test_handler`), `handlers/menu/rich_menu_handler.py` (`set_download_handler`/`set_admin_handler` inkl. der nie anderweitig gelesenen `self.download_handler`/`self.admin_handler`-Attribute), `handlers/menu/rich_menu_system.py` (`_handle_backup_bot_start`, `_handle_backup_lib_start`, `_find_menu_by_callback`), `handlers/admin/user_management_handler.py` (`add_new_user`), `services/clients/musicbrainz_client.py` (`_extract_release_group_id`). Begleitend 2 Tests angepasst, die direkt auf gelöschte Symbole zeigten (`tests/test_test_menu_handler.py::test_create_test_handler_factory`, `tests/test_enhanced_status_handler.py::TestFormatBytes`, beide entfernt statt umgeschrieben — testeten ausschließlich die gelöschte Funktion selbst). Bewusst nicht Teil dieser PR: die 2 UNCERTAIN-Fälle (`_initiate_download`/`_handle_regular_url`, weiterhin dokumentierte Legacy-Kompat-Wrapper), die 182 SELF_ONLY-Kandidaten (bräuchten einen Reachability-Graph statt Einzel-Grep). Volle Suite: 2165 passed, 1 skipped, 0 Regressionen. **Korrektur (2026-09-03, nachträglich):** die ursprüngliche Aussage in diesem Absatz, `logger.py::setup_module_logging()` sei nach diesem Cleanup verwaist, war falsch — die Funktion hat zwei echte, aktive Aufrufer (`EnhancedLoggerMenuHandler.__init__`, `EnhancedMetadataProcessor.__init__`), verifiziert bei der separaten Analyse des Logger-Toggle-Bugs (siehe Zeile darunter). Nicht mit `toggle_module()`/`ModuleLoggerManager` verwechseln, wo der tatsächliche Bug lag. | `docs/audits/HANDLER_METHOD_LEVEL_SWEEP_2026-09-03.md` |
| — (`handlers/enhanced_logger_menu_handler.py::ModuleLoggerManager.toggle_module()`, Modul-Aktivierung schaltet Logger stumm statt Log-Datei einzurichten) | CLOSED (2026-09-03) | war P1 | Live-Fund (Nutzer-Report): das Aktivieren eines Moduls über „📁 Log-Datei umschalten" (vormals irreführend „🔄 Toggle Debug" beschriftet) im Inline-Button-Menü führte NICHT dazu, dass eine dedizierte Log-Datei geschrieben wurde. Auf den ersten Blick zwei mögliche Ursachen — separat geprüft: (1) `logger.py::setup_module_logging()` selbst ist entgegen einer fehlerhaften Aussage im vorherigen Cleanup-PR **nicht** verwaist (siehe Korrektur in der Zeile oben), (2) der tatsächliche Root Cause lag in `toggle_module()`: togglete bisher `config["enabled"]` statt `config["file_handler"]`. Für jedes real aktive, aber noch nie über dieses Menü konfigurierte Modul (praktisch alle — die einzigen 6 vorkonfigurierten Default-Einträge entsprechen keinem echten Logger-Namen mehr, verifiziert per Grep) liefert `get_module_config()` per Default `enabled=True`; der erste Klick invertierte das zu `False`, und `_apply_module_config()` behandelt `enabled=False` als kompletten Logger-Stopp (`logger.disabled = True; return`), bevor die FileHandler-Logik überhaupt erreicht wird — das Modul wurde dadurch stummgeschaltet statt eine Log-Datei zu bekommen. Reproduziert per Skript am echten Produktionscode vor dem Fix (`logger.disabled=True`, `logger.handlers=[]` nach dem „Aktivieren"). Fix: `toggle_module()` ermittelt den Ausgangszustand jetzt aus der tatsächlichen Handler-Belegung des echten Loggers (Grundwahrheit statt notionellem Config-Default) und togglet ausschließlich `config["file_handler"]`; `config["enabled"]` wird dabei explizit auf `True` gesetzt (heilt auch bereits unter dem alten Verhalten kaputt-getoggelte Module beim nächsten Klick). Button-Beschriftung und Erfolgsmeldungen entsprechend klargestellt. Pre-Fix-Diskriminierung bestätigt (3 von 5 neuen Tests schlagen ungefixt fehl). | `tests/test_enhanced_logger_menu_handler_module_toggle.py` |
| — (Hard-Cancel während FFmpeg-Postprocessing wird nicht erkannt) | OPEN (akzeptiert) | P3 | Nebenfund derselben Untersuchung (nicht der ursprünglich gemeldete Fund, separater Mechanismus als die Zeile oben): der Cancel-Check-Hook ist ausschließlich in `ydl_opts["progress_hooks"]` verdrahtet — yt-dlp ruft `progress_hooks` nur während des eigentlichen Rohdownloads auf, NICHT während des nachfolgenden FFmpeg-Postprocessing-Schritts (`FFmpegExtractAudio`, Konvertierung nach m4a/mp3). Ein Cancel-Request, der exakt in dieses Zeitfenster fällt, wird dadurch nicht bemerkt — der Download läuft als vollständiger Erfolg durch, der `cancel_event` bleibt bis zum nächsten Track/Aufruf gesetzt, ohne Wirkung auf den bereits laufenden Postprocessing-Schritt. `postprocessor_hooks` (separater yt-dlp-Mechanismus für Postprocessing-Fortschritt) ist nirgends verdrahtet. Praktische Auswirkung als gering eingeschätzt (Postprocessing eines einzelnen Audio-Tracks dauert typischerweise nur wenige Sekunden, verglichen mit der gesamten Downloaddauer) — deshalb bewusst nicht behoben, sondern als akzeptiertes Restrisiko dokumentiert statt ungefragt eine `postprocessor_hooks`-Integration zu bauen (Scope-Erweiterung über den erteilten Auftrag hinaus). Bei Bedarf eigener, dedizierter Untersuchungsauftrag. | Code-Analyse `services/downloader/download/download_executor.py::build_ydl_opts()` (nur `progress_hooks`, kein `postprocessor_hooks`), yt-dlp-Quellcode (`FileDownloader._hook_progress()` wird nur aus den Downloader-Klassen aufgerufen, nicht aus `PostProcessor`) |
| — (`scripts/reprocess_artist_metadata.py::process_file()`, beschädigte Datei bricht den gesamten Artist-Lauf ab) | CLOSED (2026-09-02) | P1 | Phase-1-Optimierungsauftrag für `reprocess_artist_metadata.py` (Nutzer-Auftrag, vollständiger Vertragsaudit): `before = snapshot(path, artist_root)` (inkl. `mutagen.mp4.MP4(path)`) lief VOR dem umschließenden `try:`-Block der Datei-Verarbeitung — eine echte unlesbare/beschädigte `.m4a`-Datei (mutagen wirft z. B. `MP4StreamInfoError`) propagierte dadurch ungefangen aus `process_file()` heraus und brach die gesamte `for`-Schleife über alle Dateien des Artists in `main()` ab, statt nur diese eine Datei als `error` zu protokollieren — Verstoß gegen den expliziten Vertrag „ein fehlerhafter Track darf nicht automatisch den gesamten Artist-Lauf zerstören". Fix: eigener, dedizierter `try`/`except` um den BEFORE-Snapshot-Read mit identischer `result`-Struktur wie der bestehende Haupt-except-Block, damit `main()`s Aggregation (Fehler-Zählung, Summary) für beide Fehlerklassen gleich funktioniert. Pre-Fix-Diskriminierung bestätigt (2 neue Tests schlagen ungefixt fehl, inkl. direkter Loop-Reproduktion: gesunde Datei nach der beschädigten wurde vorher nie erreicht). Vollständige Suite: 1926 passed, 1 skipped, 0 Regressionen. | `tests/test_reprocess_artist_metadata.py::TestCorruptedFileErrorIsolation` |
| — (`scripts/reprocess_artist_metadata.py::process_file()`, reichhaltigerer Genre-Tag wird durch schwächeres frisches Ergebnis ersetzt) | CLOSED (2026-09-02) | P2 | Gleicher Phase-1-Auftrag: eine frische `determine_genre_with_fallbacks()`-Antwort ist normales Antwortverhalten externer Quellen und kann — ohne Fehler — diesmal weniger Genre-Werte liefern als bereits im `©gen`-Tag stehen (z. B. aus einem früheren, reichhaltigeren Lauf); `_write_genres_m4a()`/`_write_genres_mp3()` (`tag_writer.py`, Produktionscode) überschreiben den Tag dann mit nur dem `primary`-Wert. Fehlte bisher ein zur MB-IDs-Regel (`before.get(key) or fresh.get(key)`) analoges Schutzmuster für Genre. Fix ausschließlich im Reprocessing-Skript (kein Eingriff in `tag_writer.py`, das auch der Produktions-Download-Pfad nutzt): neue Funktion `genre_would_downgrade()` — zählt bestehende Genre-Einträge (erkennt sowohl den aktuellen `"; "`- als auch den älteren `" / "`-Separator, siehe TagWriter-Separator-Wechsel 2026-09) gegen die frische Anzahl; bei Downgrade wird `genres_result=None` an `write_tags()` übergeben (bestehender Tag bleibt unangetastet) und `UNRESOLVED` protokolliert — symmetrisch auch in der Dry-Run-Vorhersage. Liefert die frische Bestimmung gleich viele oder mehr Werte, greift der Schutz nicht. Pre-Fix-Diskriminierung bestätigt (3 von 4 neuen Tests schlagen ungefixt fehl, die Gegenprobe war bereits korrekt grün). Vollständige Suite: 1926 passed, 1 skipped, 0 Regressionen. | `tests/test_reprocess_artist_metadata.py::TestGenreDowngradeProtection` |
| — (`scripts/reprocess_artist_metadata.py`, `DEFAULT_PRODUCTION_ROOT` veraltet — Post-Run-Safety-Check faktisch wirkungslos) | CLOSED (2026-09-02) | P1 | Nutzer-Fund nach erstem echten Testlauf (Artist „Möwe"): `DEFAULT_PRODUCTION_ROOT = Path("/mnt/4tb/library")` war veraltet — die Produktions-Library wurde inzwischen nach `/mnt/musik_bilder/library` verschoben (`config.py::Config.LIBRARY_DIR` bestätigt dies bereits, `/mnt/4tb/` wird nur noch für `BACKUP_DEST_DIR` verwendet). Wichtiger Nebeneffekt, beim Fix entdeckt: da `/mnt/4tb/library` auf dem aktuellen System vermutlich gar nicht mehr existiert, war `production_check_enabled = ... and production_root.exists()` dadurch stillschweigend `False` — der automatische Post-Run-Safety-Check gegen die Produktionsbibliothek lief effektiv nie, ohne dass dies im Log sichtbar aufgefallen wäre (`Production check enabled: False` steht zwar im Log, ist aber leicht zu übersehen). Fix: Pfad korrigiert, `docs/METADATA_REPROCESSING.md` an 3 Stellen mit aktualisiert, neuer Regressionstest verhindert künftiges stilles Veralten. Zusätzlich (Nutzer-Wunsch, gleicher Commit): feste, dauerhafte Log-Datei (`Config.LOG_DIR/script.log`, analog zu `logs/bot.log`) statt einer neuen zeit-gestempelten Datei pro Lauf eingeführt — **auf erneuten Nutzer-Wunsch (2026-09-02) wieder zurückgenommen**, siehe eigene Zeile weiter unten. | `tests/test_reprocess_artist_metadata.py::TestPathSafety::test_default_production_root_matches_current_production_library` |
| — (`scripts/reprocess_artist_metadata.py::process_file()`, Remix-Zusatz landet unnötig im Album-Fallback) | CLOSED (2026-09-02) | P2 | Nutzer-Fund, gleicher Testlauf („Möwe", Track „Blauer Tag (Robin Schulz Remix)"): fehlte ein echter Album-Tag, fiel `album_info["album"]` blind auf den kompletten `clean_title` zurück — Album wurde dadurch 1:1 identisch zum Titel inkl. Remix-Zusatz (`album: ['Blauer Tag (Robin Schulz Remix)']`). Nutzer-Entscheidung bei Rückfrage (3 Optionen vorgelegt): Album = Titel ohne den Remix-Zusatz (`'Blauer Tag'`), der Titel-Tag selbst bleibt unverändert. Fix: neue Funktion `strip_remix_suffix_for_album()` — entfernt ausschließlich ein abschließendes `(...Remix...)`-Klammerpaar (case-insensitiv), andere Klammerinhalte (z. B. `(feat. X)`) bleiben unangetastet; bleibt nach dem Strip nichts Sinnvolles übrig, wird der Originaltitel unverändert verwendet (kein leeres Album raten). Greift nur, wenn kein Album-Tag bereits vorhanden ist — bestehende Werte haben weiterhin Vorrang. Pre-Fix-Diskriminierung bestätigt (4 von 5 neuen Tests schlagen ungefixt fehl, die Gegenprobe mit vorhandenem Album-Tag war bereits korrekt grün). Vollständige Suite: 1929 passed (+5 neue), 1 skipped, 0 Regressionen. | `tests/test_reprocess_artist_metadata.py::TestAlbumFallbackStripsRemixSuffix` |
| — (`scripts/reprocess_artist_metadata.py::process_file()`, Produzenten-Credit bleibt im Titel-Tag stehen) | CLOSED (2026-09-02) | P2 | Nutzer-Fund, gleicher Testlauf (Artist „makko", Track `'"ADLIBS" prod. Safecall777'`): der Titel-Tag behielt den Produzenten-Credit unverändert. Verifiziert per echtem `utils.youtube_parser.parse_youtube_title()`-Aufruf: selbst die volle Download-Pipeline liesse diesen exakten Titel unverändert — die dortige `_clean_title_suffixes()`-Regel erkennt nur die geklammerte `"(prod...)"`- oder Bindestrich-getrennte `"- prod..."`-Form, nicht die hier vorliegende klammerlose, trennerlose Form (Nutzer-Annahme „wie bei meiner Download Pipeline" war unzutreffend — siehe eigenständiger Fund darunter). Fix ausschließlich im Reprocessing-Skript (kein Eingriff in die geteilte Produktionslogik): neue Funktion `strip_producer_credit()`, angewendet direkt auf `clean_title` nach `TitleCleaner.light_title_cleanup()` — deckt geklammerte und klammerlose Form ab, `\bprod\b` mit zwingendem „."/Whitespace danach verhindert Fehltreffer in Wörtern wie „Producer"/„Production". Pre-Fix-Diskriminierung bestätigt (5 von 6 neuen Tests schlagen ungefixt fehl). Thematische Suite (Regel 8.A: kein Grund für volle Suite bei diesem eng umrissenen Einzel-Fix): 75 passed. | `tests/test_reprocess_artist_metadata.py::TestProducerCreditStrippedFromTitle` |
| — (`utils/youtube_parser.py::_clean_title_suffixes()`, klammerlose/trennerlose „prod. X"-Form wird nicht erkannt) | CLOSED (2026-09-03) | P3 | Die bestehenden Regeln deckten nur `\(prod[^)]*\)` (geklammert) und `\s*[-–—]\s*prod[^)]*$` (Bindestrich-getrennt) ab — ein Titel wie `'"ADLIBS" prod. Safecall777'` (kein Bindestrich, keine Klammer vor „prod.") blieb dadurch auch in der echten `parse_youtube_title()`-Pipeline unverändert. Fix analog zum bereits etablierten Muster in `title_cleaner.py::light_title_cleanup()` (dort für den finalen Titel-Tag bereits gelöst, hier für die vorgelagerte Artist-/Titel-Parsing-Stufe): neue Regel `\s*\bprod\.?\s+\S.*$`, `\bprod\b` mit zwingendem „."/Whitespace danach verhindert Fehltreffer in Wörtern wie „Producer"/„Production". Pre-Fix-Diskriminierung bestätigt (3 von 9 neuen Tests schlagen ungefixt fehl, inkl. End-to-End über `parse_youtube_title()`). Ein bestehender Test (`test_youtube_parser_double_space_multi_artist.py`, PR #118) dokumentierte das alte, unvollständige Verhalten im erwarteten `song_title` mit — Assertion auf das jetzt korrekte, bereinigte Ergebnis aktualisiert (Multi-Artist-Comma-Prefix-Kern des Tests unberührt). Änderung an geteilter Produktionslogik (breiter Blast-Radius) — vollständige Suite: 2045 passed, 1 skipped, 0 Regressionen. | `tests/test_youtube_parser.py::TestCleanTitleSuffixesBareProdForm` |
| — (`title_cleaner.py::light_title_cleanup()`, Produzenten-Credit bleibt im Titel-Tag) | CLOSED (2026-09-02) | P1 | Nutzer-Fund, echter Testdownload (Artist „makko", Track `'"ADLIBS" prod. Safecall777'`): der Titel-Tag behielt den Produzenten-Credit unverändert — live im Bot-Log bestätigt (Title/Album/Dateiname/MusicBrainz-Suchtitel alle betroffen). Root Cause, repoweit verifiziert: `light_title_cleanup()` ist der EINZIGE tatsächlich erreichbare Titel-Cleanup-Pfad der Produktions-Pipeline (`enhanced_metadata_processor.py` Schritt 7 ruft ausschließlich diese Methode auf) und hatte keinerlei „prod."-Regel. Nebenfund dabei: `TitleCleaner.clean_track_title_enhanced()`/`apply_title_cleanup_rules()` — mit deutlich reichhaltigeren Cleanup-Regeln (u. a. feat./ft.-Entfernung, Marketing-Suffixe) — haben repoweit verifiziert **keine Produktionsaufrufer**, laufen im echten Betrieb nie; nur in eigenen, isolierten Unit-Tests direkt aufgerufen (`test_title_cleaner_feat_ft_no_space.py`, `test_title_cleaner_marketing_suffix_bracket.py` u. a.) — bewusst NICHT in dieser Änderung angefasst/reaktiviert (separater, nicht angefragter Fund, eigene Entscheidung nötig ob Wiederverdrahtung oder Entfernung als toter Code). Fix: zwei neue Regeln direkt in `light_title_cleanup()` — geklammerte Form (`"(prod. by X)"`) und klammerlose/trennerlose Form am Titelende (`"Titel prod. X"`, auch von `utils/youtube_parser.py::_clean_title_suffixes()` nicht erkannt, per `parse_youtube_title()`-Direktaufruf verifiziert). `\bprod\b` mit zwingendem „."/Whitespace danach verhindert Fehltreffer in Wörtern wie „Producer"/„Production". Pre-Fix-Diskriminierung bestätigt (5 von 7 neuen Tests schlagen ungefixt fehl). Änderung an geteilter Produktionslogik (breiter Blast-Radius, alle künftigen Downloads) — vollständige Suite gemäß CLAUDE.md Abschnitt 8.A durchgelaufen: 1929 passed, 1 skipped, 0 Regressionen. | `tests/test_title_cleaner_producer_credit_suffix.py` |
| — (`title_cleaner.py::light_title_cleanup()`, umschließende Anführungszeichen bleiben im Titel-Tag) | CLOSED (2026-09-02) | P2 | Nutzer-Auftrag im Anschluss an die Zeile oben: vollständiger Scan von `/tmp/musicbot_test/metadaten` (13 Dateien, 2 Artists) nach weiteren ungültigen Symbolen, bevor der TitleCleaner erneut geändert wird. Fund: 7 von 13 Tracks des Artists „makko" haben einen Titel, der vollständig von Anführungszeichen umschlossen ist (`'"ADLIBS"'`, `'"Bequem"'`, `'"Grad mal ein Jahr"'`, `'"Dein Lügner"'`, `'"Pueblo"'`, `'"Ausreden"'`, `'"Jänner"'`, `'"Zickzack"'`) — offenbar eine YouTube-seitige Stilisierung dieses Artists, bisher nie entfernt. Zusätzlich gegen `/tmp/musicbot_test/library` (114 Dateien) auf sonstige verdächtige Unicode-Kategorien geprüft — einziger weiterer Treffer (`"Superst✩rs"`, 01099) als bewusste künstlerische Stilisierung identifiziert (auch im Dateinamen identisch vorhanden) und bewusst NICHT angefasst — Bereinigung darf keine fachlich relevante Information zerstören. Fix: entfernt genau EIN Anführungszeichen-Paar, das den GESAMTEN (bereits um Produzenten-Credits bereinigten — Reihenfolge-kritisch, siehe Zeile oben) Titel umschließt; deckt gerade sowie gängige typografische Varianten (deutsch/französisch/englisch) ab. Ein einzelnes Apostroph MITTEN im Titel (`"It Ain't Me"`, `"als ob ich's einfach hätte"`, beide real in der Library bestätigt) sowie normale Interpunktion (`"für immer?"`) bleiben unangetastet, da sie nicht gleichzeitig Anfang UND Ende des Titels bilden. Pre-Fix-Diskriminierung bestätigt (3 von 8 neuen Tests schlagen ungefixt fehl — die übrigen 5 sind Sicherheitsfälle, die auch ungefixt bereits korrekt unverändert bleiben). Vollständige Suite: 1929 passed, 1 skipped, 0 Regressionen. | `tests/test_title_cleaner_wrapping_quotes.py` |
| — (`enhanced_metadata_processor.py`, MusicBrainz-Suche nutzt unbereinigten Titel) | CLOSED (2026-09-02) | P2 | Nutzer-Fund, gleicher Live-Testdownload: der finale Titel-Tag wurde nach den beiden Zeilen oben korrekt bereinigt (`ADLIBS`), aber MusicBrainz (Genre- UND Album-Suche) suchte weiterhin mit dem unbereinigten Titel (`'"ADLIBS" prod. Safecall777'`) — live im Bot-Log bestätigt, während Lyrics/Cover zur selben Zeit bereits korrekt `ADLIBS` verwendeten. Root Cause: `search_title_for_genre` (an `GenreProcessor` UND `AlbumProcessor.fetch_album_from_musicbrainz()` weitergereicht) wurde über `TitleCleaner.build_search_title()` NEU aus den rohen Quellen (`youtube_parsed.get("song_title")`/`raw_title`) berechnet — ein separater Pfad, der von `light_title_cleanup()` (inkl. dessen Produzenten-Credit-/Anführungszeichen-Fixes) nie profitierte; `clean_title` (der tatsächlich final geschriebene, bereits vollständig bereinigte Titel) wurde nur für Lyrics/Cover verwendet. Fix: `search_title_for_genre` wird jetzt aus dem bereits bereinigten `clean_title` abgeleitet — `build_search_title()` macht darauf weiterhin seine eigene, zusätzliche Versions-/Remaster-Bereinigung für die Suchanfrage, keine doppelte/abweichende Bereinigungslogik. Pre-Fix-Diskriminierung bestätigt (neuer Test schlägt gegen unfixierten Code fehl, real reproduziert mit echten Produktionsklassen). Änderung an zentraler Produktionslogik (breiter Blast-Radius) — vollständige Suite: 1930 passed, 1 skipped, 0 Regressionen. | `tests/test_enhanced_metadata_processor_search_title_uses_cleaned_title.py` |
| — (`scripts/reprocess_artist_metadata.py::process_file()`, vorhandenes Album-Tag wird nie bereinigt) | CLOSED (2026-09-02) | P2 | Nutzer-Fund, echter Testlauf Artist „makko" über `main` (vor Merge dieses Branches): das bisherige `existing_album or strip_remix_suffix_for_album(clean_title)` nahm ein vorhandenes Album-Tag IMMER unverändert — reale Bestandsdateien haben aber so gut wie immer bereits ein Album-Tag (meist eine 1:1-Kopie des ursprünglich dirty Titels), wodurch der Album-Fallback-Schutz (Zeile oben, `strip_remix_suffix_for_album()`) in der Praxis kaum je griff. `'"Bequem"'`/`'"Zickzack"'`/`'"ADLIBS" prod. Safecall777'` blieben live als Album-Wert unverändert stehen, obwohl der zugehörige Titel zur selben Zeit korrekt bereinigt wurde (dank der bereits gemergten `light_title_cleanup()`-Fixes). Nutzer-Entscheidung bei Rückfrage: ein vorhandenes Album-Tag wird jetzt denselben Bereinigungsregeln unterzogen wie der Titel — `processor.title_cleaner.light_title_cleanup(existing_album, "")` (leerer Artist-Parameter, kein Artist-Präfix-Muster bei Albumnamen), keine zweite, abweichende Bereinigungslogik, dieselbe bereits produktiv laufende Regelmenge (Produzenten-Credit + umschließende Anführungszeichen). Pre-Fix-Diskriminierung bestätigt (3 von 4 neuen Tests schlagen ungefixt fehl, die Gegenprobe mit bereits sauberem Album war bereits korrekt grün). Branch zusätzlich mit `main` synchronisiert (PR #114/#115 nachgezogen), damit `light_title_cleanup()` die dafür nötigen Regeln überhaupt enthält. Vollständige Suite: 1960 passed, 1 skipped, 0 Regressionen. | `tests/test_reprocess_artist_metadata.py::TestExistingAlbumTagIsAlsoCleaned` |
| — (`scripts/reprocess_artist_metadata.py`, feste Log-Datei wieder zurückgenommen) | CLOSED (2026-09-02) | — | Nutzer-Wunsch: die zuvor eingeführte feste, dauerhafte Log-Datei (`Config.LOG_DIR/script.log`, analog zu `logs/bot.log`) wurde wieder zurückgenommen — zurück zu einer eigenen, zeit-gestempelten Datei pro Lauf (`/tmp/musicbot_test/metadata_reprocessing_<ARTIST>_<TIMESTAMP>.log`, ursprüngliches Verhalten). Die beiden zugehörigen Tests (`test_log_written_to_fixed_persistent_path_under_log_dir`, `test_second_run_appends_to_same_log_file_instead_of_new_file`) durch einen neuen Test ersetzt, der das wiederhergestellte Timestamp-Verhalten charakterisiert. `docs/METADATA_REPROCESSING.md` entsprechend zurückgesetzt. | `tests/test_reprocess_artist_metadata.py::TestMainIntegration::test_log_written_to_new_timestamped_file_per_run` |
| — (`title_cleaner.py::light_title_cleanup()`, nie geschlossene Klammer bleibt im Titel-Tag) | CLOSED (2026-09-02) | P2 | Nutzer-Fund, echter Testlauf Artist „makko": `'MAKKO 7er STOCK (Dir.'` blieb unverändert stehen (vermutlich ein abgeschnittener Regie-/Video-Credit, dessen schließende Klammer im YouTube-Titel fehlt). Dasselbe Muster existierte bereits als letztes Sicherheitsnetz in `TitleCleaner.apply_title_cleanup_rules()` (`r"\s*\([^)]*$"`) — diese Methode hat aber repoweit verifiziert keine Produktionsaufrufer (siehe Zeilen oben zu den Produzenten-Credit-/Anführungszeichen-Funden) und lief für diesen Titel nie; `light_title_cleanup()` hatte bisher keine entsprechende Regel. Fix: dieselbe, bereits etablierte Regel (nie geschlossene `(` bzw. `[` bis Titelende entfernen) als letzte Regel in `light_title_cleanup()` ergänzt — balancierte Klammern mit echtem schließendem Gegenstück (`"(feat. X)"`, `"[Remix]"`) bleiben unangetastet. Pre-Fix-Diskriminierung bestätigt (2 von 6 neuen Tests schlagen ungefixt fehl, die 4 Sicherheitsfälle waren bereits korrekt grün). Änderung an geteilter Produktionslogik (breiter Blast-Radius) — vollständige Suite: 1965 passed, 1 skipped, 0 Regressionen. | `tests/test_title_cleaner_dangling_bracket.py` |
| — (`utils/youtube_parser.py::parse_youtube_title()`, Multi-Artist-Praefix nur durch doppeltes Leerzeichen getrennt wird nicht erkannt) | CLOSED (2026-09-02) | P2 | Nutzer-Fund, echter Testdownload nach Loeschen von `Toobrokeforfiji` aus der isolierten Test-Library: `'toobrokeforfiji, Sin Davis & makko  40 Stunden Woche prod. overshiaat official video'` wurde zu `'toobrokeforfiji, Sin Davis & makko 40 Stunden Woche'` statt zu `'40 Stunden Woche'` bereinigt. Root Cause: YouTube trennt bei Feature-Collabs die komplette Artist-Liste vom Songtitel manchmal NUR durch ein doppeltes Leerzeichen, kein von `_parse_artist_and_title()` erkanntes Standard-Trennzeichen (`-–—:|`) — der Titel landete komplett im „Kein Artist gefunden"-Fallback. Zwei Loesungsversuche AN SPAETERER STELLE scheiterten zuerst: (1) `light_title_cleanup()` — das Doppel-Leerzeichen-Signal war dort bereits durch `parse_youtube_title()`s eigene Whitespace-Normalisierung (`_clean_bracket_content()`) verloren, live per Redownload-Test verifiziert fehlgeschlagen; (2) „bekannte Feature-Artists als Parameter nutzen" (Nutzer-Entscheidung nach vorgelegten Optionen) — in genau diesem Fall sind die Feature-Artists nirgends in der Pipeline bekannt, weil der Parser selbst am Erkennen scheiterte (das IST das Problem). Fix (nach Ruecksprache, korrigierter Vorschlag): Vorverarbeitungsschritt GANZ am Anfang von `parse_youtube_title()`, auf dem noch unveraenderten Roh-Titel — ersetzt ein doppeltes Leerzeichen durch das bereits unterstuetzte Standard-Trennzeichen `" - "`, wenn der linke Teil als Multi-Artist-Liste erkennbar ist (>= 2 Namen via `_split_multi_artists()`); die bestehende, bereits getestete Parsing-Pipeline laeuft danach unveraendert weiter, keine Logik-Duplizierung. `original_title` bleibt durch separate Variable der echte, unveraenderte Rohtitel. Pre-Fix-Diskriminierung bestätigt (3 von 8 neuen Tests schlagen ungefixt fehl, 5 Sicherheitsfaelle — inkl. zufaelliges Doppel-Leerzeichen ohne Namensliste — waren bereits korrekt gruen). Aenderung an zentraler, breit genutzter Parser-Komponente (breiter Blast-Radius) — vollständige Suite: 1973 passed, 1 skipped, 0 Regressionen. Live-verifiziert per echtem Redownload in der isolierten Testumgebung: Titel-Tag `'40 Stunden Woche'`, Dateiname `2021 - 40 Stunden Woche.m4a`, Artist-Tag korrekt als `['Toobrokeforfiji', 'SIN Davis', 'makko']` (alle drei Feature-Artists werden dank des Fixes jetzt zusätzlich korrekt erkannt statt nur als Titel-Text zu verbleiben). | `tests/test_youtube_parser_double_space_multi_artist.py` |
| ARCH-022 (`genre_processor.py::determine_genre_with_fallbacks()`, CONFIRMED-Genre-Eintrag wird nie mit frischen Last.fm-Daten abgeglichen) | CLOSED (won't-fix, nur manuell, 2026-09-03) | P2 | Nutzer-Fund, Live-Recherche Artist „Toobrokeforfiji": Last.fm liefert heute 8 Tags, aus denen der aktuelle `prioritize_genres()`-Code bereits `secondary=[Hip Hop]` ableiten würde — der gespeicherte Bestandseintrag hatte `secondary: []` und wurde nie aktualisiert. Root Cause, im Code verifiziert: sobald ein Genre-Eintrag `LEARNED` wird (>= 2 Beobachtungen), landet er in `GenreMapper.artist_map` und wird von Schritt 1/2 der Fallback-Kette als „bekanntes Genre" behandelt — Schritt 3 (MusicBrainz) läuft weiterhin für IDs, Schritt 4 (Last.fm) wird aber nie mehr erreicht (`genre_processor.py:164`, frühes `return known_result`), `learn_genre()` wird dadurch auch nie erneut aufgerufen. Nutzer-Entscheidung nach vorgelegten Trade-off-Optionen (zeitbasiert/beobachtungsbasiert/nur-manuell): bewusst KEIN automatisches Revalidieren im kritischen Download-Pfad — kleinste, risikoärmste Änderung, kein neues Zeit-/Zähler-Verhalten. Wer einen Artist neu bewerten lassen möchte, nutzt gezielt `scripts/reprocess_artist_metadata.py`. Charakterisiert (nicht gefixt) in `tests/test_genre_processor_revalidation_gap.py`, inkl. einer während der Testentwicklung selbst korrigierten, zu pauschalen Erst-Annahme (MusicBrainz wird weiterhin für IDs aufgerufen, nur Last.fm wird übersprungen). Im selben ARCH-022-Zug zusätzlich behoben: Namespace-Trennung von `auto_learned_artists.yaml` (vermischte zwei komplett getrennte Systeme in einer Datei) in zwei eigene Dateien, nicht-atomarer Schreibpfad in `ArtistNormalizer._save_auto_learned_entry()` gefixt, YAML→JSON-Migration der drei rein maschinell geschriebenen Auto-Learn-Dateien, Reset der als Migrations-Artefakt erkannten Bestandsdaten (32 Genre-Einträge mit je exakt 4 identischen Beobachtungen, 1 fehlerhafter Artist-Alias). Vollständige Doku in `docs/GENRE_SYSTEM.md`. Vollständige Suite über beide Teil-PRs: 1982 passed, 1 skipped, 0 Regressionen. | `docs/GENRE_SYSTEM.md`, `tests/test_genre_processor_revalidation_gap.py` |
| — (`auto_learn.py`, Genre-Mehrheitsvotum stabilisiert sich nie zu einem artist-weiten Mapping) | CLOSED (2026-09-03) | P2 | Nutzer-Fund direkt im Anschluss an die Zeile oben, Artist „Toobrokeforfiji": reines Mehrheitsvotum über die letzten 10 Beobachtungen berechnet `primary` bei *jeder* neuen Beobachtung neu — bei vielen Tracks/wechselnden Last.fm-Tags stellt sich nie ein stabiles, artist-weites Genre-Mapping ein. Nutzer-Auftrag: neue Lock-in-Regel — sobald ein Genre 3x als `primary` beobachtet wurde, wird es dauerhaft gelockt; ein Herausforderer übernimmt den Lock erst bei der 3-fachen (live, nicht eingefrorenen) Beobachtungszahl des gelockten Werts. Abgelehnte Herausforderer erscheinen (Nutzerentscheidung) explizit in `secondary`. Fix: neue reine Funktionen `_compute_genre_lock_decision()`/`_derive_genre_primary_secondary()`, additive Felder `locked_primary`/`genre_counts` (ungekappt, im Unterschied zum weiterhin auf 10 gekappten `observation_log`) — `utils/genre_map.py` liest unverändert nur `primary`/`secondary`, kompatibel. Siehe `docs/GENRE_SYSTEM.md` Abschnitt 4.a. Vollständige Suite: 1996 passed, 1 skipped, 0 Regressionen. | `tests/test_auto_learn_genre_lock_in.py` |
| — (`auto_learn.py::learn_genre()`, Artist-Namens-Override blockiert Genre-Lernen vollständig) | CLOSED (2026-09-03) | P1 | Bei derselben Untersuchung entdeckt: `learn_genre()` schrieb NIE einen Genre-Eintrag für einen in `mapping/artist_overrides.json` gelisteten Artist (Artist-**Namens**-Normalisierung, raw_name → canonical_name — kein Genre-Bezug). Verifiziert betraf das 78 von 174 Override-Artists ohne manuelles Genre in `artist_genre.yaml` (u. a. Toobrokeforfiji) — diese konnten dadurch nie ein Auto-Learn-Genre erhalten, unabhängig von der Lock-in-Regel (Zeile oben). Pre-Fix-Diskriminierung bestätigt (2 neue Tests schlagen ungefixt fehl, per `git stash` re-verifiziert nach einer Testüberarbeitung). Fix: Override-Block in `learn_genre()` vollständig entfernt — Genre-Learning und Artist-Namens-Normalisierung sind jetzt unabhängige Mechanismen. Der separate Feature-Artist-Override-Check (`_is_artist_known()`/`_compute_featured_artist_decision()`/`observe_featured_artists()`) blieb unverändert (als direkte Regression re-verifiziert: `test_featured_artist_already_in_overrides_is_skipped`). Vollständige Suite: 2001 passed, 1 skipped, 0 Regressionen. | `tests/test_auto_learn_genre_learning_independent_of_name_override.py` |
| — (`mapping/artist_overrides.json`, 174 verwaiste Einträge aus früherer, größerer Library) | CLOSED (2026-09-03) | P2 | Im selben Zug identifiziert: die Produktions-Library (`config.LIBRARY_DIR`) wurde kürzlich neu aufgebaut und hat nur noch 12 Artist-Ordner — `artist_overrides.json` enthielt weiterhin 174 eindeutige Werte aus einem früheren, deutlich größeren Bestand (inkl. eines offensichtlichen Datenmüll-Eintrags `"__pycache__"`). Nutzer-Entscheidung: nicht strikt auf die 12 Library-Artists reduziert (Risiko, bereits live bestätigte Normalisierungs-Fixes für nicht mehr in der Library vorhandene Artists zu verlieren), sondern Whitelist = 12 Library-Artists ∪ Werte, die von den bestehenden Daten-Integritätstests `test_artist_overrides_t_low_case_preserve.py`/`test_artist_overrides_miksu_macloud_duo.py` direkt gegen diese Datei geprüft werden. Bereinigt von 174 auf 19 eindeutige Keys. Neuer Integritätstest schützt gegen erneutes Anwachsen. Blast-Radius (`filenamefixer.py`, `download_utils.py`, `playlist_processor.py`, `duplicate/detector.py` — alle nur Leser über `ArtistNormalizer`) verifiziert, keine Regressionen. | `tests/test_artist_overrides_orphan_cleanup.py` |
| — (`scripts/reprocess_artist_metadata.py`, Härtung vor geplanter Telegram-Menü-Integration: Singleton-Bleeding + `sys.exit()` + ungeschützter Post-Run-Check) | CLOSED (2026-09-03) | P0 | Nutzer-Auftrag „Punkt 3, final prüfen, bevor an Telegram-Menü angebunden wird": Read-only-Audit fand 2 neue P0- und 1 P1-Kandidat, alle selbst am Code verifiziert (nicht nur vom Auditor übernommen), spezifisch relevant erst bei einem künftigen in-process-Aufruf aus dem laufenden Bot (nicht beim heutigen CLI-Subprozess-Betrieb). **P0.1:** `EnhancedMetadataProcessor`/`ArtistNormalizer`/`GenreMapper` sind `SingletonMixin` (`utils/singleton.py`, „First Mover gewinnt") — ein in-process-Aufruf aus einem bereits laufenden Bot würde trotz importiertem `config_test.Config` unbemerkt die mit echter `config.Config` konstruierte Produktivinstanz zurückbekommen und über `auto_learn_manager` direkt in echte `mapping/auto_learned_*.json` schreiben; `processor.aclose()`/`cleanup()` am Skriptende würden zusätzlich Ressourcen (Genius-Session, Metadata-Cache) der laufenden Produktivinstanz abreissen. Exakt dieses Singleton-Bleeding-Muster hat in diesem Repo bereits real `mapping/case_preserve.yaml` und `mapping/artist_overrides.json` verunreinigt (siehe `tests/conftest.py::reset_singletons()`-Docstring), bisher nur über einen anderen Auslöser (Tests). **P0.2:** `sys.exit(1)` bei ungültigem `--input` wirft `SystemExit` (erbt von `BaseException`, nicht von einem `except Exception` eines künftigen Telegram-Handlers gefangen) — könnte bei in-process-Aufruf den gesamten Bot-Prozess für alle Nutzer beenden. **P1:** Post-Run-Safety-Check (Struktur-/Production-Snapshot NACH dem Lauf) lag komplett ohne Fehlerbehandlung hinter dem Datei-Loop — ein Crash dort nach bereits erfolgreichem, sicherem Lauf hätte wie ein harter Absturz ausgesehen, Log wäre unvollständig offen geblieben. Architektur-Entscheidung (Nutzer, statt den Singleton-Mechanismus der geteilten Metadata-Pipeline-Klassen selbst anzufassen — CLAUDE.md „kein Refactor als erste Reaktion"): die künftige Telegram-Integration ruft dieses Skript weiterhin ausschließlich als eigenständigen Subprozess auf (`docs/METADATA_REPROCESSING.md` Abschnitt 2a), wodurch P0.1/P0.2 durch Bauweise irrelevant werden. Zusätzlich, unabhängig vom Aufruf-Modell, als aktives Sicherheitsnetz gehärtet: neue `assert_processor_singletons_are_fresh()` bricht mit `SingletonSafetyError` ab, sobald eine der drei Klassen in diesem Prozess bereits initialisiert wurde, BEVOR irgendeine Datei angefasst wird; `sys.exit(1)` nur noch im CLI-Entry-Point, `main()` wirft regulär `PathSafetyError`; Post-Run-Block in eigenes `try/except` gefasst (Log wird in jedem Fall sauber abgeschlossen, Exception wird nicht verschluckt). 3 unsichere Nebenfunde (AutoLearn-Zähler-Dedup bei wiederholten Live-Läufen, TOCTOU zwischen Symlink-Check und Zugriff, Log-Handle-Leak bei Crash außerhalb `process_file()`) bewusst nicht behoben — P2/P3, keine konkrete Ausnutzbarkeit belegt. Pre-Fix-Diskriminierung bestätigt (8 von 8 neuen Tests schlagen ungefixt fehl). Vollständige Suite: 2178 passed, 1 skipped, 0 Regressionen. | `docs/METADATA_REPROCESSING.md` Abschnitt 2a/12, `tests/test_reprocess_artist_metadata.py::TestSingletonSafetyGuard`/`TestMainNoLongerCallsSysExit`/`TestPostRunSafetyCheckExceptionIsolation` |
| — (Metadata-Reprocessing, Telegram-Menü-Integration) | CLOSED (2026-09-03) | war P3 | Direkter Folgeschritt zur Härtung in der Zeile oben: `scripts/reprocess_artist_metadata.py` jetzt über einen neuen „🔧 Reprocessing"-Menüpunkt im Admin-Menü nutzbar, nicht mehr nur per CLI. Nutzer-Entscheidungen (vorab geklärt): nur Owner (nicht Owner+Admin, greift auf Metadata-/Auto-Learn-Dateien zu), Artist-Auswahl über Buttons aus den vorhandenen Verzeichnissen unter `/tmp/musicbot_test/metadaten/` (kein Freitext), zwingend erst Dry-Run mit anschließendem „🔴 Jetzt LIVE ausführen"-Button statt direktem Live-Einstieg. Neu: `services/metadata/reprocessing_runner.py` (reine Subprozess-Orchestrierung, `asyncio.create_subprocess_exec`, kein Telegram-Import — Schichtgrenze `services/`) und `handlers/menu/reprocessing_menu_handler.py::ReprocessingMenuHandler` (Artist-Liste, Dry-Run/Live-Flow, HTML-escapte Ergebnis-Formatierung). Läuft als Hintergrund-Task (`asyncio.create_task()`, identisches Muster zu `_process_url()`/Bug A) — verhindert, dass ein mehrminütiger Lauf jedes weitere Telegram-Update blockiert. `^reprocess:`-Pattern in `get_telegram_handlers()` sofort mitregistriert (Bug-B-Regressionstest inklusive, nicht erst nach einem Live-Fund nachgezogen wie bei `dl:`/`maint:`). Volle Suite: 2216 passed, 1 skipped, 0 Regressionen. | `docs/MusicBot_TELEGRAM_MENU_SYSTEM.md` Abschnitt 5, `tests/test_reprocessing_runner.py`, `tests/test_reprocessing_menu_handler.py`, `tests/test_rich_menu_reprocessing.py` |
| — (`scripts/reprocess_artist_metadata.py`, Titel-Remix-Zusatz jetzt konsistent zur Download-Pipeline entfernt) | CLOSED (2026-09-03) | P2 | Nutzer-Fund über die neue Telegram-Reprocessing-Integration (Zeile oben), Artist „Möwe", Track „Blauer Tag (Robin Schulz Remix)": Titel-Tag blieb unverändert mit Remix-Zusatz stehen. Verifiziert per Code-Lesung: die echte Download-Pipeline (`utils/youtube_parser.py::parse_youtube_title()` Schritt 7, `_clean_bracket_content(song_title, preserve_remix=False)`) entfernt einen Remix-Zusatz aus dem finalen Titel — die frühere Entscheidung vom 2026-09-02 (siehe Zeile „Remix-Zusatz landet unnötig im Album-Fallback" weiter oben: nur Album bereinigen, Titel unverändert lassen) war damit inkonsistent zur echten Pipeline. Nutzer-Entscheidung (explizit bestätigt): bewusste Umkehr — Titel folgt jetzt derselben Regel. `strip_remix_suffix_for_album()` umbenannt zu `strip_remix_suffix()` (identische Regex, jetzt generisch), im TitleCleaner-Schritt direkt auf `clean_title` angewendet statt nur im separaten Album-Fallback. Da `clean_title` auch für den Dateinamen (`expected_stem`) verwendet wird, betrifft die Bereinigung bewusst (Nutzer-Entscheidung: „konsequent wie die Download-Pipeline") automatisch auch bestehende Dateinamen mit Remix-Zusatz — Rename-Logik wandelt z. B. `2013 - Blauer Tag (Robin Schulz Remix).m4a` in `2013 - Blauer Tag.m4a` um. Album-Fallback vereinfacht (erbt den bereits remix-freien `clean_title`, kein zweiter Strip-Aufruf mehr nötig). Pre-Fix-Diskriminierung bestätigt (5 von 5 aktualisierten Tests schlagen gegen den alten Code fehl). | `docs/METADATA_REPROCESSING.md` Abschnitt 5, `tests/test_reprocess_artist_metadata.py::TestAlbumFallbackStripsRemixSuffix` |
| — (`scripts/reprocess_artist_metadata.py`, Album einer Single blieb trotz bereinigtem Titel mit Remix-Zusatz stehen) | CLOSED (2026-09-03) | P2 | Nutzer-Fund direkt im Anschluss an die Zeile oben (Nutzer-Report: „title: ['Blauer Tag'] / album: ['Blauer Tag (Robin Schulz Remix)'] — bei einer Single muss Album immer gleich Titel sein"): ein vorhandenes Album-Tag durchläuft nur `light_title_cleanup()`, das laut eigenem Docstring bewusst konservativ ist und keine Remix-Zusätze entfernt (anders als `strip_remix_suffix()`, das jetzt auf den Titel angewendet wird, Zeile oben) — Titel und Album liefen dadurch auseinander, sobald bereits ein Album-Tag mit Remix-Zusatz vorhanden war. Nutzer-Regel (explizit): bei einer Single (Track im Singles-Ordner, `is_singles`) ist das Album per Definition IMMER identisch zum Titel — ein davon abweichender, vorhandener Album-Name wird dort NICHT mehr übernommen, sondern immer durch `clean_title` ersetzt. Für Album-Tracks (eigener Ordner mit Tracknummer) bleibt ein vorhandener, eigenständiger Album-Name weiterhin maßgeblich — dort ist ein vom Titel abweichender Name die Regel, nicht die Ausnahme (unverändert seit der 2026-09-02-Entscheidung, gegengeprüft). Pre-Fix-Diskriminierung bestätigt (1 von 2 neuen/überarbeiteten Tests schlägt gegen den alten Code fehl, die Gegenprobe für Album-Tracks war bereits korrekt grün). | `docs/METADATA_REPROCESSING.md` Abschnitt 5, `tests/test_reprocess_artist_metadata.py::TestAlbumFallbackStripsRemixSuffix::test_single_existing_album_tag_is_overridden_to_match_title`/`test_album_track_existing_album_tag_is_still_preserved` |
| — (Smart Library Repair, Phase 2 — alle 6 Executoren + Duplicate implementiert) | CLOSED (2026-09-04) | war P2 | Phase 2 „Smart Library Repair" auf demselben Branch/PR. **Repair Planner (erledigt):** `services/library_repair/`, `scripts/library_repair.py` — `plan_repairs(health_report) -> RepairPlan`, Registry mit genau einem Mapping pro Health-Issue-Code auf Level + bestehende ausführende Komponente. Read-only. **Level-1-Executor (erledigt):** `apply_level1()` — nur die vier verlustfreien Tag-Fixes (Genre-Delimiter, Multi-Artist-Split/Dedup, Album-Artist). Pro Datei: `safety_check` (Symlink/Root/Typ/leer), Reparaturwert via reine `tag_repairs`-Funktionen, Backup außerhalb der Library, Schreiben auf temp-Sibling + Verifikation (Ziel-Atome + **Audio-Essenz-MD5 byte-identisch**) + atomarer replace, Rollback bei Fehler, Append-Only-Journal, Verification-Scan (Ziel-Codes ↓, keine neuen/gestiegenen Issues → sonst Exit 1). **Erster Produktionslauf 2026-09-04:** 12/12 SUCCESS, Ton unverändert, Health 97.9→97.9, GENRE_DELIMITER_INCONSISTENT 3→0, MULTI_ARTIST_INCONSISTENT 9→0. **L1-Rename (erledigt):** `apply_level1_rename()` für `FILENAME_TITLE_MISMATCH`/`FILENAME_SUSPICIOUS` — ersetzt nur den Titel-Teil, behält den `NN - `/`YYYY - `-Präfix (kein Konvention-Raten), nur bei additivem Zusatz (`prod./feat./(…)`), im selben Verzeichnis, `O_EXCL`-Claim, Byte-Inhalt verifiziert. DRY-RUN gegen Produktion: 7 sichere Renames (Producer-Credits aus Dateinamen), 7 korrekt als SKIPPED (Tag-Tippfehler / nicht-triviale Abweichung). 2026-09-04 gegen Produktion ausgeführt: 7/7 SUCCESS (Producer-Credits aus Dateinamen), `FILENAME_TITLE_MISMATCH 14→7`, keine neuen Issues. **Cover-Executor (erledigt):** `apply_cover_repairs()` für `ARTWORK_MISSING/INVALID/LOW_RESOLUTION/NON_SQUARE` — `CoverProcessor.get_cover_art()` + reine `cover_repairs.decide_cover_action()` (only-if-better: ≥400px, quadratisch, +200px bei LOW_RES, kein Auflösungsverlust bei NON_SQUARE), sonst SKIP (vorhandenes Cover nie durch gleich gutes ersetzt). Backup + covr-Atom auf temp + Cover-SHA-/Audio-Verifikation. `--level COVER`/`--issue ARTWORK_*` (nie im Default-`--apply`). Album-Cache (eine Suche pro Album-Ordner → verhindert selbstverursachte ALBUM_COVER_INCONSISTENT). 2026-09-04 ausgeführt: makko-Album 6× 300→3000px; `ALBUM_COVER_INCONSISTENT`-Executor (offline: alle Album-Tracks auf das beste vorhandene Album-Cover, nie Downscale) → **198 SUCCESS**, `ALBUM_COVER_INCONSISTENT 19→0`, `ARTWORK_MISSING 5→1`, `LOW_RESOLUTION 13→3`, `NON_SQUARE 11→2`, **Health 97.9→98.0**. Übrige ARTWORK-Fälle (Singles/Bootlegs ohne besseres Cover) SKIPPED. **L3 MusicBrainz-IDs (erledigt):** `apply_external_metadata()` — `MusicBrainzClient.fetch_metadata()` (Eindeutigkeit prüft der Client) + reine `external_metadata.plan_id_writes()` (`title_is_trustworthy()` blockt unsaubere Titel, MB-Titel muss zum Datei-Titel passen, UUID-/ISRC-Formatcheck, nur FEHLENDE Felder). `--level EXTERNAL_METADATA`/`--issue META_MB_*`. DRY-RUN 01099: 6 Nachträge (Recording/Release-ID), 10 kein sicherer Match; Produktionslauf 2026-09-04: 0 sichere Treffer (Deutschrap/2Pac-Bootlegs kaum in MusicBrainz — erwartetes Verhalten des Eindeutigkeits-Guards, kein Bug). **Merge-Status:** PR #145 (2026-09-04) hat per Squash nur Planner + L1-Tag-Fixes gemergt; L1-Rename, Cover, ALBUM_COVER_INCONSISTENT und L3 (Commits `81277b0`..`742640f`) wurden per PR #146 auf `main` nachgezogen — Rebase konfliktfrei, volle Suite 2421 passed / 1 skipped / 0 Regressionen. **ALBUM_COVER_INCONSISTENT abgeschlossen:** nach PR #146 im Produktions-Report 0 verbleibende Fälle (Executor + Album-Cache verhindern Neuentstehung). **`META_TITLE_NOT_CLEAN` (PR #147):** neuer read-only Health-Check über `utils/title_cleanup.py::light_title_cleanup` (aus `TitleCleaner` ausgelagert, damit der Scanner `services/metadata/` nicht in den Import-Graph zieht) — erkennt Titel-Tags mit Parsing-/Formatierungsresten (Anführungszeichen, `prod.`-Credit, Marketing-Suffix), Severity WARNING, Planner-Mapping → METADATA_REPROCESSING. Realer Bestand: 19 Fälle (alle echt), Score 98.0→97.8. **L2-Executor (PR #148, erledigt):** Option 2a — der ~925-Zeilen-Kern (`process_file()` + `snapshot()` + alle Helfer) aus `scripts/reprocess_artist_metadata.py` liegt jetzt in `services/metadata/track_reprocessor.py` (verhaltensgleich, die importlib-Tests `test_reprocess_artist_metadata*` sind die Charakterisierung; Script behält `ALLOWED_ROOT=/tmp/musicbot_test` + Path-Safety + ReprocessLogger, re-importiert den Kern; Telegram-Menü unberührt). Neuer `apply_level2(reprocess)` in `executor.py`: ein `process_file`-Lauf pro Datei über die echte `config.Config`-Pipeline, Per-Datei-Backup ausserhalb der Library VOR dem in-place-Write, verbindliche Audio-Essenz-Prüfung danach, Rollback (inkl. Rename-Rücknahme) bei Abweichung/Pipeline-Fehler/gemeldeter Audio-Änderung, `unresolved`-Durchreichung, Auto-Learn-Mapping-Update als bewusster Nebeneffekt (CLI-Hinweis im EXECUTE-Modus). `--level METADATA_REPROCESSING` / `--issue META_TITLE_NOT_CLEAN` etc., nie im Default-`--apply`. **Produktionslauf 2026-09-04 (`--artist makko --issue META_TITLE_NOT_CLEAN`):** 19/19 SUCCESS, Audio byte-identisch; Titel `"X"`/`"X" prod. Y` → `X`, 2 Renames, 1 Rename korrekt blockiert (`/` im Titel → unresolved), 7 Dateien MB-IDs ergänzt, 1× Lyrics; `META_TITLE_NOT_CLEAN 19→0`, `LYRICS_MISSING 12→11`, Health 97.8→98.0. **Betriebs-Lehre:** `process_file` tauscht Cover, sobald die Pipeline ein abweichendes liefert (nicht „only-if-better") → bei Teil-Album-L2-Läufen entsteht `ALBUM_COVER_INCONSISTENT`; Verification-Scan fängt es (Exit 1), Nacharbeit = `--issue ALBUM_COVER_INCONSISTENT --apply` (hier 4/4 SUCCESS, `2→0`). **Loudness (PR #151, erledigt):** (1) neuer read-only Health-Check `LOUDNESS_OFF_TARGET` (Severity INFO) — nur bei `library_health_check.py --measure-loudness` (FFmpeg-loudnorm-Analyse `-f null -`, kein Re-Encode; `tag_reader.measure_loudness`); Melde-Schwelle > 2 dB von −16 LUFS, **berücksichtigt einen vorhandenen `replaygain_track_gain`** (`effective = gemessen + gain`) → setzt einen RG-fähigen Player voraus (Navidrome). (2) **Nutzer-Entscheidung 2026-09-04: kein Re-Encode** — die Download-Pipeline (`enhanced_metadata_processor.py` Schritt 15b) normalisiert frische Downloads bereits per loudnorm und läuft konstant. Neuer `apply_replaygain(measure_fn)` in `executor.py` schreibt stattdessen **verlustfrei** einen `replaygain_track_gain`-/`_peak`-Freeform-Tag — bzw. **entfernt** einen bereits vorhandenen, jetzt irreführenden RG-Tag (`compute_replaygain` → SET/CLEAR/None, berücksichtigt den bestehenden Gain). Ziel −16 LUFS — bewusst nicht die RG-2.0-Norm −18, damit getaggte Altbestände in Navidrome so laut klingen wie die ungetaggten Neu-Downloads). Sicherheitsmodell wie L1: `safety_check`, Backup ausserhalb Library, temp-Sibling + Verifikation (Atome gesetzt **und Audio-Essenz-MD5 byte-identisch**), atomarer replace, Rollback, Verification-Scan mit `--measure-loudness`. `--level LOUDNESS`/`--issue LOUDNESS_OFF_TARGET`, nie im Default-`--apply`. Der vollständige Re-Encode-Executor wurde bewusst nicht gebaut; `scripts/normalize_test_library_loudness.py` bleibt test-only. Planner: `LOUDNESS_TAG_MISSING` → NOT_REPAIRABLE, `LOUDNESS_TAG_INVALID/_PARTIAL` → MANUAL_REVIEW. **Realer Bestand 2026-09-04 (`--measure-loudness`):** 133/388 off-target — 99 SET (zu laut, Median +5,2 dB, bis +7,9; teils clippend: makko 89 / Levin Liam 8 / 2Pac 2) + 34 CLEAR (Badchieff 33 + 2Pac 1: Datei schon bei −16, aber stale RG-Tag aus früherer Quelle). **Produktionslauf 2026-09-04 durchgeführt: 133/133 SUCCESS, 0 failed, `LOUDNESS_OFF_TARGET 133→0`, Health 98.0, Verification-Scan grün (keine neuen/gestiegenen Issues) — Loudness damit abgeschlossen.** Produktionslauf = eigener Freigabe-Schritt. **Duplicate (erledigt):** `library_repair.py --allow-delete --artist <X>` dockt als Subprozess an das bereits gehärtete `scripts/resolve_duplicates.py` an — **kein neuer Lösch-Code** (Klassifikation/Resolution-Matrix/Zwei-Stufen-Execute-Sicherheit dort unverändert, `docs/MusicBot_DUPLICATE_RESOLUTION_ARCHITECTURE.md`). Läuft unabhängig vom Health-Report (`resolve_duplicates.py` hat seine eigene Duplicate-Erkennung). Erfordert `--artist` (nie der gesamte Root, spiegelt dessen eigene `--confirm-production-execute`-Zusatzsperre). **Neu: Backup-vor-Delete** — `services/duplicate/execution.py::execute_group(backup_fn=…)`, optionaler DI-Parameter (wie die bestehenden `validate_file_within_root`/`build_candidate_from_path`); ohne `backup_fn` exakt unverändertes Verhalten (Charakterisierung: alle bestehenden Tests weiterhin grün, die frühere Architekturentscheidung „kein Rollback-Versprechen" bleibt für Aufrufer ohne `backup_fn` in Kraft). Mit `backup_fn`: Kopie VOR jedem `unlink()`, schlägt sie fehl → kein Delete. `resolve_duplicates.py` bekommt `--backup-dir` (Default `<root>/../.library_repair_backups`) + `backup_path` im Audit-Log. **Read-only Dry-Run gegen Produktion 2026-09-04** (`--path /mnt/musik_bilder/library`, ohne Code-Änderung bereits vorher production-fähig): 388 Dateien, 2 Duplicate-Gruppen, 0 auto-resolvable, beide korrekt `MANUAL_REVIEW` (2Pac „Changes" Album- vs. Single-Edit Δ11,3s; makko „Nachts wach" zwei Tracks im selben Remix-EP-Ordner, `album_context_risk: HIGH`) — kein `--execute`-Produktionslauf angezeigt, da keine sicher auflösbaren Duplikate vorhanden. Tests +9. Volle Suite: 2499 passed, 1 skipped, 0 Regressionen. **Phase 2 damit vollständig abgeschlossen.** | `docs/LIBRARY_REPAIR.md`; Prompt `docs/prompts/Claude-Code-Production-Prompt v2.md` |
| — (`enhanced_metadata_processor.py` Schritt 15b — LUFS-Ziel der Download-Pipeline verifizieren) | CLOSED (2026-09-09, PR #188) | war P3 | Bei der Download-Pipeline-Performance-Analyse (Log Akon „Smack That") bestätigt: Schritt 15b lief bis dahin als **voller AAC→AAC-Re-Encode** (`AudioEnhancer.normalize_loudness()`, 2 FFmpeg-Passes mit `-c:a aac -b:a 192k`, ~22 s = ~49 % der Pipeline, gemessen) — funktional auf −16 LUFS zielend, aber mit Generationsverlust und ohne RG-Tag. Ersetzt durch `services/metadata/loudness_replaygain.py::apply_replaygain_tags()`: EBU-R128-Scan (rsgain ~1,5 s, Fallback FFmpeg-Analyse) + verlustfreier `replaygain_track_gain`/`_peak`-Tag (lowercase, Format deckungsgleich zu `library_repair/replaygain_repairs.py`), Audio byte-identisch. Deckt sich mit der Nutzer-Entscheidung 2026-09-04 für den Repair-Pfad (`docs/LIBRARY_REPAIR.md` §6b). `normalize_loudness()` bleibt für `scripts/normalize_test_library_loudness.py` (test-only). | `tests/test_loudness_replaygain.py`; PR #188 |
| — (Library Health Scanner, Phase 1) | CLOSED (2026-09-04, PR #145) | war P2 | Neue Phase „Music Library Health Scanner" (`scripts/library_health_check.py` / `services/library_health/`), read-only, inkrementell in 3 Teilen auf einem Branch. **Discovery + Per-Datei-Analyse** (Metadata/Genre/Artwork/Lyrics/Audio/Loudness-**Tag**/Multi-Artist/Struktur/Dateiname; strikte `AnalysisState`s; Observation≠Defect: Loudness-Tag/MB-IDs/Lyrics = INFO). **Group-Analyse:** Album-Konsistenz (Track-Lücken, Dublette-Tracknr pro Disc, abweichende Album-Name/-Artist/Jahr/Genre/Release-ID/Cover), Artist-Konsistenz (Verzeichnis↔dominanter Tag, Schreibvarianten), Duplicate-Gruppen (EXACT via SHA256 mit Größen-Vorfilter, RECORDING via MB-Recording-ID/ISRC, SUSPECTED via `services/duplicate/classification.py` — DUP-03: Remix/Live getrennt, nur Observation; `resolution.py`/`execution.py` nicht importiert). **Deterministischer Health-Score** (`file_/album_/artist_/library_health_score` 0–100; feste Severity-Gewichte CRITICAL −40 / ERROR −15 / WARNING −4 / INFO 0; Library-Abzug für Duplicate-Gruppen bei −15 gedeckelt; Status-Bänder; `health.weights` im Report). Read-only technisch bewiesen: SHA256/mtime/size/Pfade vorher==nachher (`run_scan` + CLI-Subprozess, inkl. defekter Datei), Import-Graph frei von TagWriter/FilenameFixer/AudioEnhancer/EnhancedMetadataProcessor/duplicate.execution, kein `ArtistNormalizer` (schreibt beim Library-Scan). 99 neue Tests (`tests/test_library_health_*.py`), volle Suite 2326 passed / 1 skipped / 0 Regressionen. **Phase-1-Finalaudit gegen die Produktions-Library** (388 Dateien): (a) `MULTI_ARTIST_DUPLICATE` verglich `©ART` gegen das `ARTISTS`-Freeform-Atom, deren Ueberlappung `tag_writer.py` bewusst erzeugt — 54 FP bei Solo-Artists, jetzt nur noch feld-intern. (b) Kalibrierung am realen Bestand: `ARTWORK_NON_SQUARE` mit 5%-Toleranz (1416x1407 ist praktisch quadratisch), `ARTWORK_LOW_RESOLUTION` Schwelle 500->400 + INFO, `AUDIO_VERY_SHORT` 30->20s + INFO + Skit/Intro-Titelunterdrueckung, `GENRE_INVALID` -> INFO (Tag-Hygiene, kein Defekt; „Alternative Hip Hop"/„Indie-Pop" sind KORREKT als nicht-konventionskonform erkannt — nur die Severity war unverhaeltnismaessig), `ALBUM_COVER_INCONSISTENT` nur noch bei Cover-Abmessungs-Unterschieden (25->19), Compilation-Erkennung (`_is_compilation_like`) stuft `ALBUM_TRACK_GAP` herab und unterdrueckt `ALBUM_RELEASE_ID_INCONSISTENT`. Score danach 97.9/EXCELLENT (vorher 96.8) — die verbleibenden 1 ERROR (Levin Liam doppelte Tracknr 1) + 13 WARNING sind echt. Regenerierter Report: `cache/data/library_health_report.{json,txt}`. Nächste Phasen (Navidrome-Abgleich, Smart-Repair-Planner) bewusst NICHT Teil davon. | `docs/LIBRARY_HEALTH.md`; PR #145; Prompt `docs/prompts/Claude-Code-Production-Prompt v2.md` |
| — (Phase 3 „Music Quality & Library Intelligence" — Musicbot.md-Ideen gegen IST-Zustand geprüft, Kandidaten umgesetzt) | CLOSED (2026-09-07, PR #157–#161) | war P1/P2 | Research + Research-Review gegen `Musicbot.md` (15 Ideen klassifiziert: ALREADY/PARTIALLY IMPLEMENTED, SUPERSEDED, STILL VALID, NOT RECOMMENDED — Audio-Fingerprint als bereits im Anti-Overengineering-Gate verworfen erkannt, nicht als offener Kandidat). Planning + Umsetzung: **P1.1 Library Statistics** (neue Telegram-Ansicht Tracks/Albums/Artists/Genre-Verteilung/Health-Score, `genre_distribution` in `services/library_health/report.py`). **P1.2 Navidrome-Scan-Automation** (`scripts/library_repair.py` löst nach `--apply` mit echtem `SUCCESS` automatisch einen Navidrome-Scan aus, wiederverwendet die bestehende `tally`-SUCCESS-Semantik unverändert, `--no-navidrome-scan`-Opt-out). **P1.3 MusicBot Doctor** (neuer Admin-only Telegram-Menüpunkt, `services/library_repair/doctor_runner.py` als reine Subprozess-Orchestrierung um `library_health_check.py`/`library_repair.py --level SAFE_AUTOMATIC --apply`, Muster von `reprocessing_runner.py`). **P2.2 Import History** (`DownloadHistoryEntry` um 5 dreiwertige `Optional[bool]`-Checklisten-Felder erweitert, „unbekannt" wird nie als `False` gespeichert). **P2.3 Bad Download Detector Stufe A** (neues, von `services/library_health` bewusst entkoppeltes `services/downloader/download_quality_guard.py`, rein beobachtend/Logging, kein Reject). **P2.1 Audio Quality Analyzer** und **P3.1 Cover-Dateigrößen-Plausibilität** nach Schritt-0-Kalibrierung gegen die reale 388-Datei-Produktionslibrary explizit NICHT umgesetzt (kein Bedarf belegt — 100 % Stereo ohne Varianz bzw. beide B/px-Ausreißer bereits anderweitig erfasst oder legitim). Jeweils eigener PR pro Einheit (kein Sammel-PR), volle Suite nach Merge aller 5 PRs: 2573 passed, 1 skipped, 0 Regressionen. **Nachtrag (PR #163, 2026-09-07):** Health-Scan-Ausgabe des Doctor-Menüpunkts benutzerfreundlicher gestaltet (Issue-Codes → Klartext aus `services/library_health/issues.py::REGISTRY`, Trennung Score-relevant/reine Beobachtung) — volle Suite danach 2580 passed, 1 skipped, 0 Regressionen. Vollständige Doku: `docs/MusicBot_PHASE3_MUSIC_QUALITY_LIBRARY_INTELLIGENCE.md`. | `docs/MusicBot_PHASE3_MUSIC_QUALITY_LIBRARY_INTELLIGENCE.md`; `docs/LIBRARY_HEALTH.md`; `docs/LIBRARY_REPAIR.md` §8/§9; PR #157–#161, #163 |
| — (P2.3 Stufe B — Bad Download Detector Reject-Gate) | OPEN (DEFER) | P2 | Stufe A (reine Beobachtung/Logging, siehe Zeile oben) ist produktiv; ein echter Reject-Gate (Datei vor Metadaten-Pipeline verwerfen) ist bewusst NICHT Teil davon. Erfordert zuerst eine reale Beobachtungsperiode mit den jetzt aktiven Log-Hinweisen aus `services/downloader/download_quality_guard.py`, daraus abgeleitete (nicht aus den Health-Scanner-Werten übernommene) Reject-Schwellen, und ein separates, explizites Nutzer-Go — noch nicht begonnen. | `services/downloader/download_quality_guard.py` (Modul-Docstring) |
| — (Metadata Confidence Score — Wiederverwendung vs. separater Score) | OPEN (DEFER) | — | Aus Phase-3-Planning zurückgestellte Entscheidung: bestehenden `file_/album_/artist_/library_health_score` (`services/library_health/scoring.py`) für eine „Confidence"-Ansicht wiederverwenden (Option A, kein neuer Code) vs. einen separaten, feld-gewichteten Score bauen (Option B) — Letzteres widerspräche der bewussten Phase-1-Entscheidung, `META_MB_*_MISSING`/`LYRICS_MISSING` als INFO (0 Strafe) statt als Defekt zu werten, und würde für dieselbe Datei zwei unterschiedliche Zahlen erzeugen. Keine Implementierung ohne diese Entscheidung. | Phase-3-Plan (Abschnitt „Metadata Confidence Score — Entscheidungsanalyse") |
| — (Library Closure Phase — Review-Vokabular, ID-CLI, Per-Repair-Verification, Coverage-Matrix) | CLOSED (2026-09-09, Branch `feat/library-closure-cp1`) | war P1/P2 | Read-only-Analyse → 7 identifizierte Lücken in 7 PRs geschlossen. **PR A:** `disposition_for_level()`/`disposition_for_code()` in `planner.py` (Roll-up der 8 `RepairLevel` auf `AUTO_REPAIR`/`MANUAL_REVIEW`/`UNREPAIRABLE`), `docs/audits/LIBRARY_CLOSURE_COVERAGE_MATRIX_2026-09-09.md`, `test_library_repair_disposition_matrix.py` (11) — 53 Codes: 29/22/2. **PR B:** `accept_finding`/`unaccept_finding`/`get_accepted_findings`/`get_review_summary` + `ReviewSummary`-Dataclass in `findings.py`. „⚪ Akzeptiert" == bestehendes `FALSE_POSITIVE` (Nutzerentscheidung, kein neuer Status); `unaccept_finding` ist die einzige neue Fähigkeit (Reaktivierung → `OPEN`, vom Scan-Merge getrennt). **PR C:** `scripts/library_health_review.py` `--accept`/`--unaccept`/`--accepted`/`--show`/`--summary` additiv, interaktiver Kategorie-Review bleibt Default. **PR D:** `report.py` Labels 🔴 Offen / 🟢 Repariert / ⚪ Akzeptiert (abwärtskompatibel). **PR E:** `_verify_issue_resolved()` in `executor.py` — nach jedem echten `SUCCESS` bei `apply_level1`/`apply_level1_rename`/`apply_cover_repairs`/`apply_external_metadata` eine frische Einzeldatei-Health-Analyse; Befund weiterhin da → `ExecOutcome.status = "UNRESOLVED"` (Journal ebenso). Album-scope + `LOUDNESS_OFF_TARGET` + L2 bewusst ausgenommen (im Code + Coverage-Matrix begründet); `repair_service` markiert ein UNRESOLVED-Finding über sein Rescan-Gate weiterhin nicht als RESOLVED. `scripts/library_repair.py`: UNRESOLVED in Tally, „geschrieben" = SUCCESS ∪ UNRESOLVED für Navidrome-Auto-Scan/`touched`. **PR F:** §34-Lifecycle-Tests, §35-Idempotenz (`apply_level1`/`_rename`, scan→repair→scan→repair), §41-End-to-End-Closure (`test_repair_integration.py`, `auto-repairable remaining == 0`). **PR G:** Doku (`LIBRARY_HEALTH.md` §1a, `LIBRARY_REPAIR.md` §5b, `INDEX.md`, v10-DRAFT, dieser Index) + Closure-Audit. **Zurückgestellt:** Telegram-UI für Accepted-Ansicht/Unaccept (Core-API steht, siehe nächste Zeile). 674 thematische Tests grün / 0 Regressionen; **volle Suite durch den Nutzer**. | `docs/audits/LIBRARY_CLOSURE_AUDIT_2026-09-09.md`; `docs/audits/LIBRARY_CLOSURE_COVERAGE_MATRIX_2026-09-09.md` |
| — (Telegram-UI für ⚪ Accepted-Findings-Ansicht + Unaccept) | CLOSED (2026-09-09, Branch `feat/library-review-accepted-telegram`) | war P2 | Direkt nach der Library-Closure-Phase umgesetzt (nach CLI-Test gegen die echte Registry durch den Nutzer). `handlers/library_health_review_handler.py` um `handle_accepted_list`/`handle_accepted_category`/`handle_accepted_show`/`handle_unaccept` erweitert; `rich_menu_system.py`-Review-Dispatcher um `review:accepted`/`review:acccode:<code>`/`review:accshow:<fid>`/`review:unaccept:<fid>`. Übersicht zeigt „⚪ Akzeptierte Findings (N)" nur wenn welche vorhanden → Liste nach Issue-Code → Detail mit Grund/Scan-Status → „↩️ Reaktivieren" (`unaccept_finding()` → OPEN, Stale-Revalidierung wie bei allen Statusänderungen). Rein lesend bis zum expliziten Tap; stale (`present_in_latest_scan==False`) mit ⚠️ markiert. Kein neues Menüsystem, gleiche `FindingsRegistry`-API + Admin-Gating (Defense-in-Depth) wie der Rest des Handlers. Tests: `test_library_health_review_handler.py` +11 (TestAcceptedFindings), `test_rich_menu_review.py` +8 (Dispatch/Gating). | `handlers/library_health_review_handler.py`; `docs/LIBRARY_HEALTH.md` §1a |
| — (Library Repair Production Audit — reale Findings zeigten `FILENAME_TITLE_MISMATCH` als SKIPPED trotz „actionable" und `LYRICS_MISSING` als SUCCESS trotz nicht gefundener Lyrics) | CLOSED (2026-09-08, PR #172–#174) | war P1/P2/P3 | Vollständiger read-only End-to-End-Audit des Repair-Stacks (Finding → Planner → Executor → `track_reprocessor.process_file()` → Verification), ausgelöst durch zwei reale Produktionsbefunde. Kein P0-Fund (Audio-Essenz überall geschützt, keine Verzeichnisüberschreitung, kein Safety-Gate umgangen). **P1 (PR #172):** Root Cause des `LYRICS_MISSING`-Befunds behoben — `apply_level2()` band `SUCCESS` bisher an „irgendein Feld der Datei hat sich geändert" statt an das für den jeweiligen Issue-Code relevante Zielfeld (`process_file()` läuft immer als volle Pipeline, ändert oft mehrere Felder gleichzeitig); jetzt ein `ExecOutcome` pro Issue-Code mit korrekter Zielfeld-Bindung. Vier strukturell tote `EXTERNAL_METADATA`-Codes aufgelöst: `META_GENRE_MISSING`/`GENRE_EMPTY` → `METADATA_REPROCESSING` (GenreProcessor läuft dort bereits identisch zu `GENRE_INVALID`), `META_YEAR_MISSING`/`ALBUM_RELEASE_ID_INCONSISTENT` → `MANUAL_REVIEW` (keine Jahr-Fetch-Implementierung vorhanden bzw. würde bereits vorhandene Werte überschreiben müssen) — unblockt 14 real offene `ALBUM_RELEASE_ID_INCONSISTENT`-Findings. `FILENAME_TITLE_MISMATCH`-Root-Cause (real >85 % Skip-Rate) identifiziert: Planner markiert pauschal `SAFE_AUTOMATIC`/keine Freigabe nötig, obwohl `rename_repairs.py` bewusst nur additive Abweichungen sicher auflöst — Executor-Logik korrekt konservativ belassen, `expected_change`-Text stattdessen präzisiert. **P2 (PR #173):** irreführendes `reuses_component="reprocess_artist_metadata.py"`-Label bereinigt (das Skript ist entgegen der ursprünglichen Auftragsannahme NICHT Teil des automatisierten Pfads — `apply_level2()` ruft `track_reprocessor.process_file()` direkt in-process auf, das Skript ist ein eigenständiges, auf `/tmp/musicbot_test` beschränktes CLI-Testwerkzeug); Tests für externe Service-Fehler und Batch-/Mixed-Outcomes ergänzt; doppelte Rename-Safety-Logik (`rename_repairs.py` vs. `track_reprocessor.py`) untersucht und bewusst NICHT extrahiert (beide Prüfungen bereits trivial, Extraktion würde nur Kopplung zwischen unabhängigen Paketen einführen — Cross-Reference-Kommentare stattdessen); Verification-Asymmetrie CLI/Telegram angeglichen (`execute_safe_automatic_repair()` erkennt jetzt Regressionen: Issue-Codes, die nicht berührt wurden, aber nach dem Scan mehr offene Findings zeigen). **P3 (PR #174):** `docs/LIBRARY_REPAIR.md` an den neuen Planner-Stand angepasst; dedizierte Testfiles für `track_reprocessor.py` (29 Tests) und `journal.py` (9 Tests) ergänzt (bisher nur indirekt mitgetestet); Entscheidung dokumentiert, Lyrics-Volltext/Cover-Binärdaten bewusst NICHT im Journal zu speichern (Copyright-/Speicherplatz-Risiko ohne Nutzen). Insgesamt 55 neue Tests über alle drei Phasen. **Closure-Audit (2026-09-08):** unabhängig neu verifiziert — volle Suite frisch ausgeführt (2865 passed/1 skipped/0 failed, Zuwachs exakt erklärbar), frischer Produktions-Health-Scan (387 Dateien, Health 98.0) + `plan_repairs()` direkt gegen die frischen Findings bestätigt korrekte Reklassifizierung (`ALBUM_RELEASE_ID_INCONSISTENT` jetzt `MANUAL_REVIEW`, 15 reale Findings), Journal-Kreuzabgleich ohne durch den Audit verursachte Aktivität. Verdikt: 🟢 APPROVED. | `docs/audits/LIBRARY_REPAIR_P1_P2_P3_CLOSURE_AUDIT_2026-09-08.md`; `docs/LIBRARY_REPAIR.md` §3/§4/§6a; PR #172–#174 |
| — (`utils/title_cleanup.py::light_title_cleanup()`, deutscher „(MIT Artist)"-Feature-Credit bleibt im Titel-Tag) | CLOSED (2026-09-08, PR #177 Commit `f510365`, nachdokumentiert PR #178) | war P2 | In PR #177 als Teil-Commit `fix(title-cleanup): handle German feature credits` mitgelaufen, ohne eigene Findings-Zeile. Muster wie „IMMER (MIT MAKKO)" (deutsche Schreibweise des Feature-Credits am Titelende) wurde von keiner bestehenden Regel in `light_title_cleanup()` — dem einzigen produktiv erreichbaren Titel-Cleanup-Pfad — erfasst. Fix: neue Regel `\s*\(\s*mit\s+[^()]+\s*\)\s*$` (case-insensitiv, nur ein abschließender Klammer-Block; normale „mit"-Vorkommen im Titel bleiben unangetastet, andere Klammerinhalte wie „(Live)" ebenfalls). Regressionstest lag PR #177 bei. | `tests/test_title_cleaner_german_feature_credit.py` |
| — (`services/library_health/file_analysis.py::_normalize_for_compare()`, `FILENAME_TITLE_MISMATCH`-False-Positives bei harmlosen Schreibvarianten) | CLOSED (2026-09-08, PR #177 Commit `9ec7699`, nachdokumentiert PR #178) | war P2 | In PR #177 als `test(library-health): …`-betitelter Commit mitgelaufen, obwohl er `_normalize_for_compare()` (Dateiname↔Titel-Tag-Vergleich des Health-Scanners) real verändert. Vorher: NFD/NFC-unterschiedliche, aber gleiche Unicode-Titel sowie reine Formatierungsvarianten (`More Love feat. Okfella` ↔ `More Love (feat. Okfella)`, `Souvenir Industrie` ↔ `Souvenir (Industrie)`) wurden fälschlich als Mismatch gemeldet. Fix: `unicodedata.normalize("NFC", …)` + `casefold()` statt `lower()` + Angleichung von `(feat. …)`-Klammern und rein formatierenden Klammern (nur wenn kein semantischer Marker wie „Remix"/„Live"/„Version" — diese bleiben unterscheidend). Echte Titelabweichungen (Tippfehler, Zusätze) bleiben Mismatch. Read-only-Charakter des Scanners unberührt. Regressionstests lagen PR #177 bei (`test_library_health_file_analysis.py`, erweitert). | `tests/test_library_health_file_analysis.py` |
| — (`services/library_repair/executor.py::apply_level2()`, `requested_issue` nur im DRY-RUN durchgereicht → Vorschau ≠ Execute) | CLOSED (2026-09-08, PR #178) | war P2 | PR #177 Commit `8248441` führte `process_file(requested_issue=…)` ein: ein issue-spezifischer L2-Repair (`LYRICS_MISSING`/`GENRE_INVALID`) soll bestehende Artist-Tags NICHT nebenbei über den `ArtistNormalizer` umschreiben und kein „ReplayGain/Loudness fehlt"-UNRESOLVED anhängen. **Beim Schreiben des Regressionstests (PR #178) entdeckt:** `apply_level2()` reichte `requested_issue` nur im DRY-RUN-Zweig durch — der EXECUTE-Zweig rief weiterhin `reprocess(path, artist_root, False)` ohne den Hint auf. Folge: die Vorschau zeigte weniger Änderungen/UNRESOLVED als der tatsächliche `--apply`-Lauf dann schrieb (dieselbe Preview-≠-Execute-Klasse wie die in PR #173 behobene CLI/Telegram-Verification-Asymmetrie). Pre-Fix-Diskriminierung: der neue Test schlägt gegen den PR-#177-Stand fehl (©ART wird trotz `LYRICS_MISSING` groß­geschrieben). Fix: EXECUTE-Zweig reicht denselben `requested_issue=codes[0] if len(codes) == 1 else None` durch. 3 lokale Reprocess-Stubs in `test_library_repair_executor.py` auf die reale Callable-Signatur (`…, requested_issue=None`) nachgezogen. | `tests/test_library_repair_executor_l2_real_pipeline.py::test_lyrics_repair_does_not_renormalize_existing_artist_tag`/`::test_broad_reprocess_still_normalizes_artist_tag` |
| — (`mapping/genre_aliases.yaml`/`genre_hierarchy.yaml`/`case_preserve.yaml`/`known_artists.yaml`, manuelle Ergänzungen ohne Testabdeckung) | CLOSED (2026-09-08, PR #177 Commit `d895834`, nachdokumentiert PR #178) | war P3 | In PR #177 als `chore(mapping): update metadata mappings` mitgelaufen — neben maschinell geschriebenen Auto-Learn-Dateien enthielt der Commit manuelle Fachlogik-Ergänzungen (CLAUDE.md §10/§28) ohne konkrete Beispiele/Test: `genre_aliases.yaml` „new wave"/„new-wave"/„neue welle" → „Pop"; `genre_hierarchy.yaml` „Piano Pop" → Parent „Pop"; `case_preserve.yaml` „lea" → „LEA"; `known_artists.yaml` + LEA, Tom Odell, Unheilig, Xavier Naidoo (aus Live-Testdownloads deutscher Pop-Artists). PR #178: Charakterisierungstest gegen die echten Mapping-Dateien nachgezogen (Input → erwartetes Ergebnis über `GenreMapper`, plus Datei-Integritätsprüfungen). Keine Bewertung, ob die Zuordnungen fachlich optimal sind — nur Schutz gegen versehentliches Zurücksetzen. | `tests/test_mapping_additions_2026_09_08.py` |
| A (`enhanced_metadata_processor.py`, Feature-Artists aus `feat.`/`ft.` im Titel werden verworfen) | CLOSED (2026-09-09, PR #180) | war P1 | Download-Pipeline-Testlauf 2026-09-09 (live, Test-Bot): ein Feature-Artist, der nur über ein `feat.`/`ft.`/`featuring` **im Titel** erscheint („Akon - Smack That ft. Eminem", „Marteria - Verstrahlt feat. Yasha"), landet vom YouTube-Parser korrekt in `youtube_parsed["featuring"]` — der `EnhancedMetadataProcessor` hat diese Liste aber nie gelesen. `feat_artists` kam ausschließlich aus (a) dem, was `determine_best_artist()` aus dem Artist-Teil splittet, und (b) dem `all_artists`-Rest bei >1 Primärartist. `feat.` nach dem Songtitel fiel durch beide Raster → Eminem/Yasha komplett aus `©ART`/`ARTISTS`/Dateiname. Kein Regression aus #176 — Lücke besteht mind. seit v9. Fix: `youtube_parsed["featuring"]` wird nach der bestehenden Bestimmung additiv zusammengeführt (`split_main_and_featuring()`, case-insensitiv dedupliziert gegen `feat_artists`/`final_artist`, kein Podcast). Pre-Fix-Diskriminierung: neuer E2E-Test schlägt gegen den alten Code fehl. | `tests/test_metadata_processor_happy_path.py::test_feat_from_title_reaches_feat_artists_in_tag_write` |
| F (`utils/youtube_parser.py::_extract_features()`, Video-Marker klebt am Feature-Namen) | CLOSED (2026-09-09, PR #182) | war P3→P1 | Download-Pipeline-Testlauf 2026-09-09: ein Video-/Marketing-Marker **hinter** dem Feature-Namen („Marteria - Verstrahlt feat. Yasha **(Offizielles Musikvideo)**") landete komplett im Artist-Namen → `featuring == ['Yasha Offizielles Musikvideo']`. Ursache: das klammerlose Feature-Pattern fängt `(.+?)$` bis Stringende inkl. eines nachfolgenden `(...)`-Blocks; die Bereinigung entfernte nur die Klammer-Zeichen, nicht den Marker-Text. Der Titel selbst wird bereits korrekt bereinigt. Durch PR #180 (Finding A) von P3 auf P1 gehoben — vorher wurde der Wert verworfen, seither landet er sichtbar im `ARTISTS`-Tag (live bestätigt). Fix: `feat_raw` wird ab der ersten öffnenden Klammer abgeschnitten. Pre-Fix-Diskriminierung: 4/8 neue Tests schlagen gegen den alten Code fehl. | `tests/test_youtube_parser_feat_video_marker.py` |
| C (`utils/artist_map.py::_standard_normalization()` RULE 2, 8-Zeichen-All-Caps-Name als Akronym gelernt) | CLOSED (2026-09-09, PR #183) | war P2 | Download-Pipeline-Testlauf 2026-09-09: der YouTube-Kanal von Marteria heißt „MARTERIA" (8 Zeichen). RULE 2 klassifizierte jeden All-Caps-String ≤ 8 Zeichen ohne Leerzeichen als Akronym (SDP/SSIO/UFO361), erhielt „MARTERIA" **und persistierte** `marteria → MARTERIA` in `case_preserve.yaml` (`_save_case_preserve_entry()`). Danach normalisierte jedes „Marteria" (auch der saubere Titel-Artist) zu „MARTERIA" → falscher Library-Ordner/©ART-Tag, self-perpetuating über den `ArtistIdentityResolver`-Tier `library_identity`. Fix: Schwelle `<= 8` → `<= 6` (echte Akronyme sind kurz; UFO361=6 bleibt, MARTERIA=8 wird title-cased ohne Persistenz). Bestehende `case_preserve.yaml`-Einträge unverändert. Nebenbefund: 15 bereits persistierte 7+-Zeichen-All-Caps-Einträge in `mapping/case_preserve.yaml` (teils legitim, teils evtl. fälschlich auto-gelernt) — eigene Mapping-Entscheidung, nicht in diesem Fix. Pre-Fix-Diskriminierung: 2/4 neue Tests schlagen gegen den alten Code fehl. | `tests/test_artist_normalizer.py::TestStandardNormalizationRules::test_long_all_caps_word_is_title_cased_not_treated_as_acronym` |
| B (`utils/genre_map.py::determine_genre()`, Fuzzy-Channel-Match zu locker) | CLOSED (2026-09-09, PR #185) | war P1/P2 | Download-Pipeline-Testlauf 2026-09-09: `_find_best_match()` (rapidfuzz `WRatio`, Schwelle **75** für Channels) matchte `channel='Akon'` → `kontor.tv` (77.1, `partial_ratio` auf 4-Zeichen-Query) und `channel='Digster Pop Music'` → `vibe music` (85.5, gemeinsames Wort „music") → falsches `Electronic`-Genre, das als Stufe 1/2 die MB/Last.fm-Kette kurzschließt. Fix: Channel-Fuzzy-Schwelle **75 → 90** (beide Aufrufe). Empirisch verifiziert: echte Namensvarianten (`Kontor.TV` exakt / `kontortv` 94.1 / `<name> official` 90.0) liegen ≥ 90, die Fehltreffer darunter. Artist-Fuzzy bleibt bei 85. Pre-Fix-Diskriminierung: 3/6 neue Tests schlagen gegen den alten Code fehl. | `tests/test_genre_map_channel_fuzzy_false_positive.py` |
| E (`services/clients/musicbrainz_client.py::_build_metadata()`, Compilation als Quell-Release → falsches Jahr/Album) | CLOSED (2026-09-09, PR #196) | war P2 | Download-Pipeline-Log-Analyse 2026-09-09: MusicBrainz liefert die `release-list` einer Aufnahme **unsortiert**. `_build_metadata()` nahm blind `release_list[0]` als Quell-Release für Album, `release_id`, `release_group_id` und (Fallback-)Jahr. Real regelmäßig eine Compilation / ein DJ-Mix statt der Original-Veröffentlichung: Akon „Smack That" → „Bravo Hits 00's" (2021) / „Casino Royale, Volume 2" (2007) statt der „Smack That"-Single (2006-11-13); live falsches Jahr 2009 bzw. Avicii „Levels" → 2022. Fix: neue Auswahl `_select_primary_release()` — Releases, deren Release-Group `secondary-type-list` „Compilation"/„DJ-mix" enthält (oder deren `type`/`primary-type` „Compilation" ist), werden zurückgestellt; unter den verbleibenden (bzw. der ganzen Liste, falls es nur Compilations gibt) gewinnt die **früheste datierte** Veröffentlichung (ISO-Datum lexikografisch). `release_date` fällt zusätzlich auf `first_release["date"]` zurück, wenn weder Recording noch Release-Group ein `first-release-date` tragen. `match["first-release-date"]`/`recording_info`/`release_group["first-release-date"]` bleiben höher priorisiert (Regressions-Guard). Pre-Fix-Diskriminierung via `git stash` bestätigt (3/4 neue Tests schlagen ungefixt fehl). **Folge-Fund (PR #197, Fritz Kalkbrenner – „Sky and Sand", Test-Bot-Re-Download 2026-09-09):** existiert die gewählte Aufnahme in MB **ausschließlich** auf Compilations/DJ-Mixen (kein Single-/Album-Release), war deren Datum trotzdem das Ergebnis-Jahr — real eine 2018er DJ-Mix-Compilation für einen Song von 2009/2010. Fix: bei „nur Compilations" wird `release_date` NICHT mehr aus dem Release-/Release-Group-Datum abgeleitet (`None`), die Pipeline fällt auf das YouTube-/Upload-Jahr zurück; `release_id`/`album` weiterhin vom frühesten Release. Recording-Level-Auswahl in `_get_best_match()` (die 2009er Version ist als „Paul & Fritz Kalkbrenner" gecreditet und verliert gegen die exakt-credit-gleiche 2018er Solo-Version) bleibt ein separater, größerer Schritt. | `tests/test_musicbrainz_client.py::TestSelectPrimaryReleaseFindingE` |
| G (`services/clients/musicbrainz_client.py::fetch_metadata()`, MB-Timeout als „unerwarteter Fehler" geloggt) | CLOSED (2026-09-09, PR #195) | war P3 | Download-Pipeline-Log-Analyse 2026-09-09: das `async with async_timeout.timeout(Config.MUSICBRAINZ_TIMEOUT)` in `fetch_metadata()` feuert bei langsamen MB-Antworten regelmäßig (v. a. bei Playlists mit dem 1-req/s-Limit). Die daraus resultierende `asyncio.TimeoutError` wurde vom generischen `except Exception`-Zweig gefangen und als `[…] 💥 Unerwarteter Fehler` **mit vollem Stacktrace auf ERROR** geloggt — obwohl der Aufrufer (`genre_processor`/`album_processor`) sauber auf Last.fm zurückfällt, also der Normalfall. Bei jedem größeren Download blähten sich die Logs mit Tracebacks auf. Fix: eigener `except asyncio.TimeoutError`-Zweig **vor** dem generischen, Log auf **WARNING ohne exc_info** (`⏱️ MusicBrainz-Timeout nach {N}s – Fallback auf Last.fm`), Rückgabe unverändert `{}`. Pre-Fix-Diskriminierung via `git stash` bestätigt (neuer Test schlägt ungefixt fehl: Log kommt als ERROR + „Unerwarteter Fehler"). | `tests/test_musicbrainz_client.py::TestFetchMetadata::test_timeout_is_logged_as_warning_not_error_and_returns_empty_dict` |
| I (`utils/youtube_parser.py::_clean_title_suffixes()`, Live-/Session-Marker „ @ …" am Songtitel) | CLOSED (2026-09-09, PR #198) | war P3 | Test-Bot-Re-Download 2026-09-09: „Leony - Remedy @ Deluxe Music Session 2022" ergab den Songtitel **„Remedy @ Deluxe Music Session"** (Titel-Tag, Album-Tag, Dateiname). YouTube-Uploads von TV-/Radio-/Live-Session-Auftritten hängen den Session-/Sendungsnamen mit „ @ " an den Songnamen (`@ Deluxe Music Session`, `@ MTV Unplugged`, `@ Rock am Ring`) — nicht Teil des Songtitels. Keine bestehende Regel erfasste das. Fix: `_clean_title_suffixes()` schneidet ein am Titelende stehendes `\s+@\s+.+$` ab (Leerzeichen auf beiden Seiten des `@` — in echten Songtiteln praktisch nie gebraucht), läuft vor der Upload-Jahr-Regel, sodass ein nachgestelltes Jahr mitgeht. Bewusst nur im Erfolgspfad dieser Funktion (nach Artist/Titel-Split), **nicht** in `title_cleaner.light_title_cleanup()`. Pre-Fix-Diskriminierung via `git stash` bestätigt (5/5 neue „entfernt"-Tests schlagen ungefixt fehl, Regressions-Guards grün). | `tests/test_youtube_parser_live_session_suffix.py` |
| H (`services/metadata/genre_processor.py::prioritize_genres()`, Last.fm-User-Tags werden zu Genres) | CLOSED (2026-09-09, PR #187) | war P2 | Durch den B-Fix sichtbar geworden: für „Andreas Bourani – Auf uns" lieferte Last.fm Tags `[pop, german, deutsch, indie, laut mitsing, eingedeutscht, radio, …]`. `prioritize_genres()` filtert `pop`/`indie`/`german` über `IGNORE_SECONDARY` (595 Einträge, u. a. bewusst die zu breiten Top-Genres) heraus → übrig `[laut mitsing, eingedeutscht, radio]` (Last.fm-User-Stimmungs-Tags). Keiner in `GENRE_PRIORITY` → der Fallback-Zweig nahm **blind `valid_tags[0]`** → `©gen: 'Laut Mitsing; Eingedeutscht; Radio'` (+ Auto-Learn). Vorher maskierte der falsche `channel_fuzzy`-Treffer „Electronic" das. Fix: der Fallback-Zweig verlangt jetzt ein **als Genre erkennbares** Tag (in `GENRE_PRIORITY` ODER von `normalize_genre_name()` über einen Alias verändert — ein blosser Title-Case zählt nicht); sonst werden die über `IGNORE_SECONDARY` herausgefilterten **echten** generischen Genres (`pop`/`rock`/`indie`) als Notnagel genutzt, sonst `Unknown`. Charakterisierungstest `test_only_unknown_tags_falls_back_to_first_tag_as_primary` (ARCH-022) fror das alte Verhalten ein → auf das neue Sollverhalten umgestellt. Pre-Fix-Diskriminierung: 3/4 neue/geänderte Tests schlagen gegen den alten Code fehl. | `tests/test_genre_processor.py::TestPrioritizeGenres::test_lastfm_user_tags_do_not_become_genre` |
| — (ARCH-021 Menu-Subsystem, Session-Legacy-State: `max_sessions` ohne Durchsetzung + `MenuSession.state`/`.data`/`.message_id` ungenutzt) | OPEN (DEFER) | P3 | Während der ARCH-021/P-4-Charakterisierung (Session/State-Extraktion nach `handlers/menu/session.py::SessionManager`) entdeckt, bewusst nicht behoben (Move-Only-Prinzip, keine Verhaltensänderung außerhalb des Extraktions-Scopes): (1) `max_sessions`/`MAX_CONCURRENT_SESSIONS` wird gehalten, aber nirgends geprüft — keine Kapazitätsbegrenzung trotz des Namens. (2) `MenuSession.state` (`MenuState`-Enum) bleibt dauerhaft auf Default `IDLE`, wird nie gesetzt/gelesen. (3) `MenuSession.data: Dict[str, Any]` vollständig ungenutzt. (4) `MenuSession.message_id` write-only (in `show_menu()` gesetzt, nirgends gelesen). Alle vier repoweit verifiziert (kein Aufrufer/Leser). Charakterisiert in `tests/test_menu_session_characterization.py::test_max_sessions_is_stored_but_not_enforced`. | `docs/MusicBot_ARCH-021_Menu_Architecture_Migration.md` |
| F1 (`handlers/enhanced_error_handler.py`, zwei unabhängige `EnhancedErrorHandler`-Instanzen) | CLOSED (ARCH-027) | war P2 | ARCH-026-Audit fand: `bot.py` erstellt Instanz A (PTB-registriert via `add_error_handler`), `RichMenuHandler.initialize()` erstellt unabhängig eine zweite Instanz B. Fix (ARCH-027): `RichMenuHandler.__init__()` erhält neuen optionalen Parameter `error_handler=None`; `bot.py` injiziert seine Instanz A via `RichMenuHandler(self.config, error_handler=self.error_handler)`; `initialize()` erzeugt nur noch einen Fallback, wenn `self.error_handler is None` (Standalone-/Test-Konstruktion). Production-Lifecycle hat jetzt genau eine geteilte Instanz. Minimal-invasiv (2 Dateien, additive Parameter, keine bestehende Konstruktionsstelle betroffen). | `docs/MusicBot_ARCH-027_Error_Handler_Consolidation.md` |
| F2 (`handlers/enhanced_error_handler.py::ErrorHandlerAdminInterface`, Monitoring-Blindspot) | CLOSED (ARCH-027) | war P2 | Folge von F1, mit F1 automatisch behoben: `ErrorHandlerAdminInterface` (unverändert die einzige Instanz, gebaut aus `bot.py`s Instanz) sieht jetzt dieselbe Instanz, die auch von allen `handle_callback_error`/`handle_command_error`-Aufrufen aus Navidrome/Duplicate/Backup/Logger/UserMgmt/RichMenuSystem verwendet wird — `/error_stats`, `/error_report`, `/recent_errors`, `erradmin:*` zeigen jetzt den tatsächlichen Zustand. | `docs/MusicBot_ARCH-027_Error_Handler_Consolidation.md` |
| F3 (`handlers/enhanced_error_handler.py::EnhancedErrorHandler.recovery_attempts`, unbegrenztes Wachstum bei Instanz B) | CLOSED (ARCH-027) | war P3 | Folge von F1, mit F1 automatisch behoben: es existiert nur noch eine `recovery_attempts`-Instanz im Production-Lifecycle; `reset_statistics()` (Admin-Befehl) setzt jetzt dieselbe Instanz zurück, die auch tatsächlich befüllt wird. Kein separater, unerreichbarer Zähler mehr. | `docs/MusicBot_ARCH-027_Error_Handler_Consolidation.md` |
| F4 (`klassen/download_handler.py`, keine `EnhancedErrorHandler`-Integration in der P0-Download-Pipeline) | OPEN (DEFER) | P1 | ARCH-026-Audit: die gesamte Datei (1162 Zeilen) hat keine einzige `error_handler`-Referenz. Download läuft als `asyncio.create_task()`-Hintergrund-Task (siehe `handlers/menu/actions/download.py`-Docstring, bewusst wegen fehlendem `concurrent_updates=True`); Exceptions darin landen ausschließlich in `_log_background_download_task_exception()` (reines Logging), erreichen weder Instanz A noch Instanz B — kein zentrales Monitoring/keine Recovery für die P0-Domäne Download. | `docs/MusicBot_ARCH-026_Error_Handler_Integration_Audit.md` |
| F5 (`handlers/enhanced_error_handler.py::handle_async_exceptions`/`handle_sync_exceptions`, totes Decorator-Paar mit latentem Bug) | OPEN (DEFER) | P3 | ARCH-026-Audit: 0 produktive Verwendungen repoweit (auch 0 in Tests). Bei tatsächlicher Auslösung würden beide Decorators `self.config.get("SUPPRESS_HANDLED_EXCEPTIONS", False)` aufrufen — `config.Config` hat keine `.get()`-Methode (reine Attribut-Klasse, kein `__getattr__`) → `AttributeError`, der die eigentliche Exception maskieren würde. Nicht behoben (betrifft ausschließlich toten Code, kein produktiver Aufrufer). | `docs/MusicBot_ARCH-026_Error_Handler_Integration_Audit.md` |
| F6 (4 Handler mit injiziertem, aber ungenutztem `error_handler`) | OPEN (DEFER) | P3 | ARCH-026-Audit: `ReprocessingMenuHandler`, `LibraryDoctorHandler`, `LibraryHealthReviewHandler`, `RepairMusicBotHandler` erhalten die echte Instanz B injiziert, rufen sie aber nie auf. Kein Coverage-Loch (alle vier laufen über `RichMenuSystem.handle_callback()`s zentralen Catch-All, derselben Instanz B), aber irreführende tote Injection. | `docs/MusicBot_ARCH-026_Error_Handler_Integration_Audit.md` |
| F7 (`FamilyChatHandler`/`FamilyChallengeHandler`/`BotRestartHandler`, undokumentierte Nicht-Integration) | OPEN (DEFER) | P3 | ARCH-026-Audit: anders als `StatistikHandler`/`FamilyStatsHandler` (dokumentierter, bewusster Verzicht, siehe Zeile oben) haben diese drei weder `error_handler`-Injection noch einen Docstring-/Findings-Hinweis auf einen bewussten Verzicht. Kein Coverage-Loch (zentraler Catch-All greift), aber inkonsistent zur sonstigen Dokumentationspraxis. | `docs/MusicBot_ARCH-026_Error_Handler_Integration_Audit.md` |
| F8 (`handlers/enhanced_error_handler.py`, doppelte `start_session()`/`end_session()`-Aufrufe) | OPEN (akzeptiert) | P3 | ARCH-026-Audit: jeder High-Level-Entry-Point (`handle_telegram_error`/`handle_command_error`/`handle_callback_error`) sowie beide Decorators rufen `debug_tracker.start_session()`/`end_session()` selbst auf UND übergeben denselben `session_id` an `handle_exception()`, das intern erneut `start_/end_session()` aufruft. `start_session()` überschreibt die bereits angelegte Session (Kontext-/Step-Verlust nur im `DebugTracker`-Objekt, nicht im Exception-Log selbst); der zweite `end_session()`-Aufruf ist ein stiller No-Op (Existenzprüfung). Bestätigt harmlos, keine Korrektur notwendig. | `docs/MusicBot_ARCH-026_Error_Handler_Integration_Audit.md` |
| F11 (`handlers/enhanced_error_handler.py::ExceptionMonitor.categorize_exception`, `OSError` in zwei Kategorien) | OPEN (akzeptiert) | P3 | ARCH-026-Audit: `OSError` ist sowohl in `categories["file_system"]` als auch `categories["network"]` gelistet; `categorize_exception()` gibt den ersten Dict-Treffer zurück → `file_system` gewinnt immer. Betrifft nur den Recovery-Nachrichtentext, kein Crash. Außerhalb des Integrations-Scopes von ARCH-026, nur dokumentiert. | `docs/MusicBot_ARCH-026_Error_Handler_Integration_Audit.md` |
| F12 (`handlers/enhanced_error_handler.py::_extract_update_info`/`_build_full_context`, personenbezogene Daten im Log) | OPEN (akzeptiert) | P3 | ARCH-026-Audit: User-ID/Username/First-Name/Language-Code, Chat-ID/-Titel, Message-Text-Preview (100 Zeichen), Callback-Data sowie Stack-Frame-`locals_keys` (nur Variablennamen) werden unredigiert über den Standard-Logger geloggt. Keine Secrets betroffen (Passwörter/Tokens sind nicht Teil dieser Felder), aber keine Produktions-/Debug-Unterscheidung bei personenbezogenen Feldern. Außerhalb des Integrations-Scopes von ARCH-026, nur dokumentiert. | `docs/MusicBot_ARCH-026_Error_Handler_Integration_Audit.md` |

---

## Format einer Zeile

- **ID**: formale ID falls vorhanden (INV-xx, AE-xx, DUP-xx, DL-xx, MIG-xx, …), sonst „—" mit dem betroffenen Modul/der Methode in Klammern als informeller Bezug.
- **Status**: `OPEN`, `OPEN (DEFER)` (bewusst zurückgestellt, aktiv re-evaluiert), `OPEN (akzeptiert)` (dauerhaft akzeptiertes Risiko, keine erneute Prüfung geplant), `CLOSED (…)` mit Schließungs-Referenz.
- **Prio**: P0–P3 nach CLAUDE.md Abschnitt 23, „—" wenn keine formale Priorität vergeben wurde.
- **Kurzfassung**: ein bis zwei Sätze, genug um die Tragweite einzuschätzen, nicht die volle Begründung.
- **Quelle**: das Dokument mit der vollständigen Analyse/Begründung.
