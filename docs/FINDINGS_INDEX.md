# Findings-Index

Lebendes, fortlaufend gepflegtes Register aller aktuell **offenen/
zurückgestellten** Findings — die einzige Stelle, die man befragen muss,
um zu wissen „ist Finding X offen oder geschlossen, und wo steht die
Begründung", ohne durch mehrere Baseline-Dokumente blättern zu müssen.

**Geltungsbereich:** nur offene/zurückgestellte Punkte (bewusste
Design-Entscheidung, siehe Diskussion 2026-09-02 — kein rückwirkendes
Backfill der kompletten Projekthistorie). Ein Fund kommt hierher, sobald
er entdeckt und zurückgestellt wird; er wird hier auf `CLOSED`
umgestellt (nicht gelöscht) und mit Schließungs-Referenz versehen, sobald
er behoben wird — für die volle historische Begründung bleibt die
verlinkte Quelle (Baseline-Abschnitt, Audit-Dokument, PR) maßgeblich,
dieser Index selbst bleibt bewusst kurz.

**Pflegeregel (Definition of Done, CLAUDE.md Abschnitt 22):** jede
Änderung, die einen hier gelisteten Punkt schließt, einen neuen offenen
Punkt erzeugt, oder eine bestehende Priorität/Einschätzung ändert,
aktualisiert die entsprechende Zeile in diesem Dokument im selben PR.
Die Tech-Debt-Tabelle in jeder `MusicBot_ENGINEERING_BASELINE_vN.md`
bleibt davon unberührt ein eingefrorener Schnappschuss zum jeweiligen
Freeze-Zeitpunkt (wird nach dem Freeze nicht mehr editiert) — dieser
Index ist ab sofort die einzige Stelle für den *aktuellen* Stand.

Stand: 2026-09-02 (Baseline v8).

---

| ID | Status | Prio | Kurzfassung | Quelle |
|---|---|---|---|---|
| INV-01 (`duplicate/cache.py`) | OPEN (DEFER) | P2 | Synchrone Filesystem-Persistenz im Event-Loop-Thread; 3 Lösungsoptionen bewertet, „mass conversion to async" bleibt laut Architecture-Evolution-Gate verboten. | `docs/MusicBot_ENGINEERING_BASELINE_v8.md` §6/8; Vollanalyse: `docs/audits/SERVICES_ARCHITECTURE_AUDIT_2026-09-01.md` §22 |
| — (`download_executor.py`) | OPEN (DEFER) | P2 | Verwaiste Teildatei bei Task-Cancellation in `download_single_track()`; kein akutes Risiko, wird vom 24h-Start-Sweep erfasst. | `docs/MusicBot_ENGINEERING_BASELINE_v8.md` §6/8 |
| — (`mugge_statistik_handler.py`) | CLOSED (won't-fix, 2026-09-02) | — | Kein `error_handler` integriert. Nutzer-Entscheidung final bestätigt: jeder `except`-Block editiert die separat gesendete Zwischennachricht, nicht die `callback_query`-Nachricht — mechanisches Verdrahten würde die falsche Nachricht treffen. Bestehende lokale `except`-Blöcke sind bereits funktional äquivalent. Dokumentiert direkt im Code (Klassen-Docstring). | `handlers/mugge_statistik_handler.py` (Docstring); Historie: `docs/archive/MusicBot_ENGINEERING_BASELINE_v6.md` §8 |
| — (`YoutubeDownloader.download_audio`) | CLOSED (2026-09-02) | war P3 | `AttributeError` bei `download_result=None` behoben — sauberer Guard liefert jetzt `{"success": False, "error": ...}` statt zu crashen. Pre-Fix-Diskriminierung via `git stash` bestätigt. | `tests/test_youtube_downloader_telegram_decoupling.py::test_empty_result_returns_clean_error_dict` |
| — (Downloader-Fehlertaxonomie) | OPEN | — | `FormatNotAvailableError`/`PermissionError` korrekt als „nicht retry-würdig" verdrahtet, aber aktuell von keiner Stelle geworfen (Infrastruktur bereit, ungenutzt). | `docs/MusicBot_ENGINEERING_BASELINE_v8.md` §6/8 |
| — (`FileProcessingError`) | OPEN | — | Nicht in die Non-Retryable-Menge aufgenommen — wird nirgends geworfen, keine Klassifikation ohne Beleg. | `docs/MusicBot_ENGINEERING_BASELINE_v8.md` §6/8 |
| — (14 Delegate-Methoden, `EnhancedMetadataProcessor`) | CLOSED (2026-09-02) | war P3 | Alle 14 entfernt — 0 externe Aufrufer UND 0 interne Selbstaufrufe repoweit erneut verifiziert (Beweispflicht vor Löschung, CLAUDE.md Abschnitt 20), 0 Testreferenzen. `_fetch_album_info_from_musicbrainz()` (ebenfalls 0 Aufrufer, aber außerhalb des benannten Blocks) bewusst nicht mitentfernt — separater, nicht angefragter Fund. | `services/metadata/enhanced_metadata_processor.py` |
| MIG-04 | CLOSED (nicht umgesetzt, 2026-09-02) | war P3 | Vertiefte Prüfung statt Verschiebung: `CoverProcessor` orchestriert 5 externe Quellen + eigenes Scoring/Caching — kein „reiner Client" laut CLAUDE.md-Definition, an seinem jetzigen Ort (`services/metadata/`) korrekt einsortiert; Verschiebung nach `services/clients/` wäre Fehlklassifizierung. `DownloadExecutor` würde strukturell passen (ein externes Tool), aber Verschiebung hätte 0 Funktionsnutzen bei ~6 Importpfad-Änderungen — nicht gerechtfertigt. | `docs/FINDINGS_INDEX.md` (diese Zeile); ursprünglich `docs/audits/SERVICES_ARCHITECTURE_AUDIT_2026-09-01.md` |
| MIG-06 | CLOSED (2026-09-02) | war P3 | Automatisierter Layer-Boundary-Test ergänzt (AST-basiert, `services/` darf nie `handlers`/`klassen`/`telegram` importieren) — Grenze war bereits sauber, ist jetzt dauerhaft gegen Regression abgesichert. | `tests/test_services_layer_boundary.py` |
| DUP-05 | CLOSED (2026-09-02) | war P1 (akzeptiert) | Check-then-Register-Race ohne Lock — behoben durch In-Memory-„in Bearbeitung"-Markierung (URL-/Content-Hash, TTL-basiert selbstheilend). Kein reiner UX-Feinschliff: schließt eine reale Race-Bedingung. | `docs/audits/DUP05_IN_FLIGHT_RACE_FIX_2026-09-02.md` |
| — (`DuplicateCache.duplicate_count`) | OPEN | P3 | Asymmetrie: `check_url_duplicate()` erhöht ohne `_save_caches()`, `check_content_duplicate()` erhöht gar nicht. Kein Korrektheitsrisiko — Wert wird nirgends gelesen/angezeigt (verifiziert). | `docs/audits/P0_DUPLICATE_CACHE_AUDIT_2026-09-02.md` |
| — (`DuplicateCache._normalize_url_for_cache`) | OPEN | P3 | `/embed/<id>`, `/live/<id>` nicht auf dieselbe Video-ID normalisiert wie `/watch?v=<id>` (analog zum P0-F-Shorts-Fix). Keine Evidenz für reale Nutzung als manuell geteilter Link. | `docs/audits/P0_DUPLICATE_CACHE_AUDIT_2026-09-02.md` |
| — (`ArtistConfig`/`ArtistNormalizer`-Verdrahtung, `DuplicateDetector`) | CLOSED (P1, PR #102) | — | War offen seit P0-E — durch P1 vollständig behoben, `DuplicateDetector` nutzt jetzt denselben `ArtistProcessor`-Pfad wie die Metadaten-Pipeline. Zeile bewusst als Beispiel für den CLOSED-Zustand stehen gelassen (siehe Geltungsbereich oben). | `docs/audits/P1_DUPLICATE_DETECTOR_ARTIST_NORMALIZER_WIRING_2026-09-02.md` |
| — (`run_test_bot.py` BOT_TOKEN-Leak) | CLOSED (2026-09-02) | P0 | Security-Sweep (erster systematischer Secrets-in-Logs-Durchgang): `BOT_TOKEN` (URL-Pfad-Auth der Telegram-Bot-API) landete bei Verbindungsfehlern unmaskiert in der Exception-Message. Gefixt (Maskierung + `__main__`-Guard, der die Datei erst sicher testbar machte). Alle anderen geprüften Secrets (`NAVIDROME_PASS` bereits gefixt, `LASTFM_API_KEY`/`_SECRET`/`GENIUS_ACCESS_TOKEN` strukturell nicht betroffen — Header/POST-Body statt URL) unauffällig. | `docs/audits/SECURITY_SECRETS_IN_LOGS_SWEEP_2026-09-02.md` |
| YTPARSE-01 | CLOSED (2026-09-02) | P0 | Live-Fund beim End-to-End-Testdownload über den echten Test-Bot: `_parse_artist_and_title()` in `utils/youtube_parser.py` splittete den generischen Bindestrich-Trenner (`\s*[-–—]\s*`, kein Whitespace zwingend) am erstbesten BAREN Bindestrich — bei „Miksu/Macloud, makko, t-low - Ich will" traf das den Bindestrich in „t-low" selbst statt des echten Artist/Titel-Trenners: Artist-Liste enthielt fälschlich isoliertes „t", Titel begann mit Leak „low - Ich will". Analog zum bereits bestehenden, aber hier fehlenden Schutz in `artist_processor.py::clean_artist_before_normalization()`. Gefixt: Regex verlangt jetzt `\s+` (zwingend Leerzeichen) auf beiden Seiten. Pre-Fix-Diskriminierung via `git stash` bestätigt (Test schlägt ungefixt fehl). | `tests/test_youtube_parser.py::TestParseYoutubeTitleDocumentedExamples::test_t_low_comma_separated_no_x_keyword_is_not_split_on_bare_hyphen` |
| — (`mapping/artist_overrides.json`, Duo „Miksu & Macloud") | CLOSED (2026-09-02) | P1 | Selber Live-Testdownload: der Schrägstrich in „Miksu/Macloud" wird überall bewusst als Kollaborations-Trenner behandelt (korrektes Verhalten für andere Titel), zerlegte hier aber den echten Duo-Namen — YT-Parser lieferte `parsed_artist="Miksu"` (nur `all_artists[0]`), das gewann laut Prioritätskette gegen den korrekten, unzerlegten `raw_artist`/`channel_name` „Miksu / Macloud" → finaler Artist „MIKSU" (Macloud komplett verloren). Bereits vorhandene Overrides deckten nur die „&"-Schreibweisen ab. Fix: 4 neue Override-Einträge (bare „miksu", „miksu / macloud", „miksu/macloud", „miksu x macloud" → „Miksu & Macloud"), Nutzer-Entscheidung für Mapping-Override statt Code-Änderung an der generischen Slash-Behandlung. Verifiziert mit echten Produktionsklassen (`ArtistProcessor.determine_best_artist()`), Pre-Fix-Diskriminierung via `git stash` bestätigt. | `tests/test_artist_overrides_miksu_macloud_duo.py` |
| — (`filenamefixer.py::extract_main_artist()`, Library-Ordner „Miksu & Macloud") | CLOSED (2026-09-02) | P1 | Nutzer-Fund nach obigem Mapping-Fix: der Tag zeigte korrekt „Miksu & Macloud", der Library-ORDNER blieb aber „Miksu" — `extract_main_artist()` splittet jeden Artist-String an „,"/„&"/„feat."/„ft."/„ x " fürs Ordner-Grouping (sinnvoll für echte Ad-hoc-Kollaborationen), zerlegte aber den bereits als atomaren Duo-Namen normalisierten Override-Wert zurück in „Miksu". Nebenwirkung: `check_library_duplicate()` (`services/duplicate/detector.py`) fand den vorhandenen Ordner dadurch auch nicht mehr (dessen `ArtistNormalizer` liefert seit dem Override-Fix ebenfalls „Miksu & Macloud"). Fix: Artist-Strings, die exakt einem Zielwert aus `artist_overrides.json` entsprechen, gelten als bereits kanonisch/atomar und werden nicht mehr gesplittet — Ad-hoc-Kollaborationen ohne eigenen Override-Eintrag unverändert. Pre-Fix-Diskriminierung via `git stash` bestätigt. Live end-to-end verifiziert (sauberer Testdownload nach vollständiger Miksu-Bereinigung: Ordner korrekt „Miksu & Macloud", erneuter Download desselben Tracks nach URL-Cache-Leerung korrekt als Duplikat über die Content-Cache-Ebene erkannt, keine erneute Datei-Verschwendung, korrekte Telegram-Meldung). | `tests/test_filenamefixer.py::TestBuildFinalPathKnownGroupNames` |
| TAG-02 (`tag_writer.py::write_tags()`, doppelter Artist im ARTISTS-Tag) | CLOSED (2026-09-02) | P1 | Nutzer-Fund am tatsächlich getaggten Testfile im Anschluss an obige zwei Funde: `ARTISTS`-Tag zeigte „Miksu & Macloud; Miksu & Macloud; MACLOUD; makko" — Duo-Name doppelt. Root Cause: `feat_artists` (roh: `["Miksu", "Macloud", "makko"]`, aus `enhanced_metadata_processor.py`s Fallback-Restartisten-Logik, die rohe geparste Artists gegen den bereits normalisierten `final_artist`-String vergleicht — funktionierte zufällig, solange kein Override einen rohen Fragment-Namen auf einen mehrteiligen kanonischen String abbildete) wird in `write_tags()` PRO EINTRAG normalisiert (Zeile 43-46) — „Miksu" trifft über den neuen Override auf denselben String wie der bereits finale Haupt-Artist „Miksu & Macloud", ohne Dedup danach. Fix: nach der Normalisierung case-insensitiv gegen `artist` und untereinander dedupen, `artist` behält immer den ersten Slot. Pre-Fix-Diskriminierung via `git stash` bestätigt. Verwandter, NICHT gefixter Punkt (separater Scope): die rohe-vs-normalisierte Vergleichslogik in `enhanced_metadata_processor.py` (Zeilen ~488-497) bleibt strukturell fragil für zukünftige ähnliche Override-Kollisionen — der Dedup in `write_tags()` fängt das Symptom robust ab, behebt aber nicht die eigentliche Ursache dort. | `tests/test_tag_writer_multi_value_artists_tag.py::TestFeatArtistDuplicateAfterNormalizationIsDeduped` |
| — (Playlist-Downloads ohne Pro-Track-Duplikatprüfung) | CLOSED (2026-09-02) | P1 | Nutzer-Fund beim Playlist-Testdownload: „Zartmann - schönhauser" existierte bereits als Single (`Zartmann/Singles/...`), wurde aber beim Download der Playlist „schönhauser EP" für denselben Track erneut heruntergeladen und als zweite Kopie unter `Zartmann/2025 - schönhauser EP/03 - schönhauser.m4a` abgelegt — der alte Duplicate-Cache-Eintrag wurde dabei durch den neuen (identischer Content-Hash) stillschweigend überschrieben. Root Cause: `_probe_artist_title_for_duplicate_check()` (`klassen/download_handler.py`) liefert für Playlist-URLs bewusst `(None, None)` zurück (dokumentiert, kein Zufallsbug) — `check_for_duplicates()` prüft für Playlists dadurch nur die URL-Ebene der Playlist selbst, nie Artist/Titel der einzelnen enthaltenen Tracks. Fix: `duplicate_detector` wird jetzt optional durch `YoutubeDownloader` → `enhanced_download_with_retry()` → `_process_playlist_download()` durchgereicht; die Playlist-Schleife ruft vor jedem Track-Download `check_for_duplicates()` mit dem eigenen Artist/Titel dieses Tracks auf und überspringt ihn bei Treffer (`DownloadResult(success=False, is_duplicate=True, ...)`, wird von `_register_playlist_track_duplicates()` korrekt nicht erneut registriert). `duplicate_detector=None` (Default) erhält das bisherige Verhalten für Aufrufer ohne eigenen Detector. Pre-Fix-Diskriminierung via `git stash` bestätigt. | `tests/test_playlist_per_track_duplicate_check.py` |
| — (Metadata-Cache-Hit + Duplicate-Cache leer) | OPEN | P2 | Beim gezielten Testen der Library-Fallback-Ebene entdeckt (nicht der ursprünglich gemeldete Fund): wird `url_duplicates.json`/`content_duplicates.json` für einen Track geleert, dessen Library-Datei aber noch existiert UND dessen `metadata_cache`-Eintrag noch vorhanden ist, lädt die Pipeline die Audiodatei komplett neu von YouTube (Netzwerk-Verschwendung), der Metadata-Cache-Hit in `process_single_track()` (`enhanced_metadata_processor.py:283-287`) gibt aber sofort das alte `MetadataResult` zurück — `move_to_library()` (Schritt 16) wird dadurch nie aufgerufen. Die frisch heruntergeladene Datei bleibt unverschoben/verwaist im `DOWNLOAD_DIR` liegen, UND der Nutzer bekommt trotzdem „✅ Download erfolgreich!" mit dem (alten) Library-Pfad gemeldet — irreführend, da nichts Neues geschrieben wurde. Noch nicht untersucht: ob ein bestehender Cleanup (siehe INV `download_executor.py`-Zeile oben, „24h-Start-Sweep") solche verwaisten Dateien erfasst. Zurückgestellt, da eigenständiger, komplexerer Fund außerhalb des ursprünglich angefragten Scopes. | Live-Testdownload 2026-09-02, `/tmp/musicbot_test/downloads/Miksu⧸Macloud x makko - Nachts wach (Official Video).m4a` |
| — (Playlist-Album unvollständig durch Singles-Vorrang) | CLOSED (2026-09-02) | P1 | Folge-Fund zur Zeile oben (Playlist-Downloads ohne Pro-Track-Duplikatprüfung): der dortige Fix übersprang den doppelten Track nur, das Album blieb dadurch unvollständig — zusätzlich griff bereits VOR diesem Check der Metadata-Cache-Kurzschluss in `CacheManager.lookup_playlist_track()`/`_build_result()`, der einen Treffer als „Erfolg" meldet, ohne die Datei in den Album-Ordner zu verschieben. Live beobachtet: „wie du manchmal fehlst" fehlte im Album „Zartmann - dafür bin ich frei EP" (Track 6/8), obwohl bereits unter `Zartmann/Singles/` vorhanden. Nutzer-Entscheidung: Playlist-/Album-Vollständigkeit hat bewusst Vorrang vor der älteren Einzeldatei. Fix: neue Methode `DuplicateDetector.resolve_playlist_single_conflict()` — findet einen Treffer ausschließlich in einem Ordner, der exakt „Singles" heißt (andere, bereits existierende Alben werden nicht angetastet), löscht die Datei physisch und invalidiert den zugehörigen Duplicate-Cache-Eintrag. Läuft in `_process_playlist_download()` jetzt VOR dem Cache-Check, sodass dieser die Datei danach korrekt als fehlend erkennt und der Track normal (neu) heruntergeladen wird. Pre-Fix-Diskriminierung via `git stash` bestätigt. Live end-to-end verifiziert (Album „Zartmann - 11 bis 2", Track „Ritalin 4K" existierte als Singles-Datei aus einem vorherigen Testdownload — Datei korrekt gelöscht, Track neu heruntergeladen, Album 6/6 vollständig). | `tests/test_duplicate_detector_playlist_singles_priority.py`, `tests/test_playlist_singles_priority_pre_check.py` |
| — (`youtube_parser.py::_clean_bracket_content()`, freistehende Auflösungsangabe im Titel) | CLOSED (2026-09-02) | P1 | Direkter Folge-Fund desselben Live-Testdownloads (Album „Zartmann - 11 bis 2"): 3 von 6 Tracks wurden mit angehängter Auflösungsangabe getaggt (Titel „Mama 4K"/„Ritalin 4K"/„Easy 4k" statt „Mama"/„Ritalin"/„Easy"). Root Cause: im Original-YouTube-Titel (z.B. „Zartmann - Mama (prod. by Drumla) [Official Video] 4K") stand „4K" NICHT in eigenen Klammern wie im bereits behandelten Fall „[4K]" — nach Entfernen von „[Official Video]" blieb „4K" als freistehendes, durch Whitespace abgetrenntes Suffix zurück, das keines der bestehenden Klammer-Patterns erfasste. Fix: zusätzliches Pattern in `_clean_bracket_content()` entfernt einen freistehenden Auflösungs-/Qualitäts-Marker (4k/8k/2k/hd/uhd/1080p/720p/480p/360p etc.) am Ende des (Titel-)Strings, unabhängig davon ob geklammert. Pre-Fix-Diskriminierung via `git stash` bestätigt (4 Tests schlagen ungefixt fehl). | `tests/test_youtube_parser.py::TestCleanBracketContent::test_removes_bare_trailing_resolution_marker`, `tests/test_youtube_parser.py::TestParseYoutubeTitleBareTrailingResolutionMarker` |
| — (`progress_tracker.py::ProgressTracker`, Playlist-Progress-State-Erweiterung) | CLOSED (2026-09-02) | P3 | Nutzer-Vorschlag (kein Bug-Report): `compute_progress_message()` erhöhte `processed_items` bisher bei JEDEM Aufruf blind um 1, unabhängig davon ob ein Element tatsächlich fertig war — zu grob für eine Playlist mit mehreren Status-Updates pro Track. Aktuell in Produktion nirgends live verdrahtet (`init_tracker()`/`self.progress_tracker` in `download_utils.py`/`download_handler.py` werden konstruiert, aber `compute_progress_message()`/`set_current_item()` haben 0 Aufrufer im Repo — bereits in `tests/test_progress_tracker.py` dokumentiert), daher kein akuter Produktionsfehler, aber ein Semantik-Fix vor künftiger Verdrahtung. Erweiterung (Architektur bewusst NICHT neu gebaut): neue `completed_items`-Liste + `mark_completed(item_name)` — `processed_items` wird jetzt ausschließlich daraus abgeleitet (Anzahl echter Abschlüsse), `compute_progress_message()` erhöht nichts mehr selbst. Dabei entdeckter Nebeneffekt gefixt: der 0-Elemente-Zweig der Nachrichtenerzeugung zeigte `current_item` bisher nicht an (durch den alten blinden Zähler faktisch nie mit `processed_items == 0` erreichbar, jetzt ein echter, häufiger Zustand — z.B. erster Track lädt noch). Telegram-spezifische Darstellung (Fortschrittsbalken, Emoji-Sektionen) bleibt bewusst Aufgabe der aufrufenden Schicht, nicht Teil dieser Erweiterung. Pre-Fix-Diskriminierung via `git stash` bestätigt (8 Tests schlagen ungefixt fehl). | `tests/test_progress_tracker.py::TestMarkCompleted`, `tests/test_progress_tracker.py::TestComputeProgressMessage` |
| — (Playlist-Fortschrittsanzeige, `ProgressTracker`-Verdrahtung) | CLOSED (2026-09-02) | P3 | Direkter Folgeschritt zur Zeile oben (Nutzer-Freigabe): der bis dahin ungenutzte `ProgressTracker` wird jetzt tatsächlich live verdrahtet, damit Schritt 3/6 ("Audio-Download") während eines Playlist-Downloads laufend aktualisiert statt eine statische Meldung für die gesamte Download-Dauer zu zeigen. `_process_playlist_download()` (`services/downloader/download_utils.py`) erzeugt pro Playlist einen `ProgressTracker`, ruft `set_current_item()`/`mark_completed()` pro Track (`mark_completed()` in einem neuen `finally:`-Block, damit auch fehlgeschlagene Tracks den Zähler korrekt weiterschalten) und ruft `status_callback` (throttled über `tracker.compute_progress_message()`) mit dem `ProgressTracker`-Objekt selbst auf — reiner Zustand, kein vorformatierter Text, `services/` bleibt dadurch weiterhin frei von Telegram-Importen (durch `tests/test_services_layer_boundary.py` automatisiert abgesichert, bleibt grün). Ersetzt die vorherige `status_callback`-Signatur `(chat_id, step, total, message, module)`, die 0 echte Aufrufer hatte. `status_callback` wird jetzt durch `YoutubeDownloader` (`services/downloader/downloader.py`) durchgereicht und in `klassen/download_handler.py` als neue Methode `_on_playlist_progress()` gebunden — baut dort die eigentliche Telegram-Darstellung (Fortschrittsbalken + "⬇️ Aktuell"/"✅ Abgeschlossen"-Sektionen, letztere auf die letzten 8 Einträge begrenzt) und sendet über das bestehende `status_msg`. Pre-Fix-Diskriminierung via `git stash` bestätigt (13 Tests schlagen ungefixt fehl). | `tests/test_playlist_progress_callback.py`, `tests/test_download_handler_playlist_progress_callback.py`, `tests/test_youtube_downloader_telegram_decoupling.py` |
| — (Dead-Code-/Legacy-Cleanup-Audit, 8 Kandidaten) | CLOSED (2026-09-02) | P3 | Nutzer-Auftrag: vollständiges Read-Only-Audit des `main`-Baums auf verwaiste Dateien, danach explizite Freigabe „lösche 1-8". Entfernt: `handlers/error_handler.py` (415 Zeilen, unveraendert seit Initial-Commit, 0 Referenzen, vollstaendig durch `handlers/enhanced_error_handler.py` ersetzt), `handlers/status_handler.py` (1-Zeilen-Stub, 0 Referenzen, ersetzt durch `handlers/enhanced_status_handler.py`), `handlers/adapters/__init__.py` (leeres Scaffold-Verzeichnis — bereits von zwei frueheren Audits als tot markiert, aber nie entfernt, siehe `docs/archive/POST-SERVICES_PROJECT-WIDE_ARCHITECTURE_AUDIT.md` G.4), `helfer/telegram_escaper.py` (0 Referenzen, 0 Tests, kein `__main__`), `Download_Pipeline.txt` (versehentlich eingechecktes rohes Log-Mitschnitt-Artefakt), `migration_report.json` (einmaliger Migrationsreport vom 2025-09-30 mit Pfadverweis auf eine andere Maschine), `mapping/test_genre_map.py` (falsch platzierte Legacy-Testdatei ausserhalb `tests/`, dadurch vom etablierten `pytest tests/`-Workflow nie erfasst — beim Baseline-Lauf mit bloßem `pytest` **7 von 8 Tests bereits fehlschlagend**, bestätigt echten Bitrot statt nur Fehlplatzierung), `__init__.py` (Root, 0 Bytes, 0 Referenzen). Vor/nach der Löschung mit bloßem `pytest` (sammelt auch außerhalb `tests/` ein) verglichen: exakt die 8 entfernten Testfälle fehlen danach, alle übrigen 1829 unveraendert gruen, 0 Regressionen. Alle anderen Audit-Funde (`scripts/*`, `mapping/*.yaml`, `docs/archive/*`) als aktiv/historisch bestaetigt, nicht angetastet. | `git diff --stat` dieses Commits (reine Loeschungen, keine Code-Aenderung) |
| — (Telegram Download-Control-Center: Cancel + Live-Status) | CLOSED (2026-09-02) | P1 | Nutzer-Vorgabe (neue Phase): „📥 Downloads" wird von einer statischen 2-Optionen-Liste zu einem echten Steuerzentrum mit Live-Status und Abbruch-Funktion. Neu: `services/downloader/active_downloads.py::ActiveDownloadRegistry` (prozessweite, threadsichere Registry, auf `RichMenuHandler` verankert — im Gegensatz zu `DownloadHandler`, das pro Update neu instanziiert wird) + `DownloadCancelledError` (`services/downloader/errors.py`, non-retryable). Hard-Cancel: ein Cancel-Check-Hook wird einmalig in `ydl_opts["progress_hooks"]` injiziert (`enhanced_download_with_retry()`) und dadurch automatisch von jedem nachfolgenden yt-dlp-Aufruf (Single **und** jeder Playlist-Track) mitgezogen — bricht den *gerade laufenden* Track sofort ab, nicht erst den nächsten. Soft-Cancel: `_process_playlist_download()` prüft vor jedem Track zusätzlich `is_cancel_requested()` und startet dann keine weiteren Tracks. `DownloadHandler.handle_youtube_links()` registriert/deregistriert (finally-Block) den aktiven Download und unterscheidet „echter Fehlschlag" von „abgebrochen" (eigene, kurze Meldung bei 0 fertigen Tracks; bei Playlists mit bereits fertigen Tracks läuft die normale Metadaten-/Bibliotheks-/Zusammenfassungs-Pipeline weiter — die Zusammenfassung selbst zeigt „🛑 Download abgebrochen" statt des Erfolgs-Headers). Telegram-Seite (`handlers/menu/rich_menu_system.py`, neuer `dl:`-Callback-Präfix, bewusst nicht admin-gated): „🔄 Aktive Downloads" zeigt Titel/Fortschrittsbalken/aktuellen Track/zuletzt abgeschlossene Tracks/verbleibende Anzahl aus demselben geteilten `ProgressTracker`, den auch die automatischen Zwischen-Updates nutzen; „❌ Abbrechen" erscheint nur, wenn tatsächlich ein Download aktiv ist (keine toten Buttons); „ℹ️ Details" zeigt URL/Typ/Laufzeit/Fortschritt. Bewusst **nicht** Teil dieser Ausbaustufe (Nutzer-Priorisierung): „📋 Download-Verlauf"/„🔁 Erneut versuchen" (brauchen persistenten JSON-Verlaufsspeicher, eigener Folgeschritt — aktuell nur Platzhalter „kommt in einem späteren Schritt"), „🔄 Reprocessing" (eigener Bereich laut Nutzer-Entscheidung), Pause/Resume (Nutzer-Entscheidung: technisch deutlich anspruchsvoller als Cancel bei yt-dlp/Playlist-Orchestrierung, unnötige Komplexität). Pre-Fix-Diskriminierung via `git stash` für jeden Teilschritt bestätigt. Vollständige Suite: 1895 passed, 1 skipped, 0 Regressionen. | `tests/test_active_download_registry.py`, `tests/test_download_executor_cancel.py`, `tests/test_playlist_download_cancellation.py`, `tests/test_download_handler_active_download_lifecycle.py`, `tests/test_rich_menu_download_control_center.py` |
| — (Download-Verlauf/Erneut-versuchen, persistenter Speicher) | OPEN | P2 | Bewusst zurückgestellter Folgeschritt der Zeile oben: „📋 Download-Verlauf" (Nutzer-Priorität 3) und „🔁 Erneut versuchen" (Priorität 4) brauchen einen persistenten Speicher der letzten N Downloads pro Chat (Titel, Status, Zeitpunkt) — Nutzer-Entscheidung bei Rückfrage: JSON-persistiert nach etabliertem Muster (wie `duplicate_cache.py`), soll Bot-Neustarts überstehen. Aktuell zeigt „📋 Download-Verlauf" nur einen Platzhalter, „🔁 Erneut versuchen" ist noch gar nicht sichtbar. | `handlers/menu/rich_menu_system.py::_handle_download_history()` |

---

## Format einer Zeile

- **ID**: formale ID falls vorhanden (INV-xx, AE-xx, DUP-xx, DL-xx, MIG-xx, …), sonst „—" mit dem betroffenen Modul/der Methode in Klammern als informeller Bezug.
- **Status**: `OPEN`, `OPEN (DEFER)` (bewusst zurückgestellt, aktiv re-evaluiert), `OPEN (akzeptiert)` (dauerhaft akzeptiertes Risiko, keine erneute Prüfung geplant), `CLOSED (…)` mit Schließungs-Referenz.
- **Prio**: P0–P3 nach CLAUDE.md Abschnitt 23, „—" wenn keine formale Priorität vergeben wurde.
- **Kurzfassung**: ein bis zwei Sätze, genug um die Tragweite einzuschätzen, nicht die volle Begründung.
- **Quelle**: das Dokument mit der vollständigen Analyse/Begründung.
